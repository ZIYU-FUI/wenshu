# Brewfile — wenshu dev environment
# Per AGENTS.md §11.1 "binary tooling via Brewfile + wenshu-devtool hooks chain"
# Verify: `brew bundle --no-upgrade` should exit 0 (= both tools at pinned version)

# Swift static analysis (per AGENTS.md §11.1 approved list; verified latest stable 2026-08-28 via `brew info swiftlint`)
brew "swiftlint", version: "0.65.1"

# Swift code formatter (per AGENTS.md §11.1 approved list; verified latest stable 2026-08-28 via `brew info swiftformat`)
brew "swiftformat", version: "0.62.1"