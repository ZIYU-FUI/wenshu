# CONTEXT · Wenshu (文枢)

> Domain glossary for wenshu. Every agent reads this before working on the project. Update when a new domain word enters the codebase.
> English-only rule applies (see `AGENTS.md` top section). Sole address for the user = "老板".

## Identity

- **wenshu / 文枢** = Apple-stack-exclusive long-form fictional-novel AI authoring platform.
- 老板 拍板 2026-08-06: self-built lightweight AI kernel, no external AI platform calls.
- v1 LLM provider = **minimax cn** (Anthropic-compatible protocol).
- Apple-stack exclusive (macOS / iPad / iPhone). 老板 8/18 拍 = macOS-only single target.
- Project root = `/Volumes/ANAN/Engineering/wenshu/`.

## Architecture

- **Stack** = Swift / SwiftUI + CoreData + single-process coroutine + self-built lightweight AI kernel.
- **Storage** = `.ws` single file = CoreData + attachments, locally self-managed, path `~/Documents/wenshu/<id>/`.
- **Build** = SwiftPM, `.macOS(.v27)` single platform, `Package.swift` as single entry.
- **LSP / LLM** = no external AI platform calls in any code file.
- **NOT used** = UIKit, Tauri, Rust, SQLite, Vue 3, sparse-clone, novel-platform / novel-craft / Hermes-Slate-Desk legacy V0.5.x protocol.
- **NOT used** = iOS / iPadOS / Catalyst adapter (dead code per 老板 8/18 拍).

## Domain words

| Term | Definition | ADR |
|------|------------|-----|
| **Zone** | 6-zone layout top level (Z-TITLE title bar / Z-NOVEL novel management zone / Z-CHAT chat management zone) | ADR-0001 |
| **Band** | Upper / lower two management bands (Y range 39–511, 512–984) | ADR-0001 |
| **Master** | Sketch `SymbolMaster` component (6 truth-source: title bar / zone top toolbar / zone bottom toolbar / zone module / drag line-vertical / drag line-horizontal) | ADR-0002 |
| **Instance** | 13 `SymbolInstance`s mapped 1:1 to SwiftUI subcomponents | ADR-0002 |
| **Drag Splitter** | 5 vertical + 1 horizontal drag lines, NSView + `NSEvent.delta` incremental drag | ADR-0003 |
| **Static Divider** | Non-draggable divider, SwiftUI `Divider` / `Color.frame` (1 PT, `NSColor.separatorColor`) | ADR-0003 |
| **Library** | `WenshuLibrary` Observable + `LibraryStoring` protocol + `FileSystemLibraryStore` truth-source | ADR-0004 |
| **Book** | `Book` data class (with `length` / `idea` fields, 8/18 答 Q2 拍) | ADR-0004 |
| **Bookshelf** | `Bookshelf` data class. Bookshelf is parent, clickable collapse/expand | ADR-0004 |
| **Document** | `Document` data class, 3-class MD document model (chapter / setting / resource library) | ADR-0005 |
| **PT** | Apple layout unit. macOS 27 @ 1x: 1 PT = 1 PX (老板 8/18 拍 1:1 mapping) | — |
| **LayoutTokens** | 18 ratio (0–1) operators + `designW = 1920` + `designH = 984` baseline. `GeometryReader` × ratio = 1:1 adaptive to any window size | ADR-0006 |
| **Toolbar height fixed + VStack stretch** | Top / bottom bar 30 PT hardcoded (`LayoutTokens.toolbarHeight`, v0.15 ticket 008). Width not explicit, SwiftUI `VStack` child view default stretch to full zone width (Apple HIG layout default behavior, v0.16 ticket 01). Never drawn through splitter | — |
| **Drag line NSView + NSEvent paradigm** | 6 drag lines (D_v1 / D_v2 / D_v3 / D_v5 / D_h) share one `NativeSplitter` component (老板 8/18 拍 "drag line = 1 component"). Internal `SplitterHitArea: NSView` subclass handles `mouseDown` / `mouseDragged` / `mouseUp` + `NSTrackingArea` hover + `NSCursor.push/pop`. `SplitterHitAreaRepresentable: NSViewRepresentable` bridges (Apple AppKit truth-source, same as Xcode / Pages / Numbers, v0.16 ticket 03). SwiftUI `DragGesture` + `.pointerStyle` fails on macOS 27 `VStack` parent gesture system | ADR-0003 |
| **Number-pair formula** | 老板 8/18 拍 "anything extra goes to chat zone, use number pairs" = drag line 1 PT visual line distributed to left/right zone. Upper band 4-zone number pairs (200, 558, 762, 400) = 1920. Lower band 3-zone number pairs (200, 1320, 400) = 1920. H number pairs (39, 472, 472, 1) = 984 | ADR-0006 |
| **Drag Splitter** | 5 vertical drag lines (D_v1 / D_v2 / D_v3 / D_v4 / D_v5, 1 PT visual line, `intrinsicContentSize` 1 PT) + 1 horizontal drag line (D_h inert, 老板 8/18 拍 50/50 lock) | ADR-0003 + ADR-0006 |
| **View menu** | `CommandMenu("视图")` top-level menu + "Restore Default Layout" ⌘⇧R + `NotificationCenter.default` bridges `vm.reset()` (Apple HIG paradigm) | ADR-0006 |
| **Appearance Mode** | wenshu appearance 3-state (system / dark / light) — default = macOS system setting. 老板 can override via Settings popover (cmd+,) Picker. `@AppStorage` persists to `UserDefaults` | v0.17 |
| **WenshuCore** | wenshu's own local core library (`Sources/WenshuApp/Core/`) — replaces hermes full capability via Apple stack. 9 truth-source modules: Memory / Skill / Agent / Kanban / Todo / Tools (File / Web / Process / Vision / AV) / Cron / Backup | v0.18 |
| **MemoryStore** | wenshu local SQLite long-term memory (replica of hermes mem0 platform pattern). Actor thread-safe. Schema = `user_id` / `memory_id` / `content` / `created_at` / `updated_at`. Interface = add / search / get / update / delete / count | v0.18 ticket 01 |
| **SkillRegistry** | wenshu local skills loader (simplified hermes `skills_hub` replica). Actor thread-safe. Scans `SKILL.md`, parses frontmatter + body. Interface = list / load / invoke | v0.18 ticket 02 |
| **A2A Protocol (`AgentProtocol`)** | wenshu inter-agent communication protocol (Google A2A spec truth-source). JSON-RPC 2.0 style = `message/send` / `task/get` / `task/list`. Actor in-process simplified | v0.18 ticket 03 |
| **AgentRuntime** | wenshu multi-agent registry + `delegateTask` + broadcast (simplified hermes delegation replica). Main agent defaults to first registered agent | v0.18 ticket 04 |
| **KanbanStore** | wenshu local Kanban (simplified hermes `kanban_db` replica, single table + 7 statuses). State machine = new → triage → ready → running → blocked → review → done (+ failed) | v0.18 ticket 05 |
| **TodoStore** | wenshu local Todo (simplified hermes todo replica). 4 statuses (pending / inProgress / completed / cancelled) + 4 priorities (low / medium / high / urgent) + `dueDate` | v0.18 ticket 06 |
| **FileTools** | wenshu local file tools (read / write atomic / patch 1-spot / search recursive / list). Apple `FileManager` + `URL` truth-source | v0.18 ticket 07 |
| **ProcessTools** | wenshu local process tools (run / `runShell` / `isRunning`). Apple Foundation `Process` truth-source | v0.18 ticket 08 |
| **WebTools** | wenshu local web tools (URLSession fetch + HTML → markdown conversion). Apple `URLSession` truth-source | v0.18 ticket 09 |
| **VisionTools** | wenshu local vision tools (text recognition + image classification). Apple Vision framework truth-source (`VNRecognizeTextRequest` / `VNClassifyImageRequest`) | v0.18 ticket 10 |
| **AVMediaTools** | wenshu local AV media tools (`AVSpeechSynthesizer` reading + duration estimate). Apple AVFoundation truth-source | v0.18 ticket 11 |
| **Cronjob** | wenshu local cron task mgmt (5-field cron expression + simple nextRun estimate). Apple LaunchAgent path truth-source (later can generate plist) | v0.18 ticket 21 |
| **Backup** | wenshu local project backup (copy source dir + ISO 8601 timestamp naming + restore + delete). Apple `FileManager` truth-source | v0.18 ticket 26 |
| **MenuBar (NSMenu install)** | wenshu macOS top menu bar manual install (`NSMenu` truth-source) — 6 items: 文枢 / File / Edit / View / Window / Help. 老板 8/19 truth-source report = `.commands` lazy-populates on macOS 27 (macOS 27 beta bug, vdhamer/Photo-Club-Hub-HTML#248 root cause). Fix = `WenshuAppDelegate.applicationWillFinishLaunching` installs `NSMenu`; SwiftUI `.commands` takes content | v0.20 ticket 01 |
| **Dock Logo (`applicationIconImage`)** | wenshu Dock logo `NSImage` historical snapshot (v0.20 ticket 01 briefly used). 老板 8/20 拍 LOGO path = `/Users/anbaiqiang/Desktop/LOGO/wenshu-icon.icns` (12K–369K byte truth-source file). Runtime safety-net fallback = SF Symbol `book.closed` was removed in v0.20 ticket 04. Current = Apple HIG standard `.app` bundle paradigm (see `macOS27AppIcon` row). Old runtime `applicationIconImage` install code = **dead code**, do not roll back | v0.20 ticket 01 (historical snapshot) |
| **macOS27AppIcon (.app bundle)** | wenshu App icon truth-source = Apple HIG standard Cocoa `.app` bundle paradigm. `Sources/WenshuApp/Resources/AppIcon.icns` (473 KB, 11 representations `ic04/05/07/08/09/10/11/12/13/14/info`) + `Info.plist` `CFBundleIconFile="AppIcon"` + `CFBundleIconName="AppIcon"`. `Scripts/build-app.sh` assembles `build/Wenshu.app/Contents/{MacOS/WenshuApp, Info.plist, Resources/AppIcon.icns}` → ad-hoc codesign → open. `Package.swift` removes `-sectcreate __TEXT __info_plist` linker flag (`.app` bundle uses `Contents/Info.plist`, not linker). Bare `swift run` still works (AppKit process-tile placeholder). v0.20 ticket 04 + 05 | — |
| **NativeSplitter SwiftUI truth-source** | wenshu drag line SwiftUI paradigm (replaces v0.16 / v0.17 NSView paradigm). SwiftUI `Color.clear` + `.contentShape` + `.onContinuousHover` + `.pointerStyle` + `DragGesture` all inside SwiftUI view tree. Visual = 1 PT Apple system separator color. Hover = 3 PT Apple system `controlAccentColor.opacity(0.25)` + shadow. Hit area = 6 PT | v0.20 ticket 02 |
| **ChatZoneView** | wenshu chat zone (`.aiChat`) subcomponent. Parent `ZoneModule .aiChat` case wraps it. **Parent unchanged** (Q51 老板 8/21 23:35 拍 + Q47 ticket 10 lock implementation). Internal = `VStack(spacing: 0) { ChatView; bottom bar HStack { Menu model picker; Spacer; context usage HStack } }`. Bottom bar background = `DesignColor.zoneSurface`. Top = 1 PT `splitterLine` divider (consistent with other 5 zones `ZoneBottomToolbar`). Other 5 zones bottom bar stays `ZoneBottomToolbar` "placeholder text" truth-source, no impact | v0.21 ticket 10 |
| **ChatZoneBottomBar (18 PT inset)** | `ChatZoneView` bottom bar 18 PT horizontal inset truth-source. Model picker `Menu` whole container `.padding(.leading, 14)` + `.menuStyle(.borderlessButton)` with built-in ~4 PT inset (Apple SwiftUI macOS 27 truth-source; swiftinterface confirms `BorderlessButtonMenuStyle` macOS 10.15+ public API; Stack Overflow 74778306 truth-source confirms inset is non-cancellable) = cpu icon left visual = 18 PT. `ChatView` input box `HStack` `.padding(.horizontal, 18)` (send button paperplane right visual = 18 PT). Context usage `.padding(.trailing, 18)` already aligned, unchanged. Bottom padding `.padding(.bottom, 6)` (Q25 placeholder text bottom distance 6 PT truth-source). Numbers 18 / 14 / 6 reused, no new magic numbers | v0.21 ticket 13 + 14 |
| **ProviderApiTab (hermes auth paradigm)** | `SettingView` 4th tab "Provider API". Replica of hermes auth CLI sub-interaction (v2026.8.3 truth-source = add / list / status / reset / remove / logout). Currently ships list + add 2 basics (status / reset / remove / logout deferred to ticket 16+). Inline edit panel (`SecureField "sk-..."` + save button + `ProviderKeychain.saveKeySync` into Apple Keychain, replicas of hermes `auth add --api-key` inline-param paradigm). **Never pop `NSAlert` / `NSWindow` dialog** (老板 2026-08-22 04:50 拍 "do not pop another key-input dialog for paste"). Q20 unchanged = existing `providerTab` (L325–377) + `ProviderKeyPrompt.prompt(for:)` (L493–559) preserved; only retracts `providerTab` L364 trigger logic. Fix v0.21 ticket 16: replace standalone Section with `DisclosureGroup(isExpanded:content:label:)` (Apple SwiftUI 11+ standard API); each provider row expands inline edit panel (老板 2026-08-22 06:00 拍 "should expand at the minimax cn row"). Fix v0.21 ticket 17: `SettingsTab` retract `.provider` case (老板 拍 "provider tab can be deleted"); replace `DisclosureGroup` with `Button(.plain) + .contentShape(Rectangle())` whole-row hot zone (Apple HIG List row hot-zone truth-source) (老板 拍 "remove the `>` before each row"); `currentDraftPreview(for:)` displays "first 8 real chars + ********" (老板 拍 "configured provider re-expand shows first 8 real chars + **** padding"). Fix v0.21 ticket 21: 5 fixes — (1) `SecureField` + save button same row (`HStack(spacing: 8) { SecureField; Button }`), (2) `Info.plist` `CFBundleName` → "设置" (keep `CFBundleDisplayName` = "文枢" for brand Dock display), (3) tab `Picker` remove `.labelsHidden()` so `Label(rawValue, systemImage:)` shows Chinese + ICON (replica of Pages segmented truth-source), (4) provider status fix — unconfigured = "待配置" unified Chinese + configured = "first 8 chars gray text" replaces checkmark ICON (`ProviderKeychain.loadKeySync` real key first 8 chars), (5) expand animation Apple default = `.animation(.default, value:)` + `.transition(.opacity)` (Apple SwiftUI standard default-animation truth-source, 老板 拍 "any Apple-official component with default animation, just use default animation"). `apiExpandedProviders: Set<String>` state controls per-provider independent expand / collapse | v0.21 ticket 15 + 16 + 17 + 21 |
| **wenshu APP bundle paradigm (.app)** | wenshu app launch = Apple HIG standard Cocoa `.app` bundle (Q33 hard constraint). `Scripts/build-app.sh` assembles `build/Wenshu.app/Contents/{MacOS/WenshuApp, Info.plist, Resources/AppIcon.icns}` + ad-hoc codesign. `swift run WenshuApp` bare launch = SwiftPM `-sectcreate __TEXT __info_plist` linker-flag paradigm, **does not register LaunchServices** (system menu bar does not take over wenshu app + LOGO placeholder absent). 老板 2026-08-22 06:18 拍 "menu bar Chinese = APP bundle issue; standard bundle launch has no problem". Settings Scene title truth-source = `{CFBundleDisplayName / CFBundleName / CFBundleExecutable} Settings` (Apple HIG truth-source). Current display = "WenshuApp Settings" (falls back to `CFBundleExecutable`; `CFBundleDisplayName` = `CFBundleName` = "文枢"). Fix Settings Scene title NOT in this ticket scope (老板 拍 "do not get stuck"). Fix uses `build-app.sh` paradigm | v0.21 ticket 17 (item 4 skipped) |
| **WenshuInteractionAnimationPrinciple** | wenshu project hard principle (老板 2026-08-22 06:46 拍 "suggest adding a principle: interaction animations use Apple standard API, continuously elegant"): for any Apple SwiftUI standard API (Apple HIG truth-source) component that supports animation, add Apple default animation (`.animation(.default, value:)` + `.transition(.opacity)`). Do NOT write custom animation (`.spring()` / `.easeInOut()` etc. Apple-undefined animation) unless 老板 拍. Do NOT write `.animation(nil)` to disable animation. Apple SwiftUI default = continuously elegant (老板 原文). 老板 added "any component that can animate, must animate" = expand audit to fix Apple standard animation everywhere | v0.21 ticket 22 |
| **WenshuCommonSenseInteractionPrinciple** | wenshu project hard principle (老板 2026-08-22 拍 "add a principle: not point-by-point implementation; without expanding scope, proactively implement common-sense interaction"): when pocock fixes any wenshu ticket, **proactively implement common-sense interaction** (do not wait for 老板 to拍 each requirement). Example: Apple API implementation = Apple-official-style elegant interaction animation (no custom). Q20 + Q47 + Q26 principles unchanged. **Scope constraint**: fix scope = 老板 拍 requirement scope, do not expand. Agent real fix = proactively complete common-sense interaction truth-source in 老板拍 requirement (e.g. fix text box = 老板 only 拍 "fix text box" = agent proactively adds Apple standard padding + focus ring + keyboard shortcut + accessibility label etc., no new requirements) | v0.21 ticket 22 (addendum) + 老板 2026-08-22 拍 |
| **ProviderKeychain (Apple Keychain)** | wenshu multi-provider API key into Apple Security framework truth-source (CLAUDE.md L42 + Q43 + Q26 principle 1). Actor static methods = `saveKeySync(_:for:)` / `loadKeySync(for:)` / `deleteKeySync(for:)` / `listProvidersWithKeys()`. `kSecClassGenericPassword` + `kSecAttrService = "com.wenshu.app.<provider-slug>"`. Do NOT learn from hermes `~/.hermes/auth.json` file approach (Q40 retract operation chain + Q43 Apple Keychain hard constraint). Tests 5/5 pass = `saveKey then loadKey returns identical` + `repeated saveKey replaces old key (no duplicateItem throw)` + `no key loadKey returns nil` + `empty key throws invalidKeyFormat` + `listProvidersWithKeys lists providers with keys` | v0.21 ticket 04 + 15 |
| **MiniMaxResponseShape (union block decode)** | wenshu MiniMax API response `content` array = union of text / thinking (CoT) / tool_use / unknown blocks (Apple Anthropic-compatible protocol extension). `MiniMaxBlock` enum (Swift Codable truth-source) dispatches decode by `type` field (Q47 lock Codable enum). `displayText` helper extracts user-visible text blocks and concatenates; `thinkingText` extracts CoT thinking blocks into `ChatMessage.thinking` field (Apple HIG footnote UI). M3 model returns text blocks only. M2.x / future MiniMax thinking-capable models return thinking block prepended at `content[0]` (chain-of-thought paradigm). Tolerate unknown type via `.unknown(type:raw:)` case (Q26 principle 1 graceful degradation, never throw on protocol new field). `MiniMaxUsage` adds `cache_creation_input_tokens` / `cache_read_input_tokens` optional fields (Anthropic prompt caching paradigm) | v0.21 ticket 39 |
| **ChatThinkingFootnote (Apple HIG footnote)** | `ChatMessageView` renders 文枢 reply; when `ChatMessage.thinking` non-empty → add `DisclosureGroup` + brain SF Symbol + "AI thinking process" label (`.tertiary foregroundStyle`) above message. Apple default animation `.animation(.default, value: thinkingExpanded)` + `.transition(.opacity)` (Q58.4 Apple SwiftUI standard). User taps ⏵️ to expand CoT thinking content (`.font(.caption).foregroundStyle(.secondary)`). Distinct from `ChatMessage.isPlaceholder`: placeholder = unanswered state with `person.crop.circle.badge.questionmark` + `.symbolEffect(.pulse)`; thinking footnote = answered state collapsed display. `source = .wenshu` only shows thinking (user / system hidden). Q20 + Q48 unchanged user-facing UI copy | v0.21 ticket 39 |
| **wenshuChatErrorTranslation (DecodingError Chinese fallback)** | `ChatViewModel.send()` catch differentiates `DecodingError` from other `Error`. `DecodingError` shows Chinese "模型 <m> 返回数据格式不支持 (DecodingError). 真因查 stderr [wenshu.chat] decoder error 行." via `source = .system` (Q36 `.error` source truth-source). Other `Error` reuses Swift Foundation `localizedDescription` translation ("未完成操作" etc.). Never expose Swift Foundation native English error string to user | v0.21 ticket 39 |
| **ChatZoneContextBinding (Observable instance sharing)** | wenshu chat zone bottom bar right context usage UI = bound to `ChatViewModel.contextUsed` (Apple `@Observable` auto-propagate), NOT bound to `ChatZoneView` own `@State`. `ChatZoneView` holds `ChatViewModel` instance (`@State private var vm`) + shares with `ChatView` (vm injection). `ChatView` init adds overload: `vm: ChatViewModel? = nil` (if passed, use; if nil, fall back to original ticket 06 self-created path; default nil for backward compatibility with old callers). `ChatZoneView` bottom toolbar `Text` + `ProgressView` + `tint` all read `vm.contextUsed` + `vm.contextMax` (Q47 lock SwiftUI `@Observable` shared vm paradigm). Q51 parent unchanged: `ZoneModule .aiChat` case body + `ChatZoneView` `VStack { ChatView; toolbar }` structure unchanged. Q20 unchanged: `ChatViewModel.send()` body / `.recomputeContextUsed()` impl / ticket 38 model switching wire | v0.21 ticket 40 |
| **ChatZoneAutoScroll (`defaultScrollAnchor` + content-based `onChange`)** | wenshu chat zone auto-scroll = Apple SwiftUI 14+ `ScrollView.defaultScrollAnchor(.bottom)` fallback (Apple HIG truth-source, auto-stick to bottom on content change) + `onChange(of: vm.messages.last?.content)` content-based trigger `scrollTo` (fix placeholder UUID reuse causing count unchanged issue; count-based `onChange` does not trigger). Remove `withAnimation` wrap (Apple SwiftUI 14+ `defaultScrollAnchor` auto-handles animation). Q44 swiftinterface truth-source = `ScrollView.defaultScrollAnchor(_:)` API truly exists (Apple SwiftUI macOS 14+). Q20 unchanged `ChatViewModel.send()` body / placeholder UUID reuse logic (Q51 parent unchanged `ChatView` body `VStack` structure). Q47 lock Apple SwiftUI standard modifier paradigm, do not switch framework | v0.21 ticket 41 |
| **ChatZoneTabSwitching + AppStoragePickerSync** | wenshu chat zone top bar 3 tabs real switching (老板 2026-08-22 06:22 拍 backlog 20; before fix = placeholder SF Symbol non-responsive) = `ChatZoneTabBar` struct (Apple HIG truth-source, `Button(.plain)` + `contentShape(Rectangle())` whole-row hot zone (ticket 17 + 21 already fixed paradigm) + `.foregroundStyle(Color.accentColor)` selected-state highlight + Apple default animation `.animation(.default, value: selectedTab)` (Q58.4) + `toolbarHeight` 30 PT consistent with other 5 zones `ZoneTopToolbar`). `ChatZoneTab` enum (`.chat` / `.search` / `.settings`; 1st case icon `person.crop.circle.badge.questionmark` 老板 拍 "use robot" = ticket 30 + 33 robot face already aligned, 2nd / 3rd case keep original icon). `ChatZoneStubView` struct (Apple HIG truth-source, `VStack` centered + large icon 48 + "开发中" text, 老板 拍 "leave for now, implement later"). `ChatZoneView.body` `VStack` expansion = `ChatZoneTabBar` + `Group { switch selectedTab }` + bottom toolbar `HStack`. **picker ↔ `UserDefaults` sync fix (out-of-scope bug)** = `ChatZoneView.currentModel` → `@AppStorage("wenshu.llm.model")` (Apple SwiftUI truth-source, single source `UserDefaults`, bidirectional auto-sync; before fix `ChatZoneView.currentModel` was `@State` not bound to `UserDefaults` + `ChatViewModel.currentModel` was init default read `UserDefaults` once = after picker switch two state chains break; 老板 screenshot ticket 39 discovered). `Menu` `Button` action no longer manually set `UserDefaults` (`@AppStorage` auto-writes). Q47 lock Apple HIG truth-source (`@AppStorage` / `Button(.plain)` / `contentShape` / Apple default animation), do not switch framework. Q51 parent unchanged = `ZoneModule .aiChat` case body + `ZoneTopToolbar` (Q20 ticket 008) unchanged. Q20 unchanged = `ChatViewModel.send()` / ticket 38 wire / ticket 39 union decode / ticket 40 binding / ticket 41 auto-scroll / ticket 42 outer shell removal / `ChatView` body `VStack` structure | v0.21 ticket 4... [truncated] |
| **WenshuAgentIdentity (文枢主调度身份, v0.23 拆 main + sub)** | `WenshuAgentIdentity` (main agent) + `SubAgentIdentity` (5 sub-agents: researcher / writer / analyst / archivist / auditor). `WenshuAgentIdentity.systemPrompt` (~700 tokens, 6 sections: Identity / Persona / Capabilities / Limitations / Workflow / Output format), `capabilitiesList` (15 capabilities covering all 14 replica modules + 1 tts read-aloud), `forbiddenTokens` (12 pollution words — listed in prompt for reinforcement), `displayName` ("文枢 (wénshū)"). Sub-agent system prompts derived from `SubAgentIdentity.systemPrompt(name:)` factory. Both prepend to every LLM call in `WenshuConductor.handle()` L1 (intent) + L3 (synthesis) as system message, alongside the pollution-defense `systemPromptEnglishOnly` constant from v0.07.2 (which is the first system segment per `WenshuVerifier.send` design). Per Anthropic "Building effective agents" 2024-12 best practice: agent needs Identity + Persona + Capabilities + Limitations / Workflow / Output format = 6 sections. **Note**: v0.22 ticket 001 originally created `WenshuConductorIdentity` (single class), but v0.23 ticket 001 split it into `WenshuAgentIdentity` + `SubAgentIdentity` for multi-agent dispatch parity (hermes pattern). | v0.22 ticket 001 → v0.23 ticket 001 |
| **SubAgentIdentity (5 sub-agent 领域专家模式)** | 5 domain expert sub-agents under 文枢 (main conductor) = 6 agents total (1 + 5). Per agent: `systemPrompt(name:)` (~500-1500 tokens, includes Identity / Capabilities / Limits / Output format), `tools(name:)` (allowed tool list), `displayName(name:)` (Chinese for debug). 5 sub-agents: **Researcher** (找资料: search / web / linkgraph), **Writer** (写: composer / template / wordcount), **Analyst** (分析结构: outline / bases / graph), **Archivist** (管记忆: memory / bookmark / backup), **Auditor** (质量门控 read-only: memory). Each sub-agent gets independent system prompt; no sub-agent-to-sub-agent direct comm (all via conductor). Boss 2026-08-23 拍: "一个类别的工作, 交给一个专职人员" | v0.23 ticket 001 |
| **TaskGroupParallelDispatch (并行派发架构)** | `WenshuConductor.handle()` step 3 = `withTaskGroup(of: (String, String).self) { group in ... }` replaces serial for-loop. All selected sub-agents run concurrently (latency = max, not sum). Kanban tasks added BEFORE TaskGroup so all parallel tasks see consistent state. TaskGroup auto-cancels on scope exit (Swift 6 strict concurrency). Token cost: 2 baseline LLM calls (intent + synthesis) + N sub-agent calls (parallel) + optional 1 auditor call = 3-6x baseline when 1-3 sub-agents run. 1 sub-agent fail does not block others (each in own task, errors captured) | v0.23 ticket 002 |
| **Auditor (质量门控 sub-agent)** | 6th agent = quality gate, NOT user-triggered. Auto-runs if Writer or Analyst in sub-agent selection. Reads sub-agent outputs + canonical settings via memory (read-only); emits verdict `{verdict: pass|warn|fail, confidence: 0-1, issues: [{type, severity, message}], fix_suggestion}`. Verdict appended to subResults for synthesis consumption. Inspired by Anthropic "verifier" pattern (Building effective agents 2024-12) | v0.23 ticket 002 |
| **MultiAgentRouting (intent → 5 sub-agents)** | L1 (intent classify) prompt: 5 sub-agent names (researcher / writer / analyst / archivist / auditor) instead of 5 module names (search / outline / wordcount / linkgraph / composer). Returns JSON array of 1-3 agent names. Unknown agent names filtered via `SubAgentIdentity.Name(rawValue:) != nil` check. Default 2-3 sub-agents per query. Boss 2026-08-23 拍: "约束归谁管理" → Auditor auto-runs if writer/analyst in selection (governance pattern) | v0.23 ticket 002 |
| **ProviderResolution (动态 key + baseURL 解析)** | `WenshuVerifier` 不再 `init` 时 capture `apiKey` / `baseURL` — 每次 `send()` call 时通过 `resolveCredentials()` 从 `UserDefaults "wenshu.provider.slug"` (override) + `ProviderCatalog.by(slug:)` (provider 配置) + `AppleKeychainStore.loadKeySync(for:)` (key 真值) 三方动态解析。无 cache,Settings 改 key 立即生效。配套 `WenshuLLMModel.providerSlug` 计算属性声明每个 model 的 provider (m3 / m2 / reasoning → "minimax-cn",未来 claudeSonnet → "anthropic", gpt4o → "openai")。修复 bug: 用户切 model 后主 + 5 sub-agent 必须同步切 provider / key,否则 401 / connection refused 卡死 (boss 2026-08-23 拍) | v0.23 ticket 010 |
| **AvailableModelsDiscovery (multi-provider 模型分组发现)** | ChatView 底栏 ModelMenu 从 flat `availableModels: [String]` 改为 `availableSections: [AvailableProviderModels]` (v0.23 ticket 011.002)。`AvailableModelsDiscovery.loadFromKeychain()` 扫 `Provider.all` (11 curated providers),过滤掉 `requiresOAuth` 的 providers (openai-codex / copilot / copilot-acp / xai-oauth 等 OAuth flow),保留 `AppleKeychainStore.loadKeySync(for:)` 有 non-empty key 的 providers,返回 `{provider, models: provider.defaultModels}` 数组。Menu 渲染用 `Section(provider.name) { ForEach(models) }` 按 provider 分组,Apple HIG menu selection pattern 显示 checkmark。`Provider.defaultModels` 是 curated 静态列表 (minimax-cn: [MiniMax-M3, MiniMax-M2, MiniMax-Reasoning], anthropic: [claude-opus-4.8], openai-codex: [gpt-5, gpt-5-mini, o4-mini], openrouter: [anthropic/claude-opus-4.8, deepseek/deepseek-v4-pro] 等)。Boss 2026-08-23 拍: '我配了三个厂家的 key, 那模型切换就应该分组展示我三个厂家的可用模型的合集, 供我选择' | v0.23 ticket 011 |
| **SubAgentPermissions (hermes DELEGATE_BLOCKED_TOOLS parity)** | `SubAgentPermissions` enum 集中管理 sub-agent tool 调用权限 (boss 2026-08-23 拍: '去 hermes 源码里扒')。`writeOnlyBlocked: Set<String>` = 4 个 hermes DELEGATE_BLOCKED_TOOLS (delegate_task / clarify / send_message / cronjob),sub-agent 调用这些 tool 任何 op 都拒。`readOnlyAllowed: Set<String>` = 1 个 (`memory`),sub-agent 只能 read-only (op=read/search 允许,op=add/write/delete/patch/update 拒)。`AgentCaller` enum (`.main` / `.subAgent(name:)`) 标记 invokeTool caller context。`WenshuConductor.invokeTool(name:input:caller:)` 接 `caller` 参数,sub-agent 调用时过 `SubAgentPermissions.checkPermission(tool:op:)` gate。配套修改:`SubAgentIdentity.archivist.tools()` 移除 `memory` (hermes: sub-agent 永不写 shared memory),`Auditor.tools()` 保留 `memory` (read-only via system prompt)。Source: github.com/NousResearch/hermes-agent/blob/main/tools/delegate_tool.py line 39 DELEGATE_BLOCKED_TOOLS | v0.23 ticket 012 |
| **MemoryWriteGate (hermes _apply_write_gate parity)** | `MemoryWriteGate` enum 集中管理 memory write 权限 (boss 2026-08-23 拍: '去 hermes 源码里扒')。`evaluateAdd(content:)` 决策: empty content → `.block`, content > 500 chars → `.stageForApproval` (hermes 行为: 写 pending queue 等用户 approve), else `.allow`。`evaluateReplace(content, oldText)` empty oldText → `.block`; length grows > 100% OR leading 16 chars differ → `.stageForApproval`; else `.allow`。`evaluateRemove()` → ALWAYS `.stageForApproval` (destructive 必 review)。`MemoryWriteDecision` enum: `.allow` / `.block(reason)` / `.stageForApproval`。`WenshuConductor.addMemory` / `MemoryManager.sync` 走这个 gate。 Source: hermes-agent/tools/memory_tool.py:949 _apply_write_gate | v0.23 ticket 013.001 |
| **FileTools.isBlockedDevice + symlink hop (hermes parity)** | `FileTools` 新增 `isBlockedDevice(path:)` 方法: 阻止 `/dev/*` 任意路径 (会 hang read 或暴露数据) + `/proc/*` 敏感后缀 (`/environ` / `/cmdline` / `/maps` / `/smaps` / `/smaps_rollup` / `/numa_maps` / `/mem` / `/auxv` / `/pagemap` / `/fd/0/1/2`)。新增 `pathHasBlockedSymlink(path:)` 用 `URL.resolvingSymlinksInPath()` 迭代 resolve symlink hops (POSIX MAXSYMLINKS=40),每个 hop + final resolved path 都过 `isBlockedDevice`。`pathDenied()` 现在调两层: 1) isBlockedDevice, 2) pathHasBlockedSymlink, 3) prefix/suffix deny-list。Source: hermes-agent/tools/file_tools.py:520 _is_blocked_device_path + symlink-hop defense | v0.23 ticket 013.002 |
| **KanbanTask metadata (hermes _task_summary_dict parity)** | `KanbanTask` 扩展 5 个 hermes 元数据字段: `priority: Int` (default 5, hermes 0-10 scale), `assignee: String?` (e.g. 'writer' / 'wenshu-conductor'), `startedAt: Date?` (auto-set on transition to .running, COALESCE preserves first start), `completedAt: Date?` (auto-set on .done / .failed), `modelOverride: String?` (e.g. 'MiniMax-M3')。DB schema: ALTER TABLE ADD COLUMN for 5 new fields (idempotent via try?, 老 DB 兼容)。`bootstrap()` CREATE TABLE 包含新列 + 5x ALTER TABLE migration。`add()` + `transition()` + `get()` + `list()` 全部 bind/read 10 列。`transition()` 动态 SQL based on which timestamps 需设 (started_at + completed_at 各自 COALESCE / SET)。Source: hermes-agent/tools/kanban_tools.py:485 _task_summary_dict | v0.23 ticket 013.003-004 |
| **MemoryConsolidator (hermes char budget + consolidation parity)** | `MemoryConsolidator` actor enforces `MemoryBudget` (默认 `memory_char_limit = 2200` / `user_char_limit = 1375` / `maxConsolidationFailuresPerTurn = 3` 完全对齐 hermes)。`checkAndConsolidate(entries:)` → `.withinBudget` (under limit) / `.consolidated(newTotalChars, droppedCount, mergedCount)` (LRU drop oldest until within) / `.failed(reason, currentTotalChars)` (terminal failure after max retries)。`resetConsolidationFailures()` 在 turn start 调用 (hermes pattern)。Counter clamp to `maxConsolidationFailuresPerTurn` in terminal failure path (hermes #42405: failed side effect must never block reply)。Source: hermes-agent/tools/memory_tool.py MemoryStore class init | v0.23 ticket 013.005 |
| **KanbanRole (hermes _require_orchestrator_tool parity)** | `KanbanRole` enum: `.orchestrator` / `.worker(taskId:)` 标识 caller context (mirrors hermes `HERMES_KANBAN_TASK` env var)。Capability flags: `canCreate` / `canTransitionOwnedTask` / `canRequestReview` / `canDelete` (orchestrator 全 allow, worker 只能 transition own task)。`KanbanOp` enum: `.create` / `.transitionOwned` / `.requestReview` / `.delete`。`KanbanRoleGuard.checkPermission(role:op:)` 仿 hermes `_require_orchestrator_tool` — 返 nil if allowed, reason string if blocked (create/delete/requestReview 仅 orchestrator 允许)。Source: hermes-agent/tools/kanban_tools.py:467 _require_orchestrator_tool | v0.23 ticket 013.006 |
| **CronPromptScanner (hermes _scan_cron_prompt parity)** | `CronPromptScanner.scan(prompt)` 扫描 cron job prompt 的 LLM prompt injection 攻击向量。Block 13 invisible unicode chars (ZWJ / ZWNJ / ZWSP / BOM / word joiner / RTL / LRO / RLO / PDF marks / soft hyphen / line separator / paragraph separator)。Warning (非 block) high emoji density (> 5% of chars)。使用 `unicodeScalars` 迭代避免 grapheme clustering (ZWJ + adjacent chars form 1 grapheme cluster → for-char loop miss). Report U+XXXX codes for traceability. Source: hermes-agent/tools/cronjob_tools.py:260 _scan_cron_prompt + _check_invisible_unicode | v0.23 ticket 013.007 |
| **SkillMeta + trust policy + quarantine (hermes parity)** | `SkillTrustLevel` enum 3-tier: `.builtin` (wenshu 自带, 永远允许) / `.trusted` (boss 拍板, 永远允许) / `.community` (需 user 显式 approve via `userApprovedCommunity: Set<String>`)。`SkillSource` enum: `.builtin` / `.github` / `.local`。`SkillMeta` struct: name / description / source / trustLevel / path / installedAt。`SkillTrustPolicy.shouldAllow(skill:, userApprovedCommunity:)` 返 Bool。`SkillQuarantine` enum: `quarantinePath` (默认 `~/.hermes/wenshu/skills/quarantine/`) + `quarantineSkill(skillPath:)` 移动到 quarantine + `promoteSkill(quarantinedPath:, installedDir:)` user-verify 后 promote。Source: hermes-agent/tools/skills_hub.py:131 SkillMeta + audit log | v0.23 ticket 013.008 |
| **MemoryManager (hermes prefetch + sync parity)** | `MemoryManager` actor implements pre-turn prefetch + post-turn sync 模式 (hermes agent/memory_manager.py)。`prefetch(userMessage:)` substring LIKE search via MemoryStore, char-budget enforced (default 2200) → 返 `.empty` / `.prefetched(memories, totalChars)`。`sync(userMessage, assistantResponse:)` concat → `MemoryWriteGate.evaluateAdd` (ticket 013.001) → `.synced(writtenCount, totalChars)` / `.stagedForApproval` / `.blocked(reason)`。`queuePrefetch(userMessage:)` detached background task, `takeQueuedPrefetch()` 拿 next-turn result (hermes `queue_prefetch_all` pattern)。 Boss 8/23 '我之前说过 X' 场景: prefetch substring 匹配 → top-N memories → 注入 system prompt context, 不再 load all messages。 | v0.23 ticket 013.009 |
| **AsyncDelegation infrastructure (hermes parity)** | `AsyncDelegationRegistry` actor tracks background sub-agent delegations (hermes tools/async_delegation.py)。`BackgroundDelegationHandle` struct: id / agentName / userMessage / state (.pending / .running / .completed / .failed / .timeout) / startedAt / completedAt / result。Registry methods: `register` / `update` / `get` / `markCompleted` / `markFailed` / `runningDelegations` / `recentCompleted` / `cleanup`。Config: `maxRetained = 50` (hermes `_MAX_RETAINED_COMPLETED`), `durableRetentionSeconds = 7 days` (hermes 7 day retention), LRU eviction。WenshuConductor v0.23 仍用同步 TaskGroup; async delegation API 已就位, UI 集成 deferred v0.24+ (boss 8/23 '5 分钟 sub-agent 不卡 user' 场景)。 | v0.23 ticket 013.010 |
| **ReadOnlyShell (hermes set_approval_callback parity)** | `ProcessTools.runReadOnlyShell(command:)` 允许 14 个 read-only commands: `ls / cat / head / tail / wc / grep / find / stat / file / pwd / echo / date / whoami / uname`。3-layer defense: (1) `readOnlyCommands` whitelist check on first token, (2) reject 18 shell metacharacters (`; & | > < \` $ ( ) { } * ? [ ] ! ~ #`), (3) check file path args against `FileTools.pathDenied` (gap 2 protection). `runShell` 仍 always deny (boss 8/23 hard rule '禁止通过聊天改系统' preserved for write/destructive). 演化: read-only allow 跟 hermes pattern, write/destructive still 严. Source: hermes-agent/tools/terminal_tool.py:288 set_approval_callback | v0.23 ticket 013.011 |

## Project conventions (hard constraints)

- Forbidden vocabulary (修真 / 渡劫 / 筑基 / 返虚 / 结丹 / 金丹 / 元婴 / 飞升 / 天劫 / 雷劫 / 心魔 / 魔障) all banned. Use 修 / 改 / fix / 替换 / 调整.
- Sole address for 老板 = 老板. No earlier honorific forms (老板 already purified in v0.07).
- No decorative emoji. No open-ended prologues / endings. No oversized headings.
- First line = fact. Last line = fact.
- Forbidden neutral words (可 / 应当 / 或许 / 可能 / 应该 / 建议 / 考虑 / 试图 / 尽量 / 大概 / 也许 / 或 / 任意 / 大概率 / 通常 / 一般来说) all banned. Use certain words (是 / 否 / 行 / 不行 / 可以 / 不可以 / 不变 / 变).
- Apple-stack exclusive → any generic placeholder / iOS / iPadOS / Catalyst adapter = dead code = delete.

## See also

- `AGENTS.md` — project baseline §11 + cross-role address hard constraint §12.
- `CLAUDE.md` — context auto-loaded when pocock starts.
- `docs/agents/issue-tracker.md` — local markdown issue tracker config.
- `docs/agents/triage-labels.md` — 5 canonical triage roles.
- `docs/agents/domain.md` — single-context rule.
- `docs/adr/` — architecture decision records.
| **WenshuLLMError LocalizedError (boss v0.24 ticket 015.001)** | `WenshuLLMError: Error, LocalizedError` (Sources/WenshuApp/Core/Agent/WenshuVerifier.swift). `errorDescription` returns 中文 human message: missingAPIKey → 'API key 未配置. 请在 Settings → Provider 配置 API key.'; invalidBaseURL(url) → 'Provider base URL 无效: \(url). 请在 Settings 检查 provider 选择.'; httpError(statusCode, body) → 'LLM HTTP \(statusCode): \(brief)' (body truncated to 120 chars). Boss 2026-08-24 fix (commit aa7caca7f). 原因: 原来是 plain Error enum, SwiftUI '\(error)' interpolation 报 default description. | v0.24 boss验收 ticket 015.001 |
| **Keychain -34018 fix (boss v0.24 ticket 015.002)** | `Sources/WenshuApp/Core/Provider/ProviderKeychain.swift` saveKeySync: removed `kSecUseDataProtectionKeychain: true` from addQuery dict. iOS-only key on macOS requires explicit entitlement (kSecAttrAccessGroupFile) and triggers -34018 errSecMissingEntitlement on ad-hoc signed apps. Default macOS file-based keychain works without entitlement. Boss 2026-08-24 fix (commit 4f4a22f17). Same commit also rewired `ChatView.loadAvailableModels()` fallback from `WenshuLLMModel.allCases` (3 hardcoded) to `AvailableModelsDiscovery.loadFromKeychain()` (multi-provider, per ticket 011 spec). | v0.24 boss验收 ticket 015.002 |
| **无模型可用 placeholder (boss v0.24 ticket 015.003 + 015.004)** | wenshu model picker UX: when UserDefaults 'wenshu.llm.model' is empty (no key configured), UI shows '无模型可用' (literally 'no model available') instead of defaulting to 'MiniMax M3'. All 6 init sites for currentModel default: App.swift SettingView (line 222) + ChatZoneView (line 1281) + model menu text (line 1349) + fallback section (line 1358) [boss commit c83a131b2], ChatView.swift ChatViewModel (line 76) + send() fallback (line 154) [assistant drift catch 0e306f6b4]. If user configures key + selects model, currentModel is set + '无模型可用' is replaced by selected model display. | v0.24 boss验收 ticket 015.003 + 015.004 |
| **Doc drift catch process (boss v0.24 ticket 015.004)** | Process pattern: when a commit's message claims X files modified but the actual `git show --stat` only changes Y files (X > Y), the un-fixed files become latent bugs. wenshu v0.24 catch: boss commit c83a131b2 message said '4 default locations' but only App.swift (1 file) was modified. ChatView.swift still had hardcoded `WenshuLLMModel.m3.rawValue` fallback. Caught by regression test in commit 351704a08 ('ChatView.swift ChatViewModel.currentModel default = '''). Future fix: add CI check that compares commit message file count vs `git show --stat` count. Caught 015.004 fixed by assistant commit 0e306f6b4. | v0.24 boss验收 ticket 015.004 (separate row from 015.003+004 placeholder) |
| **MiniMax-M3 context window = 1_000_000 tokens (boss v0.24 ticket 015.006)** | wenshu ChatView bottom-right context display shows '0 /1.0M' (= 1M token cap). Per official MiniMax docs (https://www.minimax.io/models/text/m3 + Anthropic-compatible API docs) MiniMax-M3 = 1M tokens context window (max output 512K). Empirical on public anthropic-compatible endpoint ~512K input cap (per hermes-agent issue #37289) = vendor enforces lower than marketed (separate vendor issue). Live MiniMax cn `/v1/models` does NOT return context_length field (= no API to query dynamically, Boss 8/25 OOB confirmed). Hardcoded fallback = `contextMax: Int = 1_000_000` at ChatView.swift:79. Old value 131072 was MiniMax-M2 series (= wrong model). Future ticket 015.008: models.dev lookup (hermes hybrid strategy) for dynamic refresh. | v0.24 boss验收 ticket 015.006 |
| **Zone bottom toolbar 6-zone unified (boss v0.24 ticket 015.012)** | wenshu 6 zones each have a 30 PT bottom toolbar (= per Boss plan A 2026-08-25 OOB, restored after v0.24 boss验收fix accidentally removed outer ZoneTopToolbar/ZoneBottomToolbar). Per-zone status info (all Chinese per Boss 8/25 'UI 全中文' rule): .projectSidebar='书架: N', .projectPreview='章节: 0', .editor='字数: 0', .specializedTools='工具就绪', .aiDynamic='看板' (inner tab has actual progress). .aiChat keeps its own internal ChatBottomToolbar (per ticket 10, no duplicate outer toolbar). All non-chat zones share struct ZoneBottomToolbar with String status parameter (= Standards F3 fix = init(status: String = '') is single source of truth). Real data wiring for .projectSidebar = WenshuLibrary.shelves.count (immediate); .projectPreview / .editor / .specializedTools defer to ticket 015.013. UI status strings are Chinese (= Boss product direction / AGENTS.md §11 carve-out); code comments + commit messages remain English ( per AGENTS.md §11 hard rule). | v0.24 boss验收 ticket 015.012 |
| **Splitter ratio at initial state (boss v0.24 ticket 015.021)** | wenshu 6-zone layout splitter positions: D_v1/D_v2/D_v3/D_v5 vertical splitters + D_h horizontal splitter. Boss 8/25 OOB core spec: '右上区和右下区这两个区的默认启动的时候. 占总宽度的比例是一样的' = rightmost zones (.specializedTools upper + .aiDynamic lower) have same width/totalWidth ratio. LayoutTokens.toolsWRatio == LayoutTokens.dynamicWRatio == 400/1920 at initial state satisfies this (= both zones 400/1920 * totalWidth = same fraction). Per Boss '不要纠结线的问题' = earlier overlay approach (ticket 015.018 commits 3fab7350d + 6f3019e50) abandoned. Splitter X position discrepancy (D_v3 X = 1514/1920 vs D_v5 X = 1518/1920, 4 PT off) acknowledged but not fixed per Boss拍. areRightSplittersAligned() predicate removed in commit ee801d0dd (= dead code after overlay revert). | v0.24 boss验收 ticket 015.021 |
| **projectSidebar right status book count (boss v0.24 ticket 015.019)** | projectSidebar bottom toolbar right side shows '书: N' (= total book count across all shelves, per Boss 8/25 sixth OOB '右边的字数改成, 书 N (取实际的数)'). WenshuLibrary gained private(set) var bookCount: Int + public func recomputeBookCount() (= iterates all shelves, sums books via store.loadBooks). Called in init() after loadShelves + autoSelect. ZoneBottomToolbar struct gained rightStatus parameter + init(status: String = '', rightStatus: String = '') backward-compat. ZoneModule gained private func rightStatus(for: ZoneSlot) returning '书: \(library.bookCount)' for .projectSidebar only; empty for others (= WordCountInlineLabel fallback preserved for chat zone word count + 3 other zones). ProjectSidebar tab badge = '书架: N' left side + '书: N' right side. UI 全中文 preserved (Boss 8/25 'UI 全中文' rule). Future ticket 015.015 wires recomputeBookCount() to addShelf/addBook/deleteBook/deleteShelf mutations. | v0.24 boss验收 ticket 015.019 |
| **projectSidebar single tab 书架 (boss v0.24 ticket 015.020)** | projectSidebar top tab bar shows only 1 tab (= '书架' with books.vertical.fill SF Symbol icon). Per Boss 8/25 seventh OOB '顶栏 3 个 teb 只保留第 1 个, 后面两个隐藏. 现有的收藏和模版没有用. teb1 改成 书架, 选一个合适的 ICON 替换'. Original 3 tabs [大纲/收藏/模板] filtered to [书架] only (= outline view). BookmarkPanel + TemplatePicker views preserved in codebase (= Boss拍 '回头实现需要的功能'). Icon 'list.bullet.rectangle' -> 'books.vertical.fill' (= Apple SF Symbol = solid bookshelf, semantic match for 书架; also internally consistent with .research icon in Domain/Document.swift:80). Tab persistence via @AppStorage('wenshu.tabIndex.projectSidebar') preserved (= single tab = always index 0). projectSidebar 底栏 per Boss plan A: left = '书架: N' + right = '书: N' (= from ticket 015.019). UI 全中文 (Boss 8/25 rule). | v0.24 boss验收 ticket 015.020 |
| **Library outline FCP 3-level (boss v0.24 ticket 015.022)** | projectSidebar library outline refactored to FCP Browser style 3-level disclosure (= shelf > book > book structure categories). Per Boss 8/25 ninth OOB '参考 FCP 重做库目录书. 现在安百强=书架名, 里面的项目就是各本书名. 书下的目录就是我们之前定义的, 书的结构目录, 世界观, 角色, 资料库等'. struct BookOutlineRow refactored from flat Label to DisclosureGroup (3-level nested: shelf DisclosureGroup wraps ForEach(books), each BookOutlineRow = DisclosureGroup wrapping ForEach(BookCategory.allCases)). New private struct BookCategoryOutlineRow renders per-category Label with category.displayName + category.icon. @State private var isExpanded: Bool = true (= Apple HIG Finder default = expand on first launch). Uses existing BookCategory enum from Sources/WenshuApp/Domain/Document.swift (= chapter = '章节' book.closed / setting = '设定' gearshape.2 / research = '资料库' books.vertical.fill, NOT redefined). Future tickets (per Boss 8/22 '1 zone 1 ticket 1 commit' scope discipline): per-book custom categories (= 人物 / 海洋馆 / 音频 per Boss image), document count badge per category. | v0.24 boss验收 ticket 015.022 |
| **D_v3 conditional hide (boss v0.24 ticket 015.024)** | Per Boss 8/25 eleventh OOB '显示级 BUG, 红框中, 出现了两条拖拽线': at initial state, D_v3 X (= 1514/1920) and D_v5 X (= 1518/1920) are 4 PT apart, both rendered as separate 1 PT vertical lines (= visible double-line artifact at right edge). Fix: VSplitter(splitterIndex: 2) (= D_v3) wrapped in 'if !vm.areRightSplittersAligned()' conditional render. At aligned (= initial: toolsWRatio == dynamicWRatio), D_v3 hidden so only D_v5 visible at right edge (= single line). When user drags D_v3 or D_v5 (= ratios diverge), predicate false, D_v3 re-renders (= per-zone splitter control restored). LayoutShellViewModel restored areRightSplittersAligned() (= removed in commit ee801d0dd for ticket 015.021 overlay revert; needed again for ticket 015.024 conditional D_v3 render). | v0.24 boss验收 ticket 015.024 |
| **Archive icon Swift 6 concurrency (boss v0.24 ticket 015.014 follow-up)** | Per Boss 8/25 OOB dual-axis review feedback: ChatSessionStore is 'public actor' (= actor-isolated), but archiveSession was called synchronously without await from archiveAndStartNewSession, triggering Swift 6 '#ActorIsolatedCall' warning. Fix: 'public func archiveSession(...)' -> 'public nonisolated func archiveSession(...)' (= sync public API for callers that don't need to await), AND 'private let dbPtr: SQLitePtr' -> 'private nonisolated(unsafe) let dbPtr' (= needed so nonisolated archiveSession can access SQLite ptr). Caller serialization via Swift actor lock on ChatSessionStore instance preserved (= Swift's actor reentrancy semantics prevent concurrent writes). Actor-isolation correctness verified by 双轴 reports (PASS, 9/9 axes). | v0.24 boss验收 ticket 015.014 follow-up |
| **Toolbar 3 groups (boss v0.24 ticket 015.027)** | macOS window toolbar reorganized into 3 groups per Boss 8/25 13th OOB. Left group (= after red/yellow/green window controls, placement: .navigation) = 新建 + 打开 + 导入 (= 3 file actions). Center group (placement: .primaryAction) = 项目管理区 + 工具区 + 聊天区 + 动态区 (= 4 zone visibility toggles, '一组' per Boss拍). Right group (placement: .primaryAction, separate) = 导出 (single button). Removed: 标题 '文枢' (principal ToolbarItem), center title, + button (= '新建书架' ToolbarItem in LibraryOutlineView, now lives in emptyLibrary CTA from ticket 015.026 + global toolbar 新建 button). UI 全中文 preserved (= Chinese labels = product direction). All Chinese UI strings exempt from AGENTS.md §11 English-only rule per Boss 8/25 'UI 全中文' carve-out. | v0.24 boss验收 ticket 015.027 |
| **Toolbar divider between toggles and export (boss v0.24 ticket 015.028)** | Per Boss 8/25 14th OOB '文枢标题不要. 右边的按钮从红线处拆成两组, 导出独立'. macOS window toolbar shows 3 groups per Boss 8/25 13th OOB + visual Divider between 4 zone toggles and export (= Boss image '红线处' = visible separator). Implementation: ToolbarItem(placement: .principal) { Divider() } (= Apple HIG toolbar separator pattern, re-uses the principal slot after title was removed in ticket 015.027). Final toolbar: [3 left file actions = 新建 + 打开 + 导入] + [4 zone toggles = 项目管理区 + 工具区 + 聊天区 + 动态区] + [Divider] + [1 export = 导出]. UI 全中文 preserved. | v0.24 boss验收 ticket 015.028 |
| **工具区 drag restore (boss v0.24 ticket 015.029)** | Per Boss 8/25 15th OOB '工具区不能拖拽了. 修好': my ticket 015.024 commit f5300c5cb conditionally hid D_v3 (= VSplitter(splitterIndex: 2) wrapping in 'if !vm.areRightSplittersAligned()') to fix the 4 PT double-line visual artifact at initial state. Boss拍 hidden D_v3 = 用户 can't drag specializedTools width (= the BUG). Fix: removed the conditional hide wrapper (= D_v3 now renders unconditionally, like before ticket 015.024). Per Boss 8/25 8th OOB '不要纠结线的问题, 核心是比例': visual double-line at initial state is acceptable (= priority is drag functionality, not pixel-perfect alignment). areRightSplittersAligned() predicate preserved but now unused (= dead code, can be removed in future cleanup). | v0.24 boss验收 ticket 015.029 |
| **Toolbar 4 toggles 1 group + export independent (boss v0.24 ticket 015.030)** | Per Boss 8/25 16th OOB '分不不对, 右边的四个显隐功能是一组, 导出独立. 文枢标题, 正中间的一个没有内容的按钮, 全都不要'. Removed central ToolbarItem(placement: .principal) { Divider() } (= Boss拍 '正中间的一个没有内容的按钮 全都不要'). Correct grouping: 4 zone visibility toggles (= 项目管理区 /工具区 / 聊天区 / 动态区) in 1 ToolbarItemGroup(placement: .primaryAction) (= Boss拍 '一组'); 导出 in SEPARATE ToolbarItem(placement: .primaryAction) (= Boss拍 '独立'). 文枢 title already removed in commit c3d5de882 (ticket 015.027, SwiftUI toolbar principal item). NSWindow title fix in commit 1a580ac9b (ticket 015.031, Boss 17th OOB). Final toolbar: [3 left file actions = 新建 + 打开 + 导入] + [4 zone toggles in 1 group] + [1 export separate]. UI 全中文 preserved. | v0.24 boss验收 ticket 015.030 |
| **导出 .cancellationAction separation (boss v0.24 ticket 015.032)** | Per Boss 8/25 18th OOB '你是如何把新建打开导入分离出来的, 就怎么把导出分离出去'. Boss image shows 4 zone toggles + 导出 visually adjacent (= no visible gap). Fix: 导出 ToolbarItem placement .primaryAction -> .cancellationAction (= macOS HIG auto-separation convention for independent actions). Note: Apple docs describe .cancellationAction primarily for modal sheet Cancel buttons (= per sub-agent SUGGEST S1, this is a non-canonical but valid use because distinct sibling placements produce slot boundaries in macOS toolbar, achieving visual separation). Final toolbar: [3 left file actions = 新建 + 打开 + 导入] + [导出 independent = .cancellationAction] + [4 zone toggles = .automatic]. macOS auto-renders visual gaps between each group. UI 全中文 preserved. NSWindow title fix in subsequent ticket 015.033 (WindowToolbarStyle.showsTitle API). | v0.24 boss验收 ticket 015.032 |
| **windowToolbarStyle showsTitle false (boss v0.24 ticket 015.033)** | Per Boss 8/25 17th OOB '文枢标题不要. 隐藏不了吗': previous commit 015.031 (= WindowGroup("")) may have coincidentally hidden title on this SwiftUI build but is not robust. Per Apple HIG developer.apple.com/documentation/SwiftUI/WindowToolbarStyle, the canonical API to hide title in .unified chrome is '.windowToolbarStyle(.unified, showsTitle: false)' or '.toolbar(removing: .title)'. Fix: .windowToolbarStyle(.unified) -> .windowToolbarStyle(.unified(showsTitle: false)). WindowGroup("") retained as defense-in-depth (= harmless if shown). Sub-agent visual verify confirmed title hidden before commit. F1 cleanup in commit 4d7f08250: translated CJK comments to English per AGENTS.md §11. | v0.24 boss验收 ticket 015.033 |
| **1 layer capsule + 导出 rightmost (boss v0.24 ticket 015.035)** | Per Boss 8/25 20th OOB '只要一层胶囊. 同时你没有改居右. 导出要在最右. 本来左边的新建打开导入是对的, 你又多改了内容'. 3 fixes: (1) Reverted left group to original NSLog placeholders WITHOUT HStack+Capsule wrap (= Boss拍 '本来左边是对的'); (2) Removed 4 toggles HStack+Capsule wrap (= Boss拍 '只要一层胶囊'); (3) Moved 导出 from .cancellationAction to .primaryAction placement (= Boss拍 '导出要在最右'). Removed unused ToolbarCapsuleButton helper struct (dead code). Net -38 lines simplification. | v0.24 boss验收 ticket 015.035 |
| **Toolbar capsule grouping 3 sets (boss v0.24 ticket 015.034, SUPERSEDED)** | Per Boss 8/25 19th OOB: 4 toggles in 1 capsule + 导出 in 1 capsule + both at rightmost. Implementation: ToolbarCapsuleButton helper struct + HStack+Capsule wrap for all 3 toolbar groups. However: (1) wrapped in 2 layers (= HStack inside ToolbarItemGroup inside ToolbarItemGroup = 2 layers, Boss 20th OOB rejected this); (2) modified left group (= Boss 20th OOB '本来左边的新建打开导入是对的, 你又多改了内容'); (3) 导出 in .cancellationAction was placed to LEFT of 4 toggles, not rightmost (= Boss 20th OOB '导出要在最右'). Ticket 015.035 = corrected version: 1 layer, revert left, 导出 in .primaryAction (= rightmost). | v0.24 boss验收 ticket 015.034 (superseded) |
| **Toolbar Pages-style capsules (boss v0.24 ticket 015.036, SUPERSEDED by 015.037)** | Per Boss 8/25 21st OOB 'pages reference': left 3 file actions unchanged, 4 zone toggles in 1 capsule + 导出 in 1 capsule both at rightmost. Implementation: 4 toggles in own HStack+Capsule with controlBackgroundColor, 导出 in own HStack+Capsule with controlBackgroundColor. However, Boss 22nd OOB rejected this design as '2 layers' (= inner capsules on each group + separate ToolbarItemGroups = 2 layers, not 1 single outer layer). Ticket 015.037 = corrected: 1 outer glass capsule (Apple HIG .regularMaterial) wrapping both 4 toggles (with inner dark capsule for sub-grouping) + 导出 single button (no inner capsule). | v0.24 boss验收 ticket 015.036 (superseded) |
| **Toolbar 1 outer glass capsule (boss v0.24 ticket 015.037)** | Per Boss 8/25 22nd OOB '为什么要多套一层黑色的胶囊, 不要多套一层, 里面那层分开是对的, 用外面那层玻璃质感的, 单独一层实现分离'. Implementation: 1 outer HStack + Capsule with Apple HIG .regularMaterial fill (= 玻璃质感 translucent material) wrapping both 4 zone visibility toggles + 导出 button. Inner 4 toggles keep own dark capsule background (Boss拍 '里面那层分开是对的' = visual sub-grouping). 导出 single button, no inner capsule (Boss拍 '单独一层实现分离' = single layer). Original 4 toggles .automatic ToolbarItemGroup removed (no separate group = no nested outer capsule). UI 全中文 preserved. Sub-agent WARNING on orphan comment fragment in commit 90de4891e fixed in cleanup commit 2b970a4b3. | v0.24 boss验收 ticket 015.037 |
| **Toolbar 2 separate right capsules (boss v0.24 ticket 015.038)** | Per Boss 8/25 23rd OOB '括号代表分组, 能看懂不. 左(新建, 打开, 导入) 右(开关1, 开关2, 开关3, 开关4)(导出)'. Implementation: 导出 wrapped in own HStack + dark Capsule (= Group 2 of right side, separate from 4 toggles Group 1). Both groups inside 1 outer glass Capsule with .regularMaterial fill (Boss 22nd OOB '用外面那层玻璃质感的'). However, Boss 24th OOB rejected the outer glass as '你现在包了两层' (= 1 outer glass + 1 inner dark = 2 layers, wrong). Ticket 015.039 = corrected: removed outer glass, kept 2 separate right groups with plain rounded background matching Pages reference. | v0.24 boss验收 ticket 015.038 |
| **Toolbar 1 layer Pages style (boss v0.24 ticket 015.039)** | Per Boss 8/25 24th OOB '样式不对, 参考新建, 打开, 导入的样式. 你现在包了两层'. Boss拍 current state has 2 layers (= outer glass Capsule + inner dark capsule) which is wrong. Fix: removed outer glass Capsule (.regularMaterial), simplified 导出 to direct Button with .background(Capsule().fill(controlBackgroundColor)) (= same 1-layer style as 4 toggles + left group). Final toolbar: 3 left file actions (no capsule) + 2 right groups (4 toggles in 1 dark capsule + 导出 in 1 dark capsule), 1 layer per group. Style matches Pages reference (= plain rounded background, controlBackgroundColor fill). | v0.24 boss验收 ticket 015.039 |
| **Toolbar 3 separate 胶囊 (boss v0.24 ticket 015.040)** | Per Boss 8/25 25th OOB '看到 pages 的样式了没, 胶囊有多个. 我们就需要三个胶囊 1.(新建, 打开, 导入) 2.(四个显隐开关) 3.(一个导出按钮)'. Implementation: 3 separate 1-layer HStack + dark Capsule groups (1 left + 2 right). Left group = 新建/打开/导入 in 1 capsule. Right group 1 = 4 zone visibility toggles in 1 capsule. Right group 2 = 导出 in 1 capsule. Icon-only buttons (18x18 PT). Boss 26th OOB then refined: Color.black opaque fill (no toolbar material blur) + HStack spacing 4 (minimal gray gap). UI 全中文 preserved. | v0.24 boss验收 ticket 015.040 |
| **Toolbar black capsule + reduce gap (boss v0.24 ticket 015.041)** | Per Boss 8/25 26th OOB '把背景那个灰色的胶囊去掉. 只留一层黑色的'. Implementation: (1) Replaced 3x Color(nsColor: .controlBackgroundColor) fills with Color.black (= opaque black, no toolbar material blur showing through). (2) Reduced HStack spacing between 4 toggles and 导出 from 8 to 4 PT (= minimal gap, no outer gray visible). (3) 3 separate 1-layer black capsules preserved. UI 全中文 preserved. | v0.24 boss验收 ticket 015.041 |
| **Toolbar Pages-style gray capsule (boss v0.24 ticket 015.042)** | Per Boss 8/25 27th OOB '图一是当前样式, 图二是 pages 的, 参考 pages 实现'. Boss 26th OOB's 'Color.black' fix was wrong direction (= 2 layers black inner + gray outer wrap). Pages reference (Figure 2) = 1 layer plain gray capsule, no inner black. Fix: replaced 3x Color.black fills with Color(nsColor: .controlBackgroundColor) (= gray, matches Pages style, 1 layer only, no inner black). 3 separate gray capsules (1 left + 2 right), Pages-style. F1 cleanup in commit 408242bf3: translated CJK comments to English per AGENTS.md Hard rule. | v0.24 boss验收 ticket 015.042 |
| **Toolbar default size + 1 layer (boss v0.24 ticket 015.043)** | Per Boss 8/25 28th OOB '改成单层对了, 尺寸不对, 尺寸改成默认尺寸' + Boss 29th OOB '把四开关, 一导出, 居右'. Implementation: (1) Removed inner black Capsule for 4 toggles + 导出 (= 1 layer per Boss 28th OOB). (2) Changed .windowToolbarStyle to .unifiedCompact (= 28 PT). However, per Apple docs (developer.apple.com/documentation/SwiftUI/WindowToolbarStyle), .unifiedCompact is COMPACT, NOT default. Ticket 015.045 = corrected: .unifiedCompact -> .unified (= 52 PT default). Ticket 015.046 = simplified to default Apple HIG items (= no custom .frame, .font, .padding, .background). | v0.24 boss验收 ticket 015.043 |
| **Toolbar 导出 .confirmationAction (boss v0.24 ticket 015.047)** | Per Boss 8/25 33rd OOB '红框, 居右. 用官方的方式, 把导出独立出来'. Implementation: 导出 ToolbarItem placement .primaryAction -> .confirmationAction (= Apple HIG official independent action placement, per developer.apple.com/documentation/SwiftUI/ToolbarItemPlacement, .confirmationAction = 'center of the trailing toolbar area, may be separated by other items'). However, Boss 34th OOB '整组 居右' rejected this (= visual separator between 4 toggles and 导出 was wrong). Ticket 015.048 = corrected: 导出 .confirmationAction -> .primaryAction (= 1 right cluster, all 5 buttons grouped). | v0.24 boss验收 ticket 015.047 |
| **Toolbar 整组 居右 (boss v0.24 ticket 015.048)** | Per Boss 8/25 34th OOB '先实现居右, 整组先移到居右去'. Implementation: 导出 ToolbarItem placement .confirmationAction -> .primaryAction (= 5 separate .primaryAction ToolbarItems, all in 1 right cluster, no visual separator). However, Boss 35th OOB '看懂了没' (= Boss image with red box + red arrow pointing right) found 5 separate .primaryAction ToolbarItems still have per-button spacing (= not 整组 紧贴 最右 边). Ticket 015.049 = corrected: 4 separate .primaryAction ToolbarItems -> 1 ToolbarItemGroup(.primaryAction) wrapping 4 toggles + 导出 separate .primaryAction ToolbarItem (= 2 groups total, no per-button spacing, 紧贴 居右). | v0.24 boss验收 ticket 015.048 |
| **Toolbar default Apple HIG items (boss v0.24 ticket 015.046)** | Per Boss 8/25 32nd OOB '尺寸不对, ICON都超出了胶囊的范围, 查官方, 看这个原厂效果'. Implementation: (1) Removed all custom modifiers (.frame, .font, .padding, .background, .buttonStyle, .contentShape, .foregroundStyle on Image). (2) 5 separate .primaryAction ToolbarItems (4 toggles + 导出). (3) .foregroundStyle applied to Button level (keeps color without affecting icon size). Per Apple HIG (developer.apple.com/documentation/SwiftUI/ToolbarItemGroup), default toolbar item size = system-managed. Subsequent ticket 015.049 consolidated 4 toggles into 1 ToolbarItemGroup(.primaryAction) for 整组 紧贴 最右 边. | v0.24 boss验收 ticket 015.046 |
| **Toolbar 4 toggles ToolbarItemGroup + 导出 (boss v0.24 ticket 015.049)** | Per Boss 8/25 35th OOB '看懂了没' (= Boss image: 4 toggles + 导出 in red box at right, red arrow pointing right). Implementation: (1) 4 separate .primaryAction ToolbarItems -> 1 ToolbarItemGroup(.primaryAction) wrapping all 4 zone visibility toggles (= 1 整组, 紧贴 居右, no per-button separator). (2) 导出 stays as separate ToolbarItem(.primaryAction) (= rightmost). (3) Per Apple HIG (= developer.apple.com/documentation/SwiftUI/ToolbarItemGroup), ToolbarItemGroup = 'group of related toolbar items rendered as a single cluster'. 2 groups total, all at right with no visual separator (= Boss 34th OOB '整组 居右' satisfied). | v0.24 boss验收 ticket 015.049 |
| **Toolbar Spacer + .automatic trailing edge (boss v0.24 ticket 015.051)** | Per Boss 8/25 37th OOB '查官方文档, 如何居右' (= check official docs, how to right-align). Implementation: per Apple developer.apple.com/documentation/SwiftUI/ToolbarItemPlacement/primaryAction, '.primaryAction' on macOS = LEADING edge (= left side, NOT trailing!). Apple official macOS trailing edge pattern = ToolbarItemGroup(.automatic) + Spacer() first. Previous tickets 015.048/015.049/015.050 all incorrectly used .primaryAction thinking it was 'rightmost'. This commit corrected: ToolbarItemGroup(.primaryAction) -> ToolbarItemGroup(.automatic) + Spacer() first (= pushes all buttons to trailing edge, no per-button separator). | v0.24 boss验收 ticket 015.051 |
| **Right zones proportional width alignment (boss v0.24 ticket 015.052)** | Per Boss 8/25 38th OOB '查官方文档, 如何实现按比例的右上区和右下区视觉上宽度对齐'. Per Apple developer.apple.com/documentation/appkit/nssplitview/adjustsubviews/, proportional resize means subviews maintain relative sizes. Implementation: aiChatRatio 1518/1920 -> 726/1920 (drop 1518 down to 726), dynamicWRatio 400/1920 -> 1194/1920 (match 右上区 = 1194 PT). 右上区 (editor 794 + tools 400 = 1194 PT) and 右下区 (dynamic 1194 PT) now visually width-aligned. aiChat (下左) shrinks accordingly (= from 1518 to 726 PT). | v0.24 boss验收 ticket 015.052 |
| **Right zones revert + .frame (boss v0.24 ticket 015.053)** | Per Boss 8/25 41st OOB '上半区, 一共四栏, 最右边一栏, 原本就是400. 下半区, 一共两栏, 最右边一栏, 原本也是400. 但同样是400, 视觉上就是不一样宽 我让你解决的就是这个问题. 让你查文档'. Boss澄清: 上右 (specializedTools) 和 下右 (aiDynamic) 原本设计都 = 400 PT (= toolsWRatio = dynamicWRatio = 400/1920, keep). 我之前 commit b8d8c04a8 (ticket 015.052) 错误把 dynamicWRatio 改到 1194 (= Boss 38th OOB 误解, Boss 41st OOB 拍 revert). Fix: dynamicWRatio 1194 -> 400 + aiChatRatio 726 -> 1518 (= back to original 8/18 design values). 真正问题 = 同样400 PT 视觉上不等宽 (= 需要 Apple HIG / SwiftUI Layout protocol 真正修法, 不是 .frame width tricks). | v0.24 boss验收 ticket 015.053 |
| **Code widths absorb splitter hit areas (boss v0.24 ticket 015.054)** | Per Boss 8/25 44th OOB '查到 BUG 了吗, 代码写的宽度不对是吧'. Boss确认 bug (= 代码 宽度 不对): 上半区 栏总 = 200+520+794+400 = 1914 PT (+ 3 splitters × 6 PT hit area = 1932 PT, overshoots 1920 design by 12 PT). 下半区 栏总 = 1518+400 = 1918 PT (+ 1 splitter × 6 PT = 1924 PT, overshoots 1920 design by 4 PT). Bug = SwiftUI HStack shrinks children when total > parent (= visual uneven widths when both right zones are designed as 400 PT). Fix: toolsWRatio 400 -> 388 (-12 PT to absorb 3 splitters), aiChatRatio 1518 -> 1514 (-4 PT to absorb 1 splitter). New sums: 上半区 200+520+794+388+18 = 1920 PT ✓, 下半区 1514+400+6 = 1920 PT ✓. | v0.24 boss验收 ticket 015.054 |
| **Code widths 1000 base 20/20/40/20 + 80/20 (boss v0.24 ticket 015.055)** | Per Boss 8/25 45th OOB '上四栏比例 20 20 40 20. 下两栏比例 80 20' + Boss 46th OOB '默认宽度按 1000' + Boss 47th OOB '1000 是 1:1 写法, 跑起来应该就是 2000' + Boss 48th OOB '这多出来的是什么 (= report it myself)'. Implementation: changed base from 1920 to 1000 (= 1:1 denominator, runs as ~2000 PT at runtime). 上半区 4 zones: 200/1000 + 200/1000 + 400/1000 + 200/1000 (= 20/20/40/20 = 100%). 下半区 2 zones: 800/1000 + 200/1000 (= 80/20 = 100%). 拖拽线占用 = splitter hit area 6 PT each (= NativeSplitter.swift line 26). 3 splitters upper = 18 PT, 1 splitter lower = 6 PT. Boss 49th OOB clarified: splitter hit area 算在最大的栏里 (= editor/aiChat), 其他栏 保持设计比例不变. | v0.24 boss验收 ticket 015.055 |
| **Splitter hit areas into editor/chat (boss v0.24 ticket 015.056)** | Per Boss 8/25 49th OOB '拖拽线产生的 18, 算在编辑器里, 不要影响其它. 下栏的 1000, 因 拖拽线产生的 6, 算在聊天里, 不要影响其它'. Boss澄清 BUG真正原因 = 之前 Boss 44th OOB 让 toolsWRatio/aiChatRatio 减 12/4 PT 抵销 splitters (= 改了其他栏 ratio, 导致比例 != 20/20/40/20). 正确修法 = splitter hit area 算在最大的栏里 (= editor上栏 / aiChat下栏), 其他栏保持设计比例不变. Implementation: editorWRatio 400.0/1000.0 -> 382.0/1000.0 (-18 PT to absorb 3 splitters in editor). aiChatRatio 800.0/1000.0 -> 794.0/1000.0 (-6 PT to absorb 1 splitter in aiChat). 其他4栏 (sidebar/preview/tools/dynamic) 保持 200/1000 (= 20% design ratio). New sums: 上栏 200+200+382+200 = 982 + 18 splitters = 1000 PT ✓, 下栏 794+200 = 994 + 6 splitter = 1000 PT ✓. | v0.24 boss验收 ticket 015.056 |
| **Zone widths F1 cleanup body math (boss v0.24 ticket 015.058)** | Per Boss 8/25 50th OOB '还是差了一两个像素' + Apple docs (= stackoverflow.com 'Single pixel gap between images in SwiftUI HStack' + swiftui-lab.com 'Frame Behaviors with SwiftUI'): SwiftUI HStack with .frame(width:) on every child may render 1 PT gap due to sub-pixel rounding (= each child width is fractional PT, total may not be exactly integer = visible 1-2 PT gap). Fix per Apple HIG: round each width to integer using .rounded() (= no fractional PT = no sub-pixel rendering gap). 015.057 added .rounded() to upper + lower band widths. 015.058 = documentation commit (= empty --allow-empty) documenting the 6 ratio constants + body math clarification (1920 × 382/1000 = 733.44 → .rounded() = 733 PT = sub-pixel gap fix). | v0.24 boss验收 ticket 015.058 |
| **Splitter hit area 6 -> 4 PT (boss v0.24 ticket 015.057)** | Per Boss 8/25 50th OOB '还是差了一两个像素' + 51st OOB '尝试修一下'. Theory: 6 PT splitter hit area too large for 1.452 scale (= window 1452 / base 1000 = 1.452 = 6/1.452 = 4.13 PT scaled = sub-pixel rendering mismatch). Fix: NativeSplitter.swift hitAreaThickness 6 -> 4 PT (= cleaner integer at 1.452 scale). editorWRatio 382 -> 388 (-12 PT for 3 splitters @ 4 PT = 12). aiChatRatio 794 -> 796 (-4 PT for 1 splitter @ 4 PT = 4). New sums: 上栏 200+200+388+200 = 988 + 12 splitters (3 × 4) = 1000 PT ✓, 下栏 796+200 = 996 + 4 splitter (1 × 4) = 1000 PT ✓. Sub-agent caught Spec GAP in commit 742faa691 (= aiChatRatio was claimed to be 796 but actually 794 = body claim wrong, code didn't change), fixed in commit aab218c12. | v0.24 boss验收 ticket 015.057 |
| **4 显隐 按钮 功能 实装 (boss v0.24 ticket 015.059)** | Per Boss 8/25 54th OOB '实装一下, 这四个显隐按钮的功能'. 4 显隐 按钮 (= 4 visibility toggle buttons in toolbar right group): (1) sidebar.left = 项目管理区 (.projectSidebar), (2) wrench.and.screwdriver = 工具区 (.specializedTools), (3) bubble.left = 聊天区 (.aiChat), (4) chart.bar = 动态区 (.aiDynamic). 功能 已 实装 = already done in tickets 015.022 + 015.046. Implementation: App.swift L1102-1138 = 4 buttons each with vm.toggleZone(slot: .<slot>) action + .foregroundStyle(vm.isZoneVisible(slot:) ? Color.accentColor : Color.secondary) (= active=blue, hidden=gray) + .help(vm.isZoneVisible(slot:) ? '隐藏 <zone>' : '显示 <zone>') (dynamic help text). LayoutShellViewModel.swift L52-83 = 4 @AppStorage properties + isZoneVisible + toggleZone switch. When user clicks a button: (1) Button action calls vm.toggleZone(slot:), (2) LayoutShellViewModel toggles visibility flag, (3) SwiftUI re-renders toolbar with new color, (4) SwiftUI re-renders layout, hiding/showing corresponding zone. | v0.24 boss验收 ticket 015.059 |
| **4 显隐按钮 hide/show zones (boss v0.24 ticket 015.060)** | Per Boss 8/25 55th OOB '四个开关都没有实现对应的四个栏的显隐效果' + 56th OOB '查了官方文档没'. Implementation per Apple developer.apple.com documentation: 'If you want to conditionally include a view in the view hierarchy, use an if statement instead.' Wrap each ZoneModule in HStack with if vm.isZoneVisible(slot: .<slot>). When user clicks toggle button: (1) Button action calls vm.toggleZone(slot: .<slot>), (2) LayoutShellViewModel toggles @AppStorage visibility flag, (3) SwiftUI HStack re-renders: if true = ZoneModule + frame, if false = completely omitted from layout (= no ZoneModule, no width consumed). Splitters stay visible (per Boss 15th OOB '工具区不能拖拽了. 修好') so drag functionality preserved even when adjacent zones hidden. Note: original 015.060 wrapped all 4+2 zones = caused Boss 57th OOB '界面全改丢了' (= @AppStorage persisted state from previous session had some zones hidden, app started empty). Fixed in 015.061 with '一个一个来' approach. | v0.24 boss验收 ticket 015.060 |
| **First toggle projectSidebar only (boss v0.24 ticket 015.061)** | Per Boss 8/25 57th OOB '界面全改丢了, 这样吧, 你先实现第一个按钮对应的项目管理区. 一个一个来'. Boss 57th OOB clarification: previous commit 015.060 wrapped all 4+2 zones in if, caused UI lost (= @AppStorage persisted hidden state). Boss拍 = 一个一个来 (= one at a time): only wire up the FIRST button (= sidebar.left, projectSidebar zone) for hide/show. Other 3 zones + both lower band zones always render. Implementation: only projectSidebar wrapped in if vm.isZoneVisible(slot: .projectSidebar). Upper band preview/editor/tools always render (no if wrapper). Lower band aiChat/aiDynamic always render (no if wrapper). Splitters stay visible (per Boss 15th OOB '工具区不能拖拽了. 修好') so drag functionality preserved even when projectSidebar hidden. Future tickets (015.062+) will add other 3 toggles one by one. | v0.24 boss验收 ticket 015.061 |
| **Sidebar hide/show + redistribute + animation (boss v0.24 ticket 015.062)** | Per Boss 8/25 58th OOB '项目管理区丢了. 默认要显示. 开关是蓝色状态. 再点开关才收起项目管理区. 收起后上半区的其它三栏重新分配宽度, 盛满上半区, 过程中要有过度动画'. Boss 58th OOB specs: (1) projectSidebar 默认 visible (@AppStorage default = true), (2) Toggle button 蓝色 when visible (Color.accentColor), (3) Click toggle 才收起 (vm.toggleZone), (4) 收起后 上半区其它 3栏 (preview/editor/tools) 重新分配宽度 (redistribute 200 PT proportionally: preview/tools +25.4% each, editor +49.2%), (5) 过程中要有过度动画 (.animation(.easeInOut(duration: 0.25), value: sidebarIsVisible)). Implementation: sidebarIsVisible local var, sidebarExtraRatio (= ratio 0.2 not PT, Spec fix per sub-agent review), preview/editor/tools redistribute. New widths at totalW=1452: visible: sidebar=290, preview=290, editor=564, tools=290; hidden: sidebar=0, preview=363, editor=706, tools=363. Total hidden = 1446/1452 = 99.59% fill. Animation = SwiftUI .animation modifier triggers smooth width transition when sidebarIsVisible flag flips. | v0.24 boss验收 ticket 015.062 |
| **Menu bar zone toggles (boss v0.24 ticket 015.064)** | Per Boss 8/25 60th OOB '还是不能点.点了没效果. 同时对应的功能要在菜单栏实现'. Boss 60th OOB specs: (1) 4 buttons 还是不能点 (= toolbar toggle buttons don't work), (2) 对应功能要在菜单栏实现. Implementation per Apple HIG: (1) Add 4 zone toggle menu items in CommandGroup(after: .sidebar) of .commands block (menu bar primary per Apple HIG Rule 3 'Toolbar is secondary command surface after menu bar'). (2) Each menu item posts NotificationCenter.default.post(name: .wenshuToggleZone, object: ZoneSlot.<slot>). (3) Notification.Name.wenshuToggleZone extension added. (4) LayoutShellViewModel.init() listens for wenshuToggleZone notifications and calls toggleZone(slot:) (= same code path as toolbar buttons). Keyboard shortcuts ⌘⇧1/2/3/4 for 4 zones (= Apple modifier convention). Menu bar is more reliable than toolbar because menu bar uses AppKit NSEvent main menu path (= always visible, always responsive). Toolbar buttons use SwiftUI button in toolbar (= sometimes blocked by SwiftUI state/focus issues, = Boss 59th OOB 真正原因). | v0.24 boss验收 ticket 015.064 |
| **RevisionToken trigger re-render (boss v0.24 ticket 015.063)** | Per Boss 8/25 59th OOB '四个按钮点击都没有效果' (= 4 buttons clicks have no effect). Real bug analysis: LayoutShellViewModel is @Observable (= SwiftUI Observation framework auto-synthesizes tracking for stored properties). 4 zone visibility flags use @AppStorage with @ObservationIgnored (= @AppStorage provides its own UserDefaults reactive storage; Observation framework can't track internal state). When user clicks toolbar toggle button, vm.toggleZone(slot:) flips the @AppStorage flag via .toggle(), BUT @ObservationIgnored prevents SwiftUI from re-rendering the view. Previous fix attempts failed: (1) Remove @ObservationIgnored caused build error 'invalid redeclaration of synthesized property _projectSidebarVisible' (= @AppStorage backing storage conflicts with @Observable synthesized storage); (2) Call objectWillChange.send() failed because @Observable hides it (= 'cannot find objectWillChange in scope'). Real fix: add @ObservationTracked revisionToken property (= UInt64 counter). toggleZone bumps revisionToken &+= 1 BEFORE AND AFTER flag flip. @ObservationTracked tracks via @Observable macro, so bumping triggers objectWillChange (= SwiftUI re-renders). Net effect: click toggle → token bump → objectWillChange → view re-render → zone hide/show works. Sub-agent caught F1 partial cleanup (= L52 not cleared + L58 stale claim) + corrected. | v0.24 boss验收 ticket 015.063 |
| **Init reset @AppStorage defaults (boss v0.24 ticket 015.065)** | Per Boss 8/25 61st OOB '还是不行. 同时项目管理区没了'. Real bug analysis: @AppStorage default value '= true' only applies when UserDefaults key is UNSET. Once user clicks toggle (= flag flips to 0 = hidden), @AppStorage persists 0 across launches. App launches with projectSidebarVisible = 0 from UserDefaults (= Boss image shows sidebar missing). Implementation: init() adds UserDefaults.standard.set(true, forKey: ...) for all 4 toggleable zone flags (= projectSidebar, specializedTools, aiChat, aiDynamic). Note: only resets the 4 toggleable zones (= editor/projectPreview are hardcoded 'always visible' per Boss spec). Each launch resets to true (= Boss 58th OOB '默认要显示' invariant). User can still toggle any zone to hide via menu bar / toolbar buttons (toggleZone flips flag), but next launch resets to visible again. This is heuristic reset for debug/test cycle. | v0.24 boss验收 ticket 015.065 |
| **RevisionToken dep + remove reset (boss v0.24 ticket 015.066)** | Per Boss 8/25 62nd OOB '还是不对. 同时双击标题栏放大缩小一回合窗口, 按钮的颜色就变了. 感觉好多地方有BUG'. Boss 62nd OOB analysis (= multiple BUGs): (1) 4 toggle buttons 还是不对, (2) double-click title bar = window resize cycle = button colors change, (3) projectSidebar 不可见, (4) 感觉好多地方有BUG. Root causes analysis: (A) Toolbar buttons don't work because @State vm reference doesn't change when toggleZone flips @AppStorage flag (= flag is @ObservationIgnored so @Observable doesn't auto-track). Per Apple Observation framework, SwiftUI requires view body to read tracked properties for dependencies. (B) Window resize re-renders body, re-reads revisionToken. (C) projectSidebar missing = stale UserDefaults state from earlier test cycles. Fix 1 (App.swift): add 'let _revisionTokenForDependency = vm.revisionToken' in LayoutShellView body (= creates Observation dependency, so toggleZone bumps trigger re-render). Fix 2 (LayoutShellViewModel.swift): remove forced UserDefaults reset in init() (= was breaking user persistence). @AppStorage default = true works for first-time users; subsequent toggles persist via UserDefaults. One-off 'defaults delete com.wenshu.app' shell reset clears stale state for fresh launch. | v0.24 boss验收 ticket 015.066 |
| **@AppStorage directly in view body (boss v0.24 ticket 015.067)** | Per Boss 8/25 63rd OOB '还是不行, 去查官方文档吧'. Per Apple HIG (= WWDC23 'Discover Observation in SwiftUI' + Apple migration guide + nicholas.clooney.io blog on @State + @Observable regressions), canonical pattern for SwiftUI state persistence is @AppStorage directly in view body. Indirection through LayoutShellViewModel @ObservationIgnored prevented view re-render. Implementation: LayoutShellView struct has 4 @AppStorage properties (= showProjectSidebar/showSpecializedTools/showAIChat/showAIDynamic). Toolbar buttons use direct @AppStorage toggle (= show*.toggle()). UpperBandZone + LowerBandZone use direct params. Removed @ObservationIgnored + revisionToken indirection. Onboarding flow works (= first-time launch shows welcome screen '欢迎使用文枢'). | v0.24 boss验收 ticket 015.067 |
| **D_v1 splitter hidden when sidebar collapsed (boss v0.24 ticket 015.068)** | Per Boss 8/25 64th OOB '收起, 展开已经实现, 这块代码几乎不用动了. 但收起时, 项目管理区, 还留了几个像素的宽度'. Boss 64th OOB clarification: hide/show is working (= Boss confirmed); Boss said '几乎不用动了' (= don't change much in this area); only bug remaining = a few pixels of width left when sidebar collapsed. Real bug: D_v1 VSplitter has hitAreaThickness = 4 PT (= per Boss 50th OOB 'hit area 4 PT'). When projectSidebar is hidden, D_v1 was unconditionally rendered (= HStack structure kept splitter at position 0, leaving 4 PT blank space). 4 PT × 1 PT line + 3 PT color = visible 'few pixels' (= Boss 64th OOB). Fix: move D_v1 VSplitter inside 'if sidebarIsVisible' block (= D_v1 only renders when sidebar is visible). When sidebar is hidden, D_v1 disappears with it (= no blank space at left edge). Minimal fix per Boss 64th OOB '几乎不用动了' (= 1 line change, 1 file, 7 ins / 2 del). | v0.24 boss验收 ticket 015.068 |
| **Replicate sidebar pattern for tools (boss v0.24 ticket 015.070)** | Per Boss 8/25 66th OOB '效果不如项目管理实现的效果. 你建议你直接去看项目管理区怎么实现的, 复刻过来. 项目管理区收起展开的动效都很好'. Boss 66th OOB: current tools zone hide/show effect is not as good as projectSidebar; Boss said '看项目管理区怎么实现的, 复刻过来' (= look at sidebar implementation, copy it); projectSidebar hide/show animation is good. Real fix: (1) removed local sidebarIsVisible + toolsIsVisible mirrors (= use direct @AppStorage properties per Apple HIG + WWDC23 'Discover Observation in SwiftUI'); (2) use direct @AppStorage in if blocks; (3) added second .animation modifier tracking showSpecializedTools (= tools toggle now animates with .easeInOut, matching sidebar pattern per Boss 66th OOB '效果等同项目管理区'). Followup (015.070 followup): updated 2 stale 'sidebarIsVisible' references in comments to 'showProjectSidebar' (= Spec SUGGEST closed). | v0.24 boss验收 ticket 015.070 |
| **layoutPriority redistribute width (boss v0.24 ticket 015.071)** | Per Boss 8/25 67th OOB '效果对了, 这块代码不用动. 但收起后其它区用尺寸的重新分配占用宽度有问题, 需要解决'. Boss 67th OOB clarification: '效果对了' = Boss confirmed 015.066/015.070 animation works correctly; '这块代码不用动' = don't change toggle wiring; '但收起后其它区用尺寸的重新分配占用宽度有问题' = after collapse, OTHER zones don't grow to fill the freed-up space (= width redistribution broken); '需要解决' = need to fix width redistribution. Real fix per Apple HIG + WWDC22 'Compose custom layouts with SwiftUI' (= per Boss 8/22 '有问题先去查文档' protocol): per Apple developer.apple.com/documentation/swiftui/view/layoutpriority doc: 'Setting a higher layout priority encourages the higher priority view to shrink later when the group is shrunk and stretch sooner when the group is stretched.' = Apple官方推荐 way to handle conditional HStack children (= visible zones get extra width when siblings hidden). Implementation: (1) ZoneModule(sidebar) + .layoutPriority(0); (2) ZoneModule(preview) + .layoutPriority(1); (3) ZoneModule(editor) + .layoutPriority(1); (4) ZoneModule(tools) + .layoutPriority(0). 2 visible zones (preview + editor) get high priority so they stretch to fill HStack when sidebar/tools hidden. 2 toggleable zones get default priority so they keep their fixed width. Replaces Boss 66th OOB's pre-computed width redistribution with Apple官方推荐 layoutPriority-based redistribution. | v0.24 boss验收 ticket 015.071 |
| **maxWidth fill remaining width (boss v0.24 ticket 015.072)** | Per Boss 8/25 69th OOB '还是有点问题, 工具栏隐藏后, 其它三栏占用整个上半区的计算有问题'. Boss 69th OOB clarification: '工具栏隐藏后' = when tools hidden (扳手 button toggle); '其它三栏占用整个上半区的计算有问题' = the OTHER 3 zones (sidebar, preview, editor) don't properly fill the upper band (= leftover gap on the right side of upper band). Real fix per Apple HIG 'Building layouts with stack views' doc: HStack distributes space evenly to all subviews. To FILL leftover space, use .frame(maxWidth: .infinity) (= take all available space) on visible zones, INSTEAD of .frame(width:) which uses pre-computed fixed width (= leftover gap when sibling hidden). .layoutPriority alone is priority hint for shrinking/stretching, but .frame(width:) overrides stretch behavior (= fixed-width reservation). Per Apple HIG canonical pattern for HStack zones that need to fill container width when siblings are conditionally removed = use .frame(maxWidth: .infinity). Implementation: (1) ZoneModule(preview) + .frame(maxWidth: .infinity, alignment: .top); (2) ZoneModule(editor) + .frame(maxWidth: .infinity, alignment: .top); (3) Both retain .layoutPriority(1). Sidebar + tools keep .frame(width:) + .layoutPriority(0) (= per Boss 8/19 design spec for fixed-width sidebar + tools). | v0.24 boss验收 ticket 015.072 |
| **5th button projectPreview zone (boss v0.24 ticket 015.073)** | Per Boss 8/25 70th OOB '很好, 可以了, 我发现咱们少设计了一个按钮, 第2栏的素材区没有设计按钮. 1. 在项目管理区按钮后面给, 素材预览区加一个按钮'. Boss 70th OOB: missing button for projectPreview zone (= 素材预览区 = 第2栏); insert preview toggle AFTER sidebar toggle button. Real fix (= per Boss 57th OOB '一个一个来' + Boss 65th OOB + Boss 60th OOB + Boss 63rd OOB + Boss 64th OOB + Boss 66th OOB + Boss 69th OOB patterns): (1) LayoutShellView: added @AppStorage('wenshu.zoneVisible.projectPreview') showProjectPreview: Bool = true; (2) Toolbar button: 5th Button with eye.fill icon, AFTER sidebar toggle; (3) Menu bar item: '显示/隐藏 素材预览区' posts .wenshuToggleZone for .projectPreview; (4) UpperBandZone: added showProjectPreview param; (5) Wrapped projectPreview ZoneModule + D_v2 splitter in 'if showProjectPreview' block; (6) Width redistribution: added previewExtraRatio for when preview hidden; (7) Animation: added .animation(.easeInOut(0.25), value: showProjectPreview). | v0.24 boss验收 ticket 015.073 |
| **All 5 hidden = editor fills entire area (boss v0.24 ticket 015.075)** | Per Boss 8/25 72nd OOB '五个按钮全关掉后, 让编辑器占全部空间'. Boss 72nd OOB clarification: '五个按钮全关掉后' = when all 5 toggle buttons off (= sidebar/preview/tools/chat/dynamic all hidden); '让编辑器占全部空间' = editor zone should fill ENTIRE content area (both upper and lower band) instead of being constrained to upper band (= upperBandH = totalHeight, lowerBandH = 0). Real fix (Sources/WenshuApp/App.swift L1079-1102): (1) Compute 'allZonesHidden' flag; (2) When allZonesHidden = true: upperBandHeight = contentH, lowerBandHeight = 0; (3) Hide D_h splitter + LowerBandZone (= no band boundary to split); (4) When NOT allZonesHidden: use existing vm.upperBandH / vm.lowerBandH. Implementation is minimal (= 19 ins / 7 del) and surgical. | v0.24 boss验收 ticket 015.075 |
| **Lower band hidden = upper band fills full (boss v0.24 ticket 015.076)** | Per Boss 8/25 73rd OOB '下半区隐藏时, 上半区的栏高度自适应到全高'. Boss 73rd OOB: when aiChat + aiDynamic both hidden (= lower band empty), upper band should fill entire content area. Real fix (Sources/WenshuApp/App.swift L1079-1108): (1) Compute 'lowerBandAllHidden' flag (= !showAIChat && !showAIDynamic); (2) Compute 'hideLowerBand' = allZonesHidden OR lowerBandAllHidden (= union of Boss 72nd + 73rd triggers); (3) When hideLowerBand = true: upperBandHeight = contentH, lowerBandHeight = 0; (4) Hide D_h splitter + LowerBandZone when hideLowerBand = true. This is a SUB-case of Boss 72nd OOB 'all 5 hidden = editor fills all'. Boss 73rd OOB adds the additional trigger: when ONLY lower band zones hidden (aiChat + aiDynamic), upper band zones still fill full content (= no wasted space for empty lower band). | v0.24 boss验收 ticket 015.076 |
| **Lower band 2 toggles chat + dynamic (boss v0.24 ticket 015.074)** | Per Boss 8/25 71st OOB 'OK了, 实装下半区两个栏'. Implementation (Sources/WenshuApp/App.swift L1380-1427): wrap aiChat + aiDynamic ZoneModules in 'if' blocks; wrap D_v5 VSplitter in 'if showAIChat && showAIDynamic' (= same pattern as D_v1/D_v3); use .frame(maxWidth: .infinity, alignment: .top) + .layoutPriority(1) on both visible zones (= per Boss 69th OOB maxWidth pattern = fill lower band when sibling hidden); add .animation(.easeInOut(0.25), value:) for both (= per Boss 66th OOB '复刻过来' pattern). 015.074 Spec SUGGEST followup: removed dead code aiChatW + dynamicW local vars (= unused after Boss 69th OOB maxWidth fix replaced .frame(width:) per Boss 8/25 '工程弄的干净'). | v0.24 boss验收 ticket 015.074 |
| **Transition animation for hide/show (boss v0.24 ticket 015.077)** | Per Boss 8/25 74th OOB '很好, 功能实现了, 但那个过渡动画没有, 加一下, 显隐功能就毕业了'. Boss 74th OOB: hide/show feature graduation needs transition animation for visual smoothness. Real fix (= per Boss 8/22 '有问题先去查文档' + Apple官方 docs): (1) Per developer.apple.com/documentation/swiftui/transition: Transition protocol governs view appearance/disappearance; (2) Per swiftui-garden.com + createwithswift.com: .transition(.opacity) is the Apple官方推荐 default fade in/out; (3) Per createwithswift.com 3-step rule: transition(_:) + animation(_:value:). Implementation (Sources/WenshuApp/App.swift): (1) ZoneModule(sidebar) + .transition(.opacity.combined(with: .move(edge: .leading))) (= fade + slide from left); (2) D_v1 splitter + .transition(.opacity); (3) ZoneModule(preview) + .transition(.opacity); (4) D_v2 splitter + .transition(.opacity); (5) ZoneModule(tools) + .transition(.opacity.combined(with: .move(edge: .trailing))) (= fade + slide to right); (6) D_v3 splitter + .transition(.opacity); (7) ZoneModule(aiChat) + .transition(.opacity.combined(with: .move(edge: .leading))); (8) D_v5 splitter + .transition(.opacity); (9) ZoneModule(aiDynamic) + .transition(.opacity.combined(with: .move(edge: .trailing))). Combined with existing .animation(.easeInOut(duration: 0.25), value:) modifiers. | v0.24 boss验收 ticket 015.077 |
| **Band height transition animation (boss v0.24 ticket 015.078)** | Per Boss 8/25 75th OOB '上半区占用下半区时, 过渡动画没有'. Boss 75th OOB clarification: '上半区占用下半区时' = when upper band takes lower band space (= Boss 72nd + 73rd OOB triggers when all 5 zones hidden OR lower band zones all hidden); '过渡动画没有' = transition animation missing for band height changes. Real fix (= per Boss 8/22 '有问题先去查文档' + Apple官方 docs): (1) Per developer.apple.com/documentation/swiftui/animation: '.animation(_:value:) applies animation to all animatable values within the view when value changes' (= Apple官方推荐 way for smooth view resize); (2) Per developer.apple.com/documentation/swiftui/transitionphase: Transition protocol governs view appearance/disappearance. Implementation (Sources/WenshuApp/App.swift L1108): Added .animation(.easeInOut(duration: 0.25), value: hideLowerBand) on the VStack containing UpperBandZone + (conditional) LowerBandZone. When hideLowerBand flag changes (= Boss 72nd + 73rd OOB triggers flip), VStack animates band height changes smoothly. This completes the hide/show graduation per Boss 74th OOB + Boss 75th OOB: Per Boss 74th OOB, transitions added to 5 toggleable zones + 3 splitters. Per Boss 75th OOB, transition added to VStack for band height changes. | v0.24 boss验收 ticket 015.078 |
| **v0.24 boss验收 milestone** | Wenshu app first end-to-end manual verification by boss (anbaiqiang) on 2026-08-24 morning. Found 3 UX bugs (WenshuLLMError / Keychain -34018 / Model picker default) + assistant caught 1 doc drift (ChatView.swift). 5 commits total (3 boss + 2 assistant). Wenshu binary rebuilt + running (PID 49790). | v0.24 |
| **WenshuIcon** | Single-source-of-truth icon abstraction enum for the wenshu app surface. `enum WenshuIcon: CaseIterable` with 43 cases (= 41 distinct SF Symbols used today + 1 `missingIcon` 兜底 + 1 semantic-alias row kept for symmetry). Type-safe `var lucideIcon: LucideIcon` mapping exhausts every case (= **Layer 1 防护**: Swift's exhaustive switch refuses any future case without a row). `WenshuIcon.<case>.image(size:foregroundStyle:)` is the call-site surface; `WenshuIcon.image(name: String, size:foregroundStyle:)` static helper covers 11 dynamic-string call sites (= `tab.icon`, `category.icon`, `item.icon`, `iconNames[i]`, `sourceIcon`, `task.icon`, `ZoneToolbarAction.icon`, `ZoneIcon(systemName:)`). Future SVG / PNG migration = edit only this enum. | v0.25 ticket 001 |
| **LucideIcon** | Underlying third-party icon set (= MIT, 1700+ outline glyphs, native SwiftUI `Shape` rendering via `bring-shrubbery/lucide-swift 1.25.0`). Inherits foreground color via `.foregroundStyle(...)` at the call site (= Apple HIG system color inheritance). Renders Lucide glyphs from icon name strings via `LucideIcon(rawValue:)` lookup or via the type-safe enum case. Library upstream tracks Lucide releases (`lucide-swift 1.25.0` bundles icons from `lucide@1.25.0`). | v0.25 ticket 001 |
| **SPMDependency** | Swift Package Manager dependency (= replaces pre-v0.25 `no third-party deps` baseline). Owner 2026-08-26 grill unlocked third-party SDKs with grill gate: every new dep must be owner-approved via grill, ANAN does not silently add SDKs. First applied = `bring-shrubbery/lucide-swift 1.25.0` (ticket 001). Vendor fallback path documented (= full source git-cloned into `Sources/WenshuApp/Vendor/Lucide/` if the SPM dep is ever removed); all 3 protection layers still hold under vendor. | v0.25 ticket 001 |
| **三層防護** | WenshuIcon migration three-layer protection (= owner 2026-08-26 demanded). **Layer 1 (avoidance)**: exhaustive `var lucideIcon: LucideIcon` switch with no default branch = Swift's compile-time guarantee that no `WenshuIcon` case silently falls through. **Layer 2 (avoidance)**: when caller passes `foregroundStyle: nil`, force `.foregroundStyle(Color.primary)` so the icon is never invisible against a foreground-less SwiftUI environment. **Layer 3 (兜底)**: static `WenshuIcon.image(name:)` helper covers the 11 dynamic-string call sites with `Lucide(name) ?? Lucide(.circleQuestionMark)`, so a mistyped string renders a visible Lucide missing-icon glyph instead of crashing or rendering blank. All three layers verified by build (= 0 errors), grep (`Color.primary` × 2, `circleQuestionMark` × 5 in `Sources/WenshuApp/Core/Icon/WenshuIcon.swift`), and acceptance numbers from issue 001. | v0.25 ticket 001 |