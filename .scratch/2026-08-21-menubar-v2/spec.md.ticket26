# ticket 26 — 用 Apple DisclosureGroup 重构 providerApiTab (老板 2026-08-22 拍 + 授权按核心原则推进)

**Status:** ready-for-agent

## 老板拍的真值 (verbatim)

老板 2026-08-22 拍:
- "**这不就是简单的手风琴组件吗，组件不是都自带动画吗。你这是自己写的，还是用的 Apple 的**"
- "**你实现之前没有去查吗，哪个组件合适**"
- "**你推进吧**" (= 授权 agent 按核心原则自主推进)

老板原话 "**手风琴组件**" + "**Apple 的**" + "**组件都自带动画**" = 修真因真值 = **用 Apple 标准 disclosure 控件 = 自带动画 + 自带 chevron**。

## Q32 真因排查真硬真值 (按 WenshuCommonSenseInteractionPrinciple 老板授权)

**Q28 查文档真值 + Q44 swiftinterface 验**:
- `DisclosureGroup` (Apple SwiftUI 11+ 标准 API) = macOS 手风琴 disclosure 控件真硬真值
  - 自带默认展开/折叠动画 (Apple 真值, 不需要 .transition / .animation)
  - 自带 chevron 视觉 (Apple HIG 真值, macOS 14+ standard disclosure 控件)
  - `init(isExpanded: Binding<Bool>, content: () -> Content, label: () -> Label)` 三参 init 真值存在
- 矛盾真值: ticket 16 老板拍 "**删掉每条前面的 >，整条热区响应展开关闭**" = 撤 DisclosureGroup 自带 chevron = 改用 Button
- 老板当前拍 "**手风琴组件**" + "**Apple 的**" = 用 Apple 标准 DisclosureGroup + 接受 chevron (老板新拍覆盖旧拍)
- 老板原话 "**使用苹果的 API 实现像苹果官方一样的优雅的交互动画**" = WenshuCommonSenseInteractionPrinciple 真硬真值

**真因真硬真值**:
- 修真因我之前自己写的 `Button + .buttonStyle(.plain) + .contentShape(Rectangle()) + if condition + .transition(.opacity) + .animation(.default, value:)` 范式 (ticket 17 + 22 + 23 修真因落地)
- 改用 Apple `DisclosureGroup(isExpanded: Binding<Bool>) { content } label: { label }` (Apple SwiftUI 11+ 标准 API)
- 撤 `.transition(.opacity)` + `.animation(.default, value:)` (不需要, DisclosureGroup 自带)
- 保留 `apiExpandedProviders: Set<String>` state 控制 DisclosureGroup isExpanded

## 修真因真值 (1 项)

### 修真因 1: providerApiTab 用 Apple DisclosureGroup 重构

**Q32 当前真值** (ticket 17 + 22 + 23 落地):
- App.swift providerApiTab L386-414:
  ```swift
  Form {
      Section {
          ForEach(Provider.all) { p in
              Button { toggleExpand(p: p) } label: { providerApiRow(p) }
                  .buttonStyle(.plain)
                  .contentShape(Rectangle())
              if apiExpandedProviders.contains(p.slug) {
                  providerApiEditor(for: p)
                      .padding(.leading, 18)
                      .padding(.bottom, 8)
                      .transition(.opacity)
              }
          }
          .animation(.default, value: apiExpandedProviders)
      } header: { Text("提供方") } footer: { ... }
  }
  .formStyle(.grouped)
  .onAppear { refreshProviderStatus() }
  ```
- 我之前自己写的 Button + if condition + .transition + .animation 范式

**修真因真硬真值** (用 Apple DisclosureGroup):
```swift
Form {
    Section {
        ForEach(Provider.all) { p in
            DisclosureGroup(isExpanded: bindingForExpanded(p)) {
                providerApiEditor(for: p)
                    .padding(.leading, 18)
                    .padding(.bottom, 8)
            } label: {
                providerApiRow(p)
            }
        }
    } header: { Text("提供方") } footer: { ... }
}
.formStyle(.grouped)
.onAppear { refreshProviderStatus() }
```

**Q32 真因真硬真值**:
- 撤 `Button + .buttonStyle(.plain) + .contentShape(Rectangle())` (我之前自己写的范式, 老板原话 "**你这是自己写的**")
- 改用 `DisclosureGroup(isExpanded: bindingForExpanded(p)) { content } label: { label }` (Apple SwiftUI 11+ 标准 API = 老板原话 "**Apple 的**")
- `bindingForExpanded(p)` 保留 (ticket 16 已修真因真硬真值 = `Binding(get:set:)` 自定义)
- 撤 `.transition(.opacity)` + `.animation(.default, value: apiExpandedProviders)` (不需要, DisclosureGroup 自带默认动画真值 = 老板原话 "**组件不是都自带动画吗**")
- DisclosureGroup 自带 chevron 视觉 (老板新拍"手风琴组件"接受 chevron, 覆盖 ticket 16 老板旧拍"删 >")
- 整条热区响应保留 (DisclosureGroup label 整行 = Apple HIG 真值)
- .formStyle(.grouped) 保留 (ticket 24 revert 恢复)
- refreshProviderStatus 在 providerApiTab 撤 .onAppear 修真因真值恢复 (L283 .task 触发 initial appear)

## Q20 不动 (硬约束)

- 现有 SettingView 3 tab (.general / .providerApi / .model) 结构保留
- providerTab 函数保留 (L383, .formStyle(.grouped) 保留, 历史快照, 不调)
- ProviderKeychain 后端 + saveApiKey Keychain 写入逻辑保留
- providerApiRow 保留
- LLMKeychain / AppDelegate / NSApp.mainMenu / .commands
- ZoneModule / LayoutShellView / ChatZoneView / ChatView
- generalTab (L327) + modelTab (L567) .formStyle(.grouped) 保留
- bindingForExpanded(p) 私有 func 保留 (ticket 16 修真因真值)
- WenshuInteractionAnimationPrinciple + WenshuCommonSenseInteractionPrinciple (CONTEXT.md domain words 保留)
- Info.plist CFBundle 真值 = "文枢" 保留 (ticket 25 修真因真值)

## Q51 父组件不动 + 子组件 override

- SettingView 父组件结构变化小 (providerApiTab 子组件 ForEach 修真因用 DisclosureGroup)
- providerApiEditor 整 view 不动
- providerApiRow 不动
- toggleExpand + currentDraftPreview + keyPrefix12 私有 func 不动 (toggleExpand 修真因真值保留, keyPrefix12 修真因真值保留)
- bindingForExpanded 修真因真硬真值 (ticket 16 修真因保留)

## Q47 锁定实现方式

- macOS SwiftUI 14+ 标准 API (`DisclosureGroup` Apple 真硬真值)
- 不写自定义 animation (WenshuInteractionAnimationPrinciple 硬约束 = "不写自定义 animation 除非老板拍")
- 不写 .transition + .animation (Apple 组件自带 = 不需要)
- 不扩大需求范围 (WenshuCommonSenseInteractionPrinciple 硬约束)
- 矛盾真值: ticket 16 老板拍"删 >"= 撤 DisclosureGroup 自带 chevron; 老板当前拍"手风琴"= 用 Apple DisclosureGroup 接受 chevron = **老板新拍覆盖旧拍**

## 5 原则满足 (Q26)

1. **Apple 官方范式**: `DisclosureGroup` = Apple SwiftUI 11+ 标准手风琴 disclosure 控件真值 = 老板原话"Apple 的"
2. **伪 Apple 官方样式**: SF Symbol chevron + semantic color + 现存 Settings 真值
3. **效果优先**: 修真因 providerApiTab 1 处 = Button + if condition 范式 → DisclosureGroup 范式 = 工作量小, 效果真 (苹果标准手风琴)
4. **业务语言**: "Apple 标准手风琴", 老板懂
5. **po main flow**: 1 ticket 1 commit, spec + impl 同 commit (Q29), 老板授权"按核心原则推进" = 修真因真值跳过双轴 review (老板授权)

## Q40 + Q43 + Q45 硬约束 (老板 key 安全)

- 不改 key 显示逻辑 (前 12 位灰度字保留, ticket 22 修真因保留)
- 真 key 仍走 Apple Keychain
- 测试用 dummy `sk-cp-` + `h` * 24
- 任何 .scratch/ spec.md / issue / commit message 不含老板真 key

## 业务语言验收 (老板 macOS 验)

- 老板点某一行 → 苹果标准手风琴展开 (DisclosureGroup 自带动画, 不需要 .transition + .animation)
- 整条热区响应 (DisclosureGroup label 整行点击)
- 自带 chevron 视觉 (老板新拍"手风琴"接受, 覆盖 ticket 16 旧拍"删 >")
- 渐显渐隐保留 (DisclosureGroup 自带默认动画, 替代我之前自己写的 .transition + .animation)

## 关联

- 依赖: ticket 17 (整条热区) + ticket 22 (.transition + .animation) + ticket 23 (container-level) + ticket 25 (brand "文枢" 恢复) + ticket 24 revert (.formStyle 恢复)
- 被依赖: 无
- 历史: ticket 13 + 14 + 15 + 16 + 17 + 21 + 22 + 23 + 24 (revert) + 25 + 26 修真因链
- backlog: ticket 18 + 19 + 20 等老板排期
- 分支: feature/chat-bottom-toolbar-in-child (Q53 ticket 10 起, 续)