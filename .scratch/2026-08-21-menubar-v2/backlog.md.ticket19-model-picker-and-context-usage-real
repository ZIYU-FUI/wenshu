# backlog — ticket 19 模型选择器 + context usage 真实功能 (老板 2026-08-22 06:20 拍 backlog 阶段)

## 老板拍的真值 (verbatim)

- "实现这红框两个功能的真实功能"
- "**先记需求，一会排期做**"

## 老板截图红框 2 个区域 (截图右下角 + 左下角, ChatZoneView 底栏)

### 红框 1 (左下) = 模型选择器 Menu (ticket 10 + 13 + 14 已修真因 18 PT inset)

**当前真值**:
- ChatZoneView L987-1016 Menu 组件
- label HStack(spacing: 4) { Image("cpu") + Text(currentModel) + Image("chevron.up.chevron.down") }
- Menu 整容器 `.padding(.leading, 14)` (ticket 14 修真因 18 PT 视觉)
- Menu 真值 = 静态显示 `currentModel` + `UserDefaults.standard.string(forKey: "wenshu.llm.model")` fallback
- 下拉菜单 = `ForEach(availableModels, id: \.self)` = Provider.all defaultModels 静态 fallback (实际可能不准)
- .task 时调 `MiniMaxModelFetcher.loadModelIds(apiKey: key, baseUrl: base)` 拉真可调模型列表

**真实功能真硬** (老板下个需求已声明):
- 修真因 1a = Menu 下拉显示真可调模型列表 (走 MiniMaxModelFetcher.loadModelIds 真值)
- 修真因 1b = 切模型时自动 refresh ChatViewModel.currentModel + LLM API call 走新模型
- 修真因 1c = 模型列表状态 (loading / success / failure) UI 反馈

### 红框 2 (右下) = context usage 进度条 (compactNumber + ProgressView)

**当前真值**:
- ChatZoneView L1020-1027 HStack(spacing: 6) {
    Text("\(compactNumber(contextUsed)) / \(compactNumber(contextMax))")
        .font(.system(.system, size: 13))
        .foregroundStyle(.tertiary)
    ProgressView(value: Double(min(contextUsed, contextMax)), total: Double(max(1, contextMax)))
        .progressViewStyle(.linear)
        .frame(width: 80)
        .tint(contextUsed >= contextMax ? .red : (contextUsed > contextMax * 3 / 4 ? .orange : .green))
  }
- contextMax = 131072 (硬编码 MiniMax-M3 真值 context window = 128k = 131072 tokens, Hermes ModelInfoResponse.context_length 范式)
- contextUsed = `@State private var contextUsed: Int = 0` (硬编码 0)
- **注意**: ChatZoneView 的 contextUsed 是 @State 硬编码 0, 不跟 ChatViewModel.contextUsed 联动 = 老板截图 "0 / 131.1k" 一直显示 0

**真实功能真硬** (老板下个需求已声明):
- 修真因 2a = contextUsed 走 ChatViewModel.contextUsed (不硬编码 0)
- 修真因 2b = 真 token 计数 (调 MiniMax API response.usage.input_tokens + output_tokens)
- 修真因 2c = 进度条颜色按 usage/contextMax 比例显示 (绿/橙/红, 已有 tint 修真因保留)

## 业务语言真因

- 症状: 红框 2 个 UI 元素是占位/近似, 不是真实功能
- 风险: 用户看到 "MiniMax-M3" 模型名但下拉不是真可调列表; "0 / 131.1k" 进度条永远 0 = 误以为没在用
- 老板要的: 实现 2 个功能的真实功能 (真实模型列表 + 真实 token 计数)

## Q47 + Q51 + Q20 锁定边界 (下轮 impl 用)

- Q51 父组件不动: ChatZoneView / ChatView / ChatViewModel / WenshuConductor / AgentProtocol 父组件结构不变
- Q51 子组件 override: 在 ChatZoneView / ChatViewModel 内修真因真值
- Q47 锁定实现方式: macOS SwiftUI 14+ 标准 API + MiniMaxModelFetcher.loadModelIds + MiniMaxVerifier 真值
- Q20 不动:
  - ChatZoneView VStack 顺序 (ticket 13 老板拍 "就现在的位置, 不改")
  - ChatView 输入框布局
  - ChatBottomBar 18 PT inset (ticket 13 + 14 已修真因)
  - ChatZoneView 顶 1 PT splitterLine 分割线
  - ChatViewModel.send / clear / switchModel / loadAvailableModels API (修真因 model picker 真值是修真因 "下拉列表真值" 而非修真因 "send/clear API")
  - WenshuConductor.handle 真值 (intent classify → 派子 agent → LLM synthesis)
  - AgentProtocol 协议
- Q40 + Q45 不动: key 真值不入 log/commit, 测试用 dummy `sk-cp-` + `h` * 24

## 下轮要做的 ticket 候选

**候选 A: 模型选择器 Menu 真值** (单独 ticket)
- ChatZoneView Menu ForEach 修真因: 走 MiniMaxModelFetcher.loadModelIds(apiKey, baseUrl) 真可调模型列表
- ChatZoneView @State 真值: currentModel / availableModels 修真因逻辑
- ChatViewModel 修真因: 加 @Published var availableModels: [String] (跟 ChatViewModel.loadAvailableModels 同步)
- LLM API call (MiniMaxVerifier.chat) 用真 currentModel + availableModels 真值

**候选 C: context usage 真值** (单独 ticket)
- ChatZoneView contextUsed 撤硬编码 0, 走 ChatViewModel.contextUsed
- ChatViewModel.recomputeContextUsed 修真因: 调 MiniMax API response.usage.input_tokens + output_tokens (真 token 计数, 不是估算)
- ChatViewModel.send 修真因: send 后接收 LLM response 拿真 token usage → update contextUsed
- contextMax 修真因: 走 currentModel 真值 context window (不是硬编码 131072)

**候选 A + C 合并**: 一个 ticket 修真因 2 个真值 (工作量中等)

## 关联

- 历史: ticket 13 + 14 chat zone 底栏 18 PT inset 已修真因 (Q47 锁定实现方式不变)
- 历史: ticket 10 ChatZoneView 子组件范式已修真因 (Q51 父组件不动 + 子组件 override)
- 历史: 之前 v0.20 ticket 04 + 05 LOGO .app bundle 范式已修真因
- 分支: feature/chat-bottom-toolbar-in-child (Q53 ticket 10 起, 续)
- backlog: ticket 18 (AI 回复状态指示器) 也是 backlog 阶段, 等老板排期先后

## Q34 拍板

**本轮不修真因** (老板原话 "先记需求，一会排期做"). 老板下轮排期时再起新 ticket 19 (候选 A + C 合并) 或 ticket 20 + 21 (分两个 ticket).

## Status

backlog / 等老板排期