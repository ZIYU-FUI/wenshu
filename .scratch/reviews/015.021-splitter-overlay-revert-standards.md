# Standards Axis Report — ticket 015.021 / commit ee801d0dd

- **Commit:** `ee801d0dd850c92196d3da1db56d03c6fc01adf2` (`fix(wenshu): v0.24 boss验收 — splitter overlay revert, 比例 same (ticket 015.021)`)
- **Author:** `cc-runner (wenshu) <cc-runner-wenshu@local>` · AuthorDate 2026-08-25 10:36:18 +0800
- **Files:** `Sources/WenshuApp/App.swift` (+9 / −36 = 45 changed lines), `Sources/WenshuApp/LayoutShellViewModel.swift` (+0 / −16 = 16 changed lines) = **2 files total**, 15 insertions / 52 deletions (net −37 = simplification)
- **Scope reviewed:** Standards axis only (AGENTS.md §11 hard rules + project conventions). No Spec / Architecture / tests.
- **Prior commit referenced:** `6f3019e50` (ticket 015.018, the previous 双轴 FAIL cleanup that introduced the overlay + predicate this commit reverts).
- **Boss 8/25 eighth OOB cited in body:** '拖拽线实现的有问题, 看着像多出来一根线, 然后上下还是没对齐. 你不要纠结线的问题, 核心是, 右上区和右下区这两个区的默认启动的时候. 占总宽度的比例是一样的'.
- **Boss image:** `composer-images/composer_2026-08-25_02-20-19-921_643a14.png` (right edge with extra visual line = overlay artifact).

---

## PASS

1. **12 forbidden vocabulary (12-term family) — CLEAN** — grep over commit body + both touched files (post-commit) for `修真|渡劫|筑基|返虚|结丹|金丹|元婴|飞升|天劫|雷劫|心魔|魔障` returns **0 hits**. Same outcome as 015.005, 015.006, 015.012, 015.014, 015.018 reports.

2. **AGENTS.md §11 English-only carve-outs respected (UI strings, boss quotes)** — the only CJK content remaining in the diff is in **explicit carve-out categories** allowed by AGENTS.md + Boss 8/25 'UI 全中文' clarification:
   - UI strings: N/A for this commit (no new UI strings; surrounding UI strings unchanged and out of scope).
   - Boss verbatim quotes cited as evidence of the spec:
     - `Boss 8/25 eighth OOB '拖拽线实现的有问题...占总宽度的比例是一样的'` (commit body L7-9) — the full Boss quote being responded to.
     - `Boss 8/25 eighth OOB '不要纠结线的问题, 核心是比例'` (App.swift L1008) — the directive being satisfied.
     - `Boss spec '占总宽度的比例是一样的'`-equivalent citation (commit body L26).
   - `Boss`/`老板` audit markers in `v0.24 boss验收fix` + `Boss 8/25 eighth OOB ticket 015.021` — established project convention used in every prior v0.24 fix commit (015.005/006/012/014/018).
   These uses are intentional anchors (per 015.014 PASS item 5 + 015.018 PASS item 2), not violations.

3. **AGENTS.md §11 forbidden neutral words — CLEAN** — grep over commit body for `可|应当|或许|可能|应该|建议|考虑|试图|尽量|大概|也许|或|任意|大概率|通常|一般来说` returns **0 hits**. The body uses factual verbs throughout (`REMOVED`, `restored`, `removed`, `acknowledged but not fixed`, `satisfied`, `passed`). Same outcome as 015.018 reports.

4. **Apple HIG SwiftUI idiomatic pattern — straightforward simplification** — the change is purely subtractive. Post-commit:
   - `App.swift` L1008-1016: a single explanatory code comment replaces the old `.overlay(alignment: .topLeading) { if ... { Rectangle()...allowsHitTesting(false) } }` block. The block being removed is the canonical Apple HIG overlay-with-non-hit-testing-subview pattern, but the **decision to drop it entirely** is also a canonical pattern (= "don't use an overlay when you don't need one"). This is consistent with the Apple HIG "decoration vs interaction" split — by reverting the overlay, the code goes back to relying solely on the underlying `VSplitter` rendering (already interactive by definition) and `ZoneModule` background fills.
   - `App.swift` L1081: `VSplitter(length: bandH, totalWidth: totalW, splitterIndex: 2, vm: vm)` is restored to unconditional render (= the simplest SwiftUI form: a subview inside an `HStack`, no `if` guard, no `.opacity()` hack, no `Group` workaround). This is the most idiomatic form for "always render this splitter" and is exactly the Apple HIG pattern for subview composition.
   - `LayoutShellViewModel.swift`: predicate removal is canonical — a function with zero call sites is dead code, and removing it is the canonical Swift codebase hygiene action.

5. **Boss-anchored audit comments present on every retained block** — per the project convention, every new/modified code block carries the `v0.24 boss验收fix` + `Boss 8/25 eighth OOB ticket 015.021` audit trio:
   - `App.swift` L1008-1016 (replacement comment for the removed overlay): cites `Boss 8/25 eighth OOB '不要纠结线的问题, 核心是比例'` + names the core spec (rightmost zone widths must have the same ratio of total width) + cites the satisfying `LayoutTokens.toolsWRatio == LayoutTokens.dynamicWRatio = 400/1920` + explicitly distinguishes the acknowledged-but-not-fixed 4 PT splitter X discrepancy.
   - `App.swift` L1076-1080 (VSplitter D_v3 restoration comment): cites `Boss 8/25 eighth OOB` + explains the restoration (= D_v3 no longer hidden) + re-affirms the core fix (`toolsWRatio == dynamicWRatio = 400/1920`).
   - `LayoutShellViewModel.swift`: no new comment needed (predicate removed entirely, not modified).
   - All three audit markers match the precedent set by 015.012, 015.014, 015.018.

6. **1 zone 1 ticket 1 commit 2 file structure (Boss 8/22 precedent applies)** — this commit touches exactly the two files the previous ticket 015.018 implementation modified:
   - `App.swift` touches the `.overlay` block in `LayoutShellView.body` (L1005-1035 in pre-commit state → L1008-1016 replacement comment in post-commit state) and the `VSplitter(splitterIndex: 2)` render call in `UpperBandZone` (L1081).
   - `LayoutShellViewModel.swift` only touches the `areRightSplittersAligned()` predicate (L148-163 in pre-commit → entirely deleted in post-commit).
   - No drive-by edits, no formatting churn, no unrelated refactor, no incidental indentation fixes.
   - This is **2 files** but matches the Boss 8/22 'tightly coupled atomic change' precedent (mirrored in `394d8be2f` = 2 files for ticket 015.005, `a03e0c0a5` = 2 files for 015.014, `6f3019e50` = 2 files for 015.018, all accepted under the same exception in prior reports). Commit body L11-19 explicitly enumerates this scope (`Fix (2 files):`).

7. **Vendor-neutral naming** — all retained identifiers are vendor-neutral. The deleted identifier `areRightSplittersAligned()` was already vendor-neutral (= descriptive English), and its removal eliminates that name from the codebase entirely. No new identifiers introduced. `grep -iE 'minimax|openai|anthropic|claude|gpt|gemini|mistral'` over the diff returns **0 hits**. Same outcome as 015.014 PASS item 7 and 015.018 PASS item 6.

8. **Atomic change scope — purely subtractive revert** — the diff is tightly scoped to three concrete changes (commit body L11-19 enumerates them, all deletion-only except for replacement comments):
   - `App.swift` L1005-1035: `.overlay(alignment: .topLeading) { if ... { Rectangle()...allowsHitTesting(false) } }` block deleted (≈27 lines removed) + replacement 9-line audit comment added.
   - `App.swift` L1100-1102: `if !vm.areRightSplittersAligned() { VSplitter(...) }` guard wrap removed + D_v3 restored to unconditional `VSplitter(...)` render at L1081.
   - `LayoutShellViewModel.swift` L148-163: `areRightSplittersAligned()` function (16 lines, including doc-comment) deleted entirely.
   - Net: **−37 lines** (15 insertions / 52 deletions). The negative delta is the **defining feature** of this commit (= Boss 8/22 '工程管理我不懂, 你把工程弄的干净' principle in action).

9. **Dead code removal is complete and consistent with the Boss 8/22 simplification principle** — the commit's defining property is subtraction:
   - The `areRightSplittersAligned()` predicate at `LayoutShellViewModel.swift` had **zero callers** after the overlay block was removed (`grep -rn 'areRightSplittersAligned' Sources/` returns 0 hits post-commit). A predicate with zero callers is dead code by definition.
   - The `if !vm.areRightSplittersAligned() { VSplitter(...) }` conditional render is dead code by the same criterion (= its predicate has no callers, so the conditional is permanently evaluated as `if !false { ... }` after the only call site is removed — actually permanently `if true { ... }` because removing the overlay doesn't change `areRightSplittersAligned()`'s output, but the call site doesn't exist, so the compiler-eliminable `if` becomes pointless).
   - Removing both atomically in the same commit is correct (= dead-code lifecycle should end atomically, not be left dangling).
   - This directly satisfies Boss 8/22 '工程管理我不懂, 你把工程弄的干净' (= the Boss explicitly cited this principle in the commit body L51 as the motivation for the cleanup).

10. **Ratio verification — toolsWRatio == dynamicWRatio == 400/1920 is mathematically TRUE in `LayoutTokens`** — verified:
    - `LayoutTokens.toolsWRatio = 400.0 / 1920.0` (App.swift L78).
    - `LayoutTokens.dynamicWRatio = 400.0 / 1920.0` (App.swift L82).
    - At default `totalW = LayoutTokens.designW = 1600 PT`: both zones have width `1600 * 400/1920 ≈ 333.33 PT`. Same ratio (= 400/1920 ≈ 20.83% of total width each).
    - This is exactly Boss's core spec '占总宽度的比例是一样的' (= ratio of total width is the same for both right zones).
    - The commit body correctly identifies that **no further ratio adjustment was needed** (= the existing `LayoutTokens` already satisfies the spec at initial state, offsets=0).

11. **Splitter X discrepancy (D_v3 vs D_v5, 4 PT off) is acknowledged in comments, not fixed (per Boss拍)** — the commit correctly distinguishes between the **zone-width ratio** (= satisfied, Boss spec) and the **splitter X position** (= 4 PT off, acknowledged):
    - D_v3 X = `(200 + 520 + 794) / 1920 = 1514/1920` (= sidebar + preview + editor sum, pre-existing `LayoutTokens` math).
    - D_v5 X = `1518/1920` (= `LayoutTokens.aiChatRatio`).
    - Delta at 1920 PT width = **4.00 PT** (= commit body's "4 PT off" claim).
    - The replacement comment at `App.swift` L1014-1016 explicitly names this discrepancy: `Splitter positions may still differ slightly (= D_v3 X = 1514/1920 vs D_v5 X = 1518/1920, 4 PT off) — that's a splitter placement issue, not a zone-width issue.`
    - The commit body L31-34 also acknowledges this explicitly: `Per Boss '不要纠结线的问题', this splitter placement discrepancy is acknowledged but not fixed (= Boss拍 core = zone widths ratio, not splitter X positions).`
    - This is correct per Boss 8/25 eighth OOB '不要纠结线的问题' (= the Boss explicitly de-scoped the X-position issue, so re-fixing it would violate Boss拍).

12. **UI strings in Chinese — N/A for this commit (no new UI strings added)** — consistent with Boss 8/25 'UI 全中文' rule + the carve-out pattern documented in 015.005/014/018 PASS items. Surrounding UI strings (e.g. tab labels, help text) unchanged.

13. **First / last line fact rule (AGENTS.md L10)** — commit body opens with a fact (`Boss 2026-08-25 eighth OOB: '拖拽线实现的有问题...占总宽度的比例是一样的'.`) and closes with a fact (`Pending per Boss 8/25 '双轴每次都跑' protocol: 双轴 code-review auto-dispatched for this commit.`). Both lines are concrete, testable, and verifiable. Matches the AGENTS.md `First line of every doc = fact. Last line of every doc = fact.` constraint.

14. **Verification trace is concrete** — commit body L37-43 records:
    - `swift build: clean`
    - `App rebuilt + relaunched (PID 37563)`
    - `Right-most zones (.specializedTools upper + .aiDynamic lower) both have width = totalWidth * 400/1920 at initial state (= same ratio).`
    - `Splitter overlay REMOVED (= no extra line artifact).`
    - `Total tests: 584 / 80 unit pass`
    - Matches 015.012 PASS item 10 (`specific PID + byte size + path-not-at-legacy assertion`) and 015.018 PASS item 13 (verification trail pattern).

15. **Predicate removal is consistent with Swift's dead-code-elimination contract** — `func areRightSplittersAligned()` was an instance method on `@Observable final class LayoutShellViewModel` with no `override`, no `@MainActor` decoration, no protocol conformance. Removing it does not break any subclass invariant, any protocol witness, or any `@Observable` macro-generated registrar (= the macro reads `var` properties and `_$observationRegistrar` calls, not arbitrary methods). The Swift compiler would have flagged a warning (`@unused` for `@Observable` methods is not auto-emitted, but the lack of callers would show up in any test coverage tool). Removing it manually is the correct action.

---

## SUGGEST

### S1 — Commit body has a typo: `dialyMXRatio` should be `dynamicWRatio`

At commit body L27:
```
LayoutTokens.toolsWRatio == LayoutTokens.dialyMXRatio == 400/1920
```

The intended identifier is `dynamicWRatio` (= `LayoutTokens.dynamicWRatio = 400.0 / 1920.0` at App.swift L82). `dialyMXRatio` is not a defined identifier in the codebase (`grep -rn 'dialyMXRatio\|dialyMX' Sources/` returns 0 hits outside this commit body). This is a one-character-per-character transposition / typo (= `dialyMX` looks like the result of typing `d-i-a-l-y-M-X` instead of `d-y-n-a-m-i-c-W-R`).

Impact: zero functional impact (the commit body is human-readable text, not parsed by any tool). But:
- A future maintainer searching for `dialyMX` in the codebase will find nothing (= bad search experience).
- The Standards axis 'commit body must be a fact' rule is partially weakened: the body contains an unrecognizable identifier that doesn't correspond to any defined `LayoutTokens` member.

Recommendation: amend the commit body to fix the typo (`dialyMXRatio` → `dynamicWRatio`). Alternatively, leave it and accept the typo as a one-off slip — but flag for the next refactor commit if the commit body is ever amended for another reason.

Non-blocking — the typo does not affect compilation, runtime, or any other Standards PASS item.

### S2 — `rightX` no longer exists, so no rename suggestion needed (S4 of prior 015.018 report is now obsolete)

The 015.018 SUGGEST item S4 (`rightX` → `dV5X` rename) is now obsolete because `rightX` was deleted in this commit along with the overlay block. Flagging this as a confirmation only — no action needed. The retained code at `App.swift` L1081 uses `splitterIndex: 2` (= the conventional naming pattern throughout the file), so no rename is in flight.

### S3 — `areRightSplittersAligned` is now gone, so the S1 predicate-rename suggestion from 015.018 is also obsolete

The 015.018 SUGGEST item S1 (`areRightSplittersAligned` → `shouldMergeRightSplitters` rename) is now obsolete because the predicate was deleted entirely. Flagging as a confirmation only — no action needed.

### S4 — Replacement comment at `App.swift` L1008-1016 is dense and could split into two paragraphs

The replacement comment is 9 lines long and covers:
1. The removal (line 1).
2. The Boss拍 core spec (lines 2-3).
3. The `LayoutTokens` math satisfying the spec (lines 4-5).
4. The acknowledged splitter X discrepancy (lines 6-7).

A future maintainer reading this comment in isolation would benefit from a blank line between (2-3) and (4) to separate the "what was done" (1) + "what the spec is" (2-3) from the "what's still imperfect" (4). This is a readability polish only.

Non-blocking — the comment is fully correct as written.

### S5 — The 015.018 doc-comment block deleted at `LayoutShellViewModel.swift` L148-159 was valuable audit context

The deleted doc-comment at `LayoutShellViewModel.swift` L148-159 (pre-commit) documented:
- The predicate's role (= detect Boss spec '宽度是一模一样的').
- The initial-state assumption (= offsets=0 → toolsWRatio = dynamicWRatio = 400/1920).
- The 4 PT splitter X discrepancy acknowledgment.
- A cross-reference to "ticket 015.018 双轴 review for math".

The replacement is the `App.swift` L1008-1016 comment + the `App.swift` L1076-1080 comment. Together, those two comments cover (a) the spec and (b) the D_v3 restoration, but **the explicit cross-reference to "ticket 015.018 双轴 review for math" is lost** (= the 015.018 review file at `.scratch/reviews/015.018-splitter-alignment-cleanup-standards.md` is still on disk and discoverable, but no code comment points at it anymore).

For an external maintainer trying to reconstruct the design history, this is a minor regression: the in-code audit trail now stops at "Boss 8/25 eighth OOB ticket 015.021" without naming the upstream ticket 015.018 that this commit supersedes.

Recommendation: add one line to the `App.swift` L1008-1016 comment naming the upstream: `// Supersedes ticket 015.018 (= overlay approach, reverted here per Boss 8/25 eighth OOB).`

Non-blocking — the Git history itself preserves the linkage (= `git log` shows the 015.018 → 015.021 chain), so the loss is recoverable.

---

## VERDICT

**PASS** — All 15 PASS items hold. The 5 SUGGEST items are non-blocking polish:
- S1: typo in commit body (`dialyMXRatio` → `dynamicWRatio`) — should fix in next amend.
- S2, S3: confirmations that prior 015.018 SUGGEST items are now obsolete (no action).
- S4: comment readability polish (split into paragraphs).
- S5: minor audit-trail regression (lost cross-reference to ticket 015.018 in code comments).

No FAIL items.

The implementation is **functionally and Standards-aligned** for the Boss 8/25 eighth OOB response:
- **F1 (English-only hard rule)**: the only CJK content in the diff is in explicit carve-out categories (boss quotes cited as evidence, `Boss` audit markers, no new UI strings). Same pattern as 015.005/006/012/014/018 PASS items.
- **12 forbidden vocabulary**: 0 occurrences.
- **Apple HIG SwiftUI patterns**: subtractive cleanup, canonical `VSplitter` unconditional render, removal of overlay + non-hit-testing subview in favor of relying solely on the underlying interactive splitter.
- **1 zone 1 ticket 1 commit 2 file (Boss 8/22 precedent)**: exactly the two files the previous ticket 015.018 modified, with all 015.018 additions removed.
- **Vendor-neutral naming**: 0 vendor-specific identifiers introduced (and one dead-code identifier removed).
- **Atomic scope**: tightly scoped to the 3 concrete reverts (overlay block deletion, D_v3 conditional-render guard removal, predicate removal).
- **Boss-anchored audit comments**: every retained block carries `v0.24 boss验收fix` + `Boss 8/25 eighth OOB ticket 015.021`.
- **UI strings in Chinese**: N/A (no new UI strings added).
- **Ratio verification**: `LayoutTokens.toolsWRatio == LayoutTokens.dynamicWRatio = 400/1920` is mathematically TRUE — both right zones have width `totalW * 400/1920 ≈ 333.33 PT` at default `totalW = 1600 PT`. Boss spec '占总宽度的比例是一样的' is satisfied.
- **Splitter X discrepancy acknowledged**: D_v3 X = 1514/1920 vs D_v5 X = 1518/1920 (= 4 PT off at defaults) is explicitly named in both the commit body and the replacement comment as **acknowledged but not fixed** per Boss '不要纠结线的问题'. Correct per Boss拍.
- **Dead code removal**: the predicate had 0 callers post-overlay-removal and was correctly deleted in the same commit (= atomic dead-code lifecycle).
- **Boss 8/22 simplification principle**: net −37 lines (15 insertions / 52 deletions) is the defining property of this commit. The Boss-cited '工程管理我不懂, 你把工程弄的干净' principle is materially satisfied.

The previous ticket 015.018 implementation (overlay at D_v5 X + D_v3 conditional hide + predicate `areRightSplittersAligned`) is now fully reverted:
- `App.swift` L1005-1035: overlay block deleted.
- `App.swift` L1081: `VSplitter(splitterIndex: 2)` rendered as a plain subview inside `HStack`.
- `LayoutShellViewModel.swift` L148-163: predicate + doc-comment deleted.

The codebase returns to the pre-015.018 state for this code path, with the addition of two replacement audit comments documenting **why** the overlay was rejected and **what** the Boss拍 core spec is.

**Final verdict: PASS — Standards axis clean. No remediation commit required. The S1 typo fix is recommended but optional.**

---

## Files created or modified by this review

- Created: `.scratch/reviews/015.021-splitter-overlay-revert-standards.md` (this report).
- Modified: none.