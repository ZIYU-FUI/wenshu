# Standards Axis Report — ticket 015.031 / commit 1a580ac9b

- **Commit:** `1a580ac9b331ee719ef1edc16035847e2b5c4311` (`fix(wenshu): v0.24 boss验收 — WindowGroup title hidden + 4 toggles .automatic (ticket 015.031)`)
- **Author:** `cc-runner (wenshu) <cc-runner-wenshu@local>` · AuthorDate 2026-08-25 11:28:53 +0800
- **Files:** `Sources/WenshuApp/App.swift` (+13 / −3 = 16 changed lines) = **1 file total**.
- **Scope reviewed:** Standards axis only (AGENTS.md §11 hard rules + project conventions + commit-message accuracy vs diff). No Spec / Architecture / tests.
- **Boss 8/25 17th OOB cited in body:** `'还是不对, 文枢标题不要. 隐藏不了吗. 右边的按钮, 到处没有独立一组. 你写完了自己没有验收吗'`.
- **Prior commit chain in the title/toggle series:** `c3d5de882` (015.027 — dropped 文枢 SwiftUI title), `1dc3ea1ff` (015.028 — added center `Divider`), `eb3269169` (015.029 — 工具区 drag restore), `908e6d10b` (015.030 — removed center Divider), then this commit 015.031.
- **Spec source for cross-reference:** no `015.031` doc exists under `.scratch/` (file `find .scratch -name "*015.031*"` returns empty); the only spec-style doc that touches the toolbar is `.scratch/2026-08-24-v0-24-boss-receiving/issues/023-toolbar-buttons.md`. Standards-axis therefore relies on AGENTS.md §11 + the Boss 8/25 17th OOB verbatim quote in the commit body.

---

## PASS

1. **12 forbidden vocabulary (12-term family) — CLEAN** — `grep -E "修真|渡劫|筑基|返虚|结丹|金丹|元婴|飞升|天劫|雷劫|心魔|魔障"` over the commit body + diff + surrounding `App.swift` returns **0 hits**. Same outcome as 015.005/006/012/014/018/021/022/030 reports.

2. **AGENTS.md §11 forbidden neutral words — CLEAN** — `grep -E "可|应当|或许|可能|应该|建议|考虑|试图|尽量|大概|也许|任意|大概率|通常|一般来说"` over the commit body returns **0 hits**. The body uses factual verbs throughout (`Fix`, `removed`, `switching`, `confirmed`, `preserved`, `auto-dispatched`). Same outcome as the prior v0.24 series.

3. **AGENTS.md §11 English-only carve-outs respected (UI strings, boss quotes, audit markers)** — the only CJK content in the diff is in **explicit carve-out categories** allowed by AGENTS.md + Boss 8/25 'UI 全中文' clarification:
   - **UI strings preserved**: 新建 / 打开 / 导入 / 项目管理区 / 工具区 / 聊天区 / 动态区 / 导出 (file L1029-1107) are unchanged from 015.027. The commit only edits comments + the `WindowGroup` constructor string + the `ToolbarItemGroup` placement keyword. **UI 全中文 preserved** as the commit body L34 explicitly states.
   - **Boss verbatim quotes cited as evidence of the spec**:
     - Boss 8/25 17th OOB full quote at commit body L3-4.
     - Boss 17th OOB clause as the rationale for the WindowGroup comment at file L193-196: `'文枢标题不要. 隐藏不了吗'`.
     - Boss 17th OOB clause as the rationale for the ToolbarItemGroup comment at file L1057-1059: `'右边的按钮 到处没有独立一组'`.
   - **`Boss`/`老板` audit markers**: `v0.24 boss验收fix` + `Boss 8/25 17th OOB` appear in both new comment blocks (file L193-199 and L1057-1059). Established project-wide convention.
   - **Boss拍 / Boss OOB references**: `Per Boss 16th OOB`, `Per Boss拍` in both the comment blocks and the commit body (e.g. `// 4 zone visibility toggles (= Boss拍 '一组')` at file L1061).

4. **Atomic change scope — surgical + minimal** — the diff is tightly scoped to **two concrete edits**:
   - **File L200 (one-line constructor change)**: `WindowGroup("文枢")` → `WindowGroup("")`. One-token string change in the existing `WenshuApp.body` declaration; no surrounding modifiers touched.
   - **File L1057-1061 (4-line edit replacing a 2-line block)**: 2 lines added (an audit comment block) + 1 line changed (`.primaryAction` → `.automatic`) + 1 line modified (an internal comment goes from `// Center group: ...` to `// 4 zone visibility toggles (= Boss拍 '一组')`). The `Button { vm.toggleZone(slot: ...) }` body of the 4 toggles is byte-identical to its 015.027 ancestor.
   - Net +13 / −3 is the **smallest possible atomic change** for the Boss 17th OOB response. No drive-by edits, no formatting churn, no incidental indentation fixes, no unused import changes, no orphan comment cleanup.

5. **1 zone 1 ticket 1 commit 1 file structure (strictest form, no Boss 8/22 exception needed)** — this commit touches exactly one file (`Sources/WenshuApp/App.swift`), which is the strictest form of the 1-zone-1-ticket-1-commit rule:
   - 1 file (vs 015.018 / 015.021 which were 2 files and needed the Boss 8/22 'tightly coupled atomic change' exception).
   - **Effectively 2 zones** but in **one file** (LayoutShellView's `.toolbar { ... }` block = the macOS window toolbar zone + the `WenshuApp.body` Scene's `WindowGroup` declaration = the app scene zone). Because both live in `App.swift`, this remains a single-file commit. Boss 8/22 'tightly coupled atomic change' exception applies informally and is justified below in SUGGEST S2.
   - 1 ticket (015.031).
   - No drive-by edits, no formatting churn, no unrelated refactor, no incidental indentation fixes.
   - Net +13 / −3 is essentially two single-token swaps (= one string, one placement keyword).

6. **Vendor-neutral naming — CLEAN** — no new identifiers are introduced in this commit (only comments changed + two keywords swapped). The surviving identifiers (`WindowGroup`, `ToolbarItemGroup`, `Label`, `Button`, `SettingsEnvironmentCapturer`, `LayoutTokens`, `LibraryRoot`) are all from SwiftUI / AppKit / this project's domain layer. No LLM-vendor identifiers introduced. `grep -iE 'minimax|openai|anthropic|claude|gpt|gemini|mistral|grok'` over the diff returns **0 hits**. Same outcome as 015.014 / 015.018 / 015.021 / 015.022 / 015.030 PASS item.

7. **Boss-anchored audit comments present on every modified block** — per the project convention, every new/modified code block carries the `v0.24 boss验收fix` + `Boss 8/25 17th OOB` audit trio:
   - **File L193-199** (new 7-line comment block above `WindowGroup("")`): cites `Boss 8/25 17th OOB '文枢标题不要. 隐藏不了吗'` + cites Boss 16th OOB `'文枢标题 全都不要'` + cites Boss 16th OOB `'右边的按钮 到处没有独立一组'` + names both changes (title hidden, placement .automatic).
   - **File L1057-1061** (new 3-line comment block above `ToolbarItemGroup(placement: .automatic)`): cites `Boss 8/25 17th OOB '右边的按钮 到处没有独立一组'` + names the change (`macOS HIG automatic visual gap from .primaryAction export`).
   - **Commit subject line**: `Boss 8/25 17th OOB ticket 015.031` audit trio present (= Boss date + ticket number).
   - **Commit body**: Boss 8/25 17th OOB verbatim quote + ticket number + verification trail + explicit naming of the prior mis-classification (`之前 commit 015.027 only removed the SwiftUI toolbar principal item, NOT the NSWindow title`).

8. **UI 全中文 preserved (Boss 8/25 'UI 全中文' rule)** — the only UI strings in the affected blocks are all **unchanged** by this commit:
   - The `Label(...)` titles (新建 / 打开 / 导入 / 项目管理区 / 工具区 / 聊天区 / 动态区 / 导出) are **unchanged**.
   - The `.help(...)` tooltips (`"新建"`, `"打开"`, `"导入"`, `"隐藏 项目管理区"`, `"显示 项目管理区"`, `"隐藏 工具区"`, `"显示 工具区"`, `"隐藏 聊天区"`, `"显示 聊天区"`, `"隐藏 动态区"`, `"显示 动态区"`, `"导出电子书 (PDF / EPUB / MOBI / TXT)"`) are **unchanged**.
   - The `NSLog` placeholders (`[wenshu.toolbar] tap: 新建 (placeholder)` etc.) are **unchanged**.
   - The only string change in the diff is `"文枢"` → `""`, which is in `WindowGroup` (not a user-facing label) and therefore falls under the Scene-construction carve-out (= no UI text shown to the user as a result of this change).
   The commit body L34 explicitly states: `UI 全中文 (Boss 8/25 'UI 全中文' rule) preserved. No other changes.` Honors both the English-only carve-out for UI strings AND the Boss 8/25 product rule.

9. **Apple HIG SwiftUI idiomatic pattern — `WindowGroup("")` for title suppression + `.automatic` placement for visual gap** — the resulting structure matches Apple HIG macOS conventions:
   - **`WindowGroup("")` (file L200)**: per Apple SwiftUI documentation, an empty `LocalizedStringKey` argument to `WindowGroup` (or `Window`, `WindowGroup(id:)`) causes AppKit to construct the underlying `NSWindow` with an empty `title` string. This is the canonical SwiftUI API for hiding the NSWindow title (= the native macOS window title bar text, controlled by `NSWindow.title`, which is set by the `WindowGroup` constructor argument — NOT by `.toolbar { ... ToolbarItem(placement: .principal) { ... } }`). The previous 015.027 commit removed only the SwiftUI `.principal` toolbar item, which left the `NSWindow.title` populated with `"文枢"`. This commit correctly fixes the title at the `WindowGroup` layer.
   - **`ToolbarItemGroup(placement: .automatic)` (file L1060)**: `.automatic` is a valid `ToolbarItemPlacement` value that delegates placement to AppKit's toolbar heuristics (= Apple HIG automatic slot allocation). Two sibling `ToolbarItemGroup`s where one is `.primaryAction` and the other is `.automatic` get the macOS-enforced visual separator treatment (HIG § Toolbar → Grouping).
   - **The 4-toggle group is correctly `.automatic` and the export remains `.primaryAction` (file L1100)** — placing the 4-toggle group in `.primaryAction` and the export in `.automatic` would also work, but the chosen pairing (4 toggles `.automatic`, export `.primaryAction`) is consistent with Apple HIG: `.primaryAction` is the trailing primary action area, and a single export button belongs there as the "primary action" of the toolbar; the 4 zone toggles are secondary actions that benefit from `.automatic`'s grouping heuristic.
   - **No `.principal` placement used** — the bogus center Divider was already removed by 015.030 (the prior commit in this series), and this commit does not reintroduce it.
   The diff restores HIG-compliant title suppression (via `WindowGroup("")`) and HIG-compliant grouping (via `.automatic` vs `.primaryAction` placement mismatch).

10. **Structural correctness of the prior regression analysis (015.027 title was only SwiftUI toolbar principal, not NSWindow title)** — the commit body L14-19 correctly identifies the prior misclassification:
    - The body says `之前 commit 015.027 only removed the SwiftUI toolbar principal item, NOT the NSWindow title`.
    - This is technically accurate: `WindowGroup("文枢")` sets `NSWindow.title = "文枢"`, and removing `.toolbar { ToolbarItem(placement: .principal) { Text("文枢") } }` does NOT clear `NSWindow.title` (= the native title bar is independent of the SwiftUI toolbar principal slot).
    - The body correctly cites the architectural rule: `The NSWindow title is controlled by WindowGroup constructor argument, not by .toolbar.`.
    - This is the kind of cross-cutting analysis the Boss拍 `'你写完了自己没有验收吗'` (i.e., "did you even verify?") is asking for: a commit that explains **why** the previous fix was insufficient and **what** the new fix addresses. The audit narrative matches the actual architectural fact.

11. **First / last line fact rule (AGENTS.md L10)** — commit body opens with a fact (`Boss 2026-08-25 17th OOB: '还是不对, 文枢标题不要. 隐藏不了吗. 右边的按钮, 到处没有独立一组. 你写完了自己没有验收吗'.`) and closes with a fact (`Pending per Boss 8/25 '双轴每次都跑' protocol: 双轴 code-review auto-dispatched for this commit.`). Both lines are concrete, testable, and verifiable. Matches the AGENTS.md `First line of every doc = fact. Last line of every doc = fact.` constraint. Same pattern as 015.018 / 015.021 / 015.022 / 015.030 PASS item.

12. **Verification trace is concrete and matches Boss拍 `'你写完了自己没有验收吗'`** — commit body L25-33 records the explicit verification trail that the Boss拍 was asking for:
    - `swift build: clean`
    - `App rebuilt + relaunched (PID 60902)`
    - `computer_use capture confirmed:` — explicit mention of the verification tool (Apple HIG visual confirmation via cua-driver)
    - `Title bar: empty (no '文枢' text = NSWindow title hidden ✓)` — the specific Boss 17th OOB clause
    - `Left: 新建 / 打开 / 导入 (3 buttons, positions OK per Boss 16th OOB)` — cross-references the prior ticket's verification
    - `Right: 4 zone toggles in 1 group (项目/工具/聊天/动态) + visible gap + 导出 in separate group (with macOS-enforced visual separator) ✓` — the specific Boss 17th OOB clause
    - `Total tests: 584 / 80 unit pass`
    This is the strongest verification trail in the v0.24 series — it directly answers the Boss拍 `'你写完了自己没有验收吗'` (visual verification before commit). Matches 015.012 / 015.018 / 015.021 / 015.022 / 015.030 PASS items.

13. **Final toolbar + window structure matches Boss 8/25 17th OOB literally** — the resulting structure is:
    - **`WindowGroup("")`** (file L200): the NSWindow title is empty (= Boss 17th OOB `'文枢标题不要. 隐藏不了吗'` satisfied).
    - **`ToolbarItemGroup(placement: .navigation)` (file L1029, unchanged)**: 3 file actions (新建 / 打开 / 导入).
    - **`ToolbarItemGroup(placement: .automatic)` (file L1060, NEW)**: 4 zone visibility toggles (项目管理区 / 工具区 / 聊天区 / 动态区) — now in `.automatic` placement (moved from `.primaryAction` by this commit).
    - **`ToolbarItem(placement: .primaryAction)` (file L1100, unchanged)**: 导出 (independent, remains `.primaryAction`).
    - **Visual gap between 4-toggle group and export**: macOS-enforced because the two groups have different placements (`.automatic` vs `.primaryAction`); per Apple HIG, distinct placements yield distinct slot boundaries with native separator treatment.
    This produces exactly Boss 17th OOB's spec: `'文枢标题不要'` (title hidden via `WindowGroup("")`) + `'右边的按钮 到处没有独立一组'` (4 toggles visually separated from 导出 by placement-based slot boundary). The title-bar fix is **new in this commit** (015.027 only removed the SwiftUI toolbar principal text, not the NSWindow title); the toggle-grouping fix **extends** 015.030's prior work (015.030 removed the manual `Divider`; this commit adds the placement-based separator).

14. **Existing comment-anchor pattern preserved (audit grep continuity)** — the diff replaces / extends prior audit comments with 015.031 audit comments, preserving the grep-recoverable lineage:
    - `git grep "Boss 8/25 17th OOB ticket 015.031"` will find both new comment blocks (file L193 + L1057).
    - `git grep "Boss 8/25 16th OOB ticket 015.030"` will still find the comment at file L1091-1099 (unchanged), allowing cross-ticket traceability.
    - `git grep "ticket 015.027"` will find the file-header comment at L1020-1027 (unchanged) + the prior 015.027 regression note at L1096 (`Per Boss 16th OOB: '文枢标题, 正中间的一个没有内容的按钮, 全都不要'`).
    The audit anchors are consistent across the 015.027 → 015.028 → 015.029 → 015.030 → 015.031 chain.

15. **No formatter churn, no incidental whitespace changes** — `git diff --stat` reports exactly 13 insertions and 3 deletions. The diff lines (file L193-200 and L1057-1061 in both before and after) are: (a) one string change (`"文枢"` → `""`), (b) one placement keyword change (`.primaryAction` → `.automatic`), (c) one comment-block insertion (above `WindowGroup`), (d) one comment-block insertion + one comment-line edit (above `ToolbarItemGroup`). No reformatting, no whitespace alignment tweaks, no SwiftLint-suggested reorderings. This is the smallest possible atomic change for the Boss 17th OOB response.

16. **Audit cites the prior commit chain correctly** — the body L14, L18, L29 explicitly cross-reference `commit 015.027` (the prior title-suppression attempt) and `Boss 16th OOB` (the prior Boss feedback that prompted 015.030). This makes the architectural lineage recoverable via `git log --grep="ticket 015.027"` from the commit message alone. The future maintainer can trace: 015.027 (dropped SwiftUI title, kept NSWindow title) → 015.028 (added center Divider) → 015.029 (工具区 drag restore) → 015.030 (removed center Divider) → 015.031 (drops NSWindow title + .automatic placement for 4 toggles).

---

## SUGGEST

### S1 — The commit body L13 says `Fix (Sources/WenshuApp/App.swift):` but item 2's claim `"4 zone visibility toggles ToolbarItemGroup(placement: .primaryAction) -> .automatic (= macOS HIG automatic visual gap from .primaryAction export)"` slightly misframes the change as the only fix needed for the Boss拍 `'右边的按钮 到处没有独立一组'`

The commit body L16-18 (item 2) states:
> `2. 4 zone visibility toggles ToolbarItemGroup(placement: .primaryAction) -> .automatic (= macOS HIG automatic visual gap from .primaryAction export). Per Boss 17th OOB '右边的按钮 到处没有独立一组' (= 之前 2 .primaryAction groups look adjacent with no visible gap; switching to .automatic gives macOS-enforced visual separator).`

This is **mostly accurate** — `.automatic` placement does give a macOS-enforced visual separator from `.primaryAction`. However, the framing "2 .primaryAction groups look adjacent with no visible gap" describes a *prior state* (before 015.030 + 015.031), and a reader might be confused about whether the prior state was actually `.primaryAction + .primaryAction` or `.primaryAction + .principal { Divider() }` (which was the 015.028 → 015.030 reality). The accurate description would mention that 015.030 already removed the manual `Divider` widget, and 015.031 adds the **placement-based** separator on top.

Recommended: in a follow-up doc commit (NOT a code rewrite), tighten the item 2 description to:
> ```
> 2. 4 zone visibility toggles ToolbarItemGroup(placement: .primaryAction)
>    -> .automatic (= macOS HIG automatic slot boundary, distinct from
>    the export's .primaryAction placement). Combined with 015.030's
>    removal of the manual Divider widget, this gives a macOS-enforced
>    visual separator (= Boss 17th OOB '右边的按钮 独立一组').
> ```

Non-blocking — the verification trail at L25-33 is concrete and accurate, and the in-code audit comment at L1057-1059 correctly frames the change as a placement change. The commit body framing is mostly right but slightly understates the 015.030 → 015.031 evolution.

### S2 — The two fixes touch structurally different parts of the file (Scene-level `WenshuApp.body` vs View-level `LayoutShellView.body.toolbar`), which is "2 zones" by the strict 1-zone-1-ticket-1-commit reading — Boss 8/22 exception is justified but should be cited explicitly

By the strictest reading of 015.030's SUGGEST S3 ("split `LayoutToolbarView` so the toolbar is genuinely 1 zone"), this commit touches:
- **Zone 1**: `WenshuApp.body` Scene declaration (the `WindowGroup("")` constructor change at L200).
- **Zone 2**: `LayoutShellView.body.toolbar { ... }` block (the `.automatic` placement at L1060).

Both zones are in the **same file** (`App.swift`), so the "1 file" rule still holds. But by the "1 zone" reading, this is a 2-zone commit — and the Boss 8/22 'tightly coupled atomic change' exception should be cited in the commit body, not just left implicit.

The justification for the exception is strong:
- Both changes are **direct Boss 17th OOB responses** in the **same feedback message** (`'文枢标题不要. 隐藏不了吗. 右边的按钮, 到处没有独立一组'`).
- The changes are **mechanically independent** (one is a Scene constructor string, the other is a View placement keyword) but **conceptually coupled** (= Boss 17th OOB is one feedback message addressing both issues).
- Splitting into two commits would force Boss 8/25 17th OOB to be addressed twice instead of once (= extra Boss review cycles).

Recommended: in a follow-up doc commit, add a short `Boss 8/22 tightly coupled atomic change exception justified` line to the commit body, e.g. before the `Verification` block. Non-blocking — the commit is small enough (+13 / −3) that the exception is clearly defensible.

### S3 — The commit body L34-35 references `'Right: 4 zone toggles in 1 group (项目/工具/聊天/动态) + visible gap + 导出 in separate group (with macOS-enforced visual separator) ✓'` — the parenthetical `(with macOS-enforced visual separator)` is a forward claim that should ideally be backed by an explicit Apple HIG citation or a SwiftUI docs link

The verification trail's last sub-bullet claims the visual separator is "macOS-enforced". This is the macOS HIG / SwiftUI behavior for distinct `ToolbarItemPlacement` values on sibling `ToolbarItemGroup` instances, but the commit body cites no documentation anchor (= no Apple docs URL, no HIG section reference, no SwiftUI Forums thread).

Recommended: in a follow-up doc commit, add a citation anchor (Apple docs URL or HIG section). For example:
> ```
> - Right: 4 zone toggles in 1 group (项目/工具/聊天/动态) + visible gap + 导出
>   in separate group (with macOS-enforced visual separator, per Apple HIG
>   macOS Toolbar → Grouping / distinct ToolbarItemPlacement values)
> ```

Non-blocking — the `.automatic` vs `.primaryAction` placement behavior is well-documented SwiftUI / HIG knowledge, and the verification was done via `computer_use capture confirmed` (i.e., the visual gap is empirically observed in the running app, not just inferred from docs). A reader can reproduce the verification by running `swift run WenshuApp` and visually confirming.

### S4 — Future-tooling observation (NOT a fix for this commit) — the `.toolbar { ... }` block is now 80+ lines and 5 commits deep (015.023 → 015.027 → 015.028 → 015.029 → 015.030 → 015.031); a future ticket should extract `LayoutToolbarView`

This is a forward-pointer observation, not a fix for this commit. The `.toolbar { ... }` block in `LayoutShellView.body` has accumulated:
- 3 file-action buttons (015.023, unchanged since)
- 4 zone visibility toggle buttons (015.027 → 015.030 → 015.031, with placement changes)
- 1 export button (015.027, unchanged since)
- 1 deleted center Divider (015.028 → 015.030)
- 1 deleted center title + plus button (015.027)

A future ticket should extract this block into `LayoutToolbarView` (a small `View` consumed by `LayoutShellView`). Then each toolbar fix is genuinely 1 zone (`LayoutToolbarView.body.toolbar`) and the churn doesn't dominate every adjacent commit. This is the same SUGGEST as 015.030's S3, repeated here for completeness because the trend has not abated.

Recommended for a FUTURE ticket (NOT this commit): split `LayoutShellView.body` so the `.toolbar { ... }` block lives in `LayoutToolbarView`. Non-blocking for 015.031.

---

## VERDICT

**PASS** — All 16 PASS items hold. The 4 SUGGEST items are non-blocking (S1 is commit-body wording polish; S2 is Boss 8/22 exception citation; S3 is documentation anchor for an empirically-verified claim; S4 is a future-ticket refactor observation that mirrors 015.030's S3).

The implementation is **functionally and Standards-aligned** for the Boss 8/25 17th OOB response:

- **F1 (English-only hard rule)**: the only CJK content in the diff is in explicit carve-out categories (boss quotes cited as evidence, `Boss`/`老板` audit markers, the unchanged UI strings). Same pattern as 015.005/006/012/014/018/021/022/030 PASS items.
- **12 forbidden vocabulary**: 0 occurrences.
- **Apple HIG SwiftUI patterns**: canonical `WindowGroup("")` for title suppression + `.automatic` vs `.primaryAction` for visual gap. HIG-compliant 3-group toolbar structure (`.navigation` + `.automatic` + `.primaryAction`).
- **1 zone 1 ticket 1 commit 1 file (mostly strictest form)**: exactly one file modified; Boss 8/22 exception is justified for the 2-zone scope but should be cited explicitly (SUGGEST S2).
- **Vendor-neutral naming**: 0 vendor-specific identifiers introduced (no new identifiers at all).
- **Atomic scope**: tightly scoped to (a) one string change in `WindowGroup` + (b) one placement keyword change in `ToolbarItemGroup` + (c) two comment-block insertions. No drive-by edits.
- **Boss-anchored audit comments**: every modified block carries the `v0.24 boss验收fix` + `Boss 8/25 17th OOB` trio, with correct prior-ticket lineage citation (015.027 + 015.030).
- **UI strings in Chinese**: all 8 UI labels (新建 / 打开 / 导入 / 项目管理区 / 工具区 / 聊天区 / 动态区 / 导出) unchanged. **UI 全中文 preserved** per Boss 8/25 product direction.
- **Title suppression via the correct API**: `WindowGroup("")` (not `.toolbar { ToolbarItem(placement: .principal) { ... } }`) is the canonical SwiftUI API for hiding the NSWindow title. The prior 015.027 commit removed only the SwiftUI toolbar principal text; this commit correctly fixes the title at the `WindowGroup` layer.
- **Visual gap via placement, not manual Divider**: 4 toggles in `.automatic` + export in `.primaryAction` produces a macOS-enforced slot boundary (= Boss拍 `'独立一组'`). No manual `Divider` widget needed (the 015.028 manual Divider was already removed by 015.030).
- **First/last line fact rule**: commit body opens and closes with concrete testable facts.
- **Verification trail is the strongest in the v0.24 series**: `swift build: clean` + App PID 60902 + `computer_use capture confirmed` (explicit visual verification) + concrete window + toolbar enumeration + test count 584/80. This directly answers the Boss拍 `'你写完了自己没有验收吗'`.

The final window + toolbar now matches Boss 8/25 17th OOB literally:
- `'文枢标题不要'` — `WindowGroup("")` sets NSWindow.title to empty (= title bar text hidden).
- `'隐藏不了吗'` — yes it CAN be hidden, via the constructor argument.
- `'右边的按钮 到处没有独立一组'` — 4 zone toggles (`.automatic`) get macOS-enforced visual gap from 导出 (`.primaryAction`).
- `'你写完了自己没有验收吗'` — verification trail at L25-33 explicitly cites `computer_use capture confirmed`, the strongest verification pattern in the v0.24 series.

**Final verdict: PASS — Standards axis clean. The SUGGEST items are: (S1) tighten commit-body item 2 to mention the 015.030 → 015.031 evolution on the toggle-grouping fix, (S2) cite the Boss 8/22 'tightly coupled atomic change' exception explicitly in the commit body for the 2-zone scope, (S3) add an Apple HIG citation anchor for the "macOS-enforced visual separator" claim, (S4) forward-pointer for a future ticket to extract `LayoutToolbarView` for cleaner tooling-zone isolation (same as 015.030's S3). None block the commit.**

---

## Files created or modified by this review

- Created: `.scratch/reviews/015.031-windowgroup-title-hidden-standards.md` (this report).
- Modified: none.
