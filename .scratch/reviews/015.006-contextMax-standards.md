# Standards Axis Report — ticket 015.006 / commit dc741ceac

- **Commit:** `dc741ceac` (`fix(wenshu): v0.24 boss验收 — contextMax 131072 → 1_000_000 (MiniMax-M3 = 1M context)`)
- **Author:** `cc-runner (wenshu) <cc-runner-wenshu@local>` · 2026-08-25 09:01:47 +0800
- **File:** `Sources/WenshuApp/Views/Chat/ChatView.swift` (1 file, +10 / −1)
- **Scope reviewed:** Standards axis only (AGENTS.md §11 hard rules + project conventions). No Spec / Architecture / tests.

---

## PASS

1. **1-zone-1-ticket-1-commit-1-file** (boss 8/22 protocol) — diff is a single file, 10 insertions / 1 deletion. No unrelated refactor leaked in.
2. **12 forbidden vocab (修真 family)** — `search_files` over the changed file for `修真|渡劫|筑基|返虚|结丹|金丹|元婴|飞升|天劫|雷劫|心魔|魔障` returns 0 hits. None of the historical 修真-typo-for-修正 residue.
3. **Atomic change scope** — only `contextMax: Int = 131072 → 1_000_000` and its preceding comment are touched. No drive-by edits.
4. **Vendor-neutral identifier** — the field name stays `contextMax` (not `minimaxContextMax` / `m3ContextMax` / vendor-coupled). The vendor surfaces only in prose comments and in the model name `MiniMax-M3` which the comment explicitly references as a model id, not as a type or module name. Compliant with the boss 8/22 vendor-naming decouple rule.
5. **Boss-anchored audit comment** (per AGENTS.md / project convention) — the new code comment cites:
   - Source-of-truth URL: `https://www.minimax.io/models/text/m3`
   - Empirical caveat with upstream ref: `hermes-agent issue #37289`
   - Boss OOB date marker: `Boss 8/25 拍` (twice — once with the original 拍 quote, once for the "no API" sub-question)
   - Decision rationale: official value 1M, with vendor-empirical 512K caveat called out as a vendor issue, not a wenshu fix.
   This matches the boss-anchored audit comment pattern (date-stamped + source URL + upstream issue link).
6. **Numeric literal style** — `1_000_000` uses Swift underscore grouping (idiomatic, readable). Matches the `131072` style of the old value (which was raw, but the new value's underscore form is a strict improvement).
7. **Fix verification trace** — commit body records `swift build: clean`, app PID 5518 rebuild/relaunch, UI shows the new cap. Honest verification trail.
8. **Pure numeric value, no library call** — the comment is a TODO-style documentation block, not a runtime reference. No actual call site in the file consumes the comment text.

---

## FAIL

### F1 — AGENTS.md §11 English-only hard rule VIOLATED (commit body + new code comment)

`AGENTS.md` line 5–6 is an unconditional, project-wide hard rule: *"This file is English only. No Chinese characters. No CJK punctuation. No mixed CJK + Latin characters. All commit messages, comments, prompts, … follow the same English-only rule."*

The commit and the changed comment break this rule in **60 CJK-containing lines** in the diff (`grep -c "[\u4e00-\u9fff]"` on `git show dc741ceac -- Sources/WenshuApp/Views/Chat/ChatView.swift` → 60).

Concrete violations:

- **Commit body** (commit message lines, full text):
  - Subject: `fix(wenshu): v0.24 boss验收 — contextMax 131072 → 1_000_000 (MiniMax-M3 = 1M context)` — contains `验收`.
  - Body: `Boss 2026-08-25 拍: 'minimax m3 不是 1mb 的上下文吗, 你现在设定的才是 131k'.` — full CJK sentence.
  - Body: `Boss 拍: '没有接口获取的到吗'` — full CJK sentence.
  - Body: `Boss 8/22 拍 1 zone 1 ticket 1 commit` + `此 commit ticket 1 of context window fixes; ticket 2 = UI label 'k → M'` — mixed CJK + Latin.
  - Body footer: `Fix (Sources/WenshuApp/Views/Chat/ChatView.swift):` `- contextMax: Int = 131072 → 1_000_000 (= 128K → 1M)` — the `=` symbol is fine, but the body also has `旧 value 131072 was actually MiniMax-M2 series value (= wrong model).` style parenthetical fragments mixing CJK.
  - `Code-review axes:` body bullet `Standards: Apple HIG + chat LLM API contract (= chat UI shows real context window; mismatch causes user confusion per Boss 8/25 拍)` — trailing `拍` CJK.

- **New code comment** (`ChatView.swift` line 79–87, post-diff):
  - L79: `// v0.24 boss验收fix (Boss 8/25 拍 'minimax m3 不是 1mb 的上下文吗, 你现在设定的才是 131k'):` — heavy CJK.
  - L80–L87: each line uses `Boss 拍 '没有接口获取的到吗' = boss confirmed no API` and other mixed fragments.

This is the dominant Standards violation of the commit. The previously-merging commits in this v0.24 batch (`8f937eab7`, `05c586303`) were titled *English-only cleanup*. The cleanup did NOT cover this commit's body or its new comment.

### F2 — Comment block indentation BROKEN

`od -c` of the new block (`ChatView.swift` lines 79–88, post-diff) shows the lines start at **column 1** (no leading spaces). Surrounding class members are indented 4 spaces (column 5):

```
0000000                   p   u   b   l   i   c       v   a   r       c
0000140    u   r   r   e   n   t   M   o   d   e   l ... (col 5, 4-space indent)
0000140 (end of line)  \n                   /   /       v   0   .   2   4   ...
0000200 (start of L79 — first char '/' at col 1, NO leading spaces)
```

In Swift, a top-level `//` at column 1 is a **file-scope comment**, not a class-member comment. The class `ChatViewModel` opens at L67 with `@Observable public final class ChatViewModel {`, and every property (L72, L76, L77, L78) is indented one level (4 spaces). The new L79–L88 block sits **outside the indentation contract** for class members.

Functionally the Swift compiler is happy (it's still inside the braces), but stylistically the file now has:

```swift
    public var contextUsed: Int = 0
    // v0.24 boss验收fix (...):
// MiniMax M3 context window = 1_000_000 tokens per official docs
// (...)
public var contextMax: Int = 1_000_000
```

where the comment lines and the `public var` declaration are flushed left while the surrounding properties are indented. This is a regression in file readability, and `git blame` will show it as an unindented comment block next to properly-indented neighbors. The diff itself is what introduced the regression (the old line `    public var contextMax: Int = 131072  // ...` was correctly indented).

### F3 — Vendor name (`MiniMax`) in identifier-context comment

Boss 8/22 vendor-naming decouple rule: vendor names should not leak into identifiers or hard-coded strings. The diff keeps the vendor name only inside prose comments, not in code identifiers — so the *identifier* part of the rule passes (see PASS item 4). **But** the new code comment opens with the literal model id `MiniMax-M3` as if it were the canonical answer to "what is the context window", and the commit subject line and body repeatedly use `MiniMax-M3` as the context-window truth source. If a future agent is told to make wenshu vendor-agnostic (per the AGENTS.md §11 "no external AI platform calls" being relaxed via v1 only "minimax cn" — see line 18), these comments will become wrong-but-not-detectable dead text.

The fix is not to strip the audit trail (the comment must keep the audit trail per PASS item 5), but to reword so the model id is a documented *value*, not the *answer*: e.g. `// Default = 1_000_000 tokens (= MiniMax-M3 per https://...; override per selected model when adding multi-provider)`.

This is a borderline item — strictly the rule is about *identifiers*, and identifiers here are clean. Marking as a SUGGEST-level concern; flipping to FAIL if boss treats any vendor string in code comments as a violation.

---

## SUGGEST

1. **Numerical comparison to docs value** — the comment says `1M Context`, but `MiniMax-M3` actual public docs page (cross-checked) states both "1M context window with a guaranteed minimum of 512K tokens" and the API limit is sometimes reported as 200K / 256K output depending on tier. The comment captures the marketed-vs-empirical split correctly (vendor issue, not wenshu). No change recommended, but a future spec-review might want to promote the 512K-cap from "empirical" to "documented worst-case" once we have the bug-report reference.

2. **`App.swift:1386` `private let contextMax: Int = 131072` is dead code** — `search_files` over `App.swift` for `contextMax` shows only the declaration at L1386 and references to `vm.contextMax` (the `ChatViewModel` instance property, the one this commit fixed). The local `private let` on L1386 is never read. Out of scope for *this* ticket (boss 8/22 says 1 commit = 1 ticket = 1 file), but worth filing as ticket 015.007 or 015.008 cleanup.

3. **`compactNumber` formatter** — `App.swift:1345` and `ChatView.swift:484` both produce `"1M"` (not `"1.0M"`) at the 1_000_000 threshold because of the `.replacingOccurrences(of: ".0M", with: "M")` strip. The commit body claims verification showed `"0 /1.0M"`, which is inconsistent with the formatter. Either the commit body is paraphrasing / mistyping the actual rendering (likely), or there's a third formatter elsewhere. Minor — commit-narrative accuracy only.

4. **`Hermes ModelInfoResponse.context_length` mention in old line, gone in new** — the OLD deleted comment referenced `Hermes ModelInfoResponse.context_length 范式` (a hermes cross-reference that was already stale since wenshu is no longer a hermes fork per AGENTS.md L25). Good that it's gone. Not a violation either way; just noting that the comment cleanup is directionally correct on this point.

5. **Date format** — boss date markers in the comment use the form `Boss 8/25 拍` (no year, no leading zero). Matches the project convention seen in surrounding comments (`Boss 8/24 OOB`, `Boss 8/22 拍`). PASS-level, just confirming the date marker style is consistent.

---

## VERDICT

**needs-fix** (Standards axis).

Two real Standards violations in this commit:

1. **F1 (English-only hard rule)** — the entire commit body and the new code comment are written in mixed CJK + Latin, in direct contradiction of AGENTS.md §11. This is the same hard rule that the immediately-preceding commits in this v0.24 batch were cleaning up. The fix is mechanical (translate to English; keep boss quotes inside English narrative as paraphrases or as quoted citations of 老板's original Chinese).
2. **F2 (comment block indentation)** — the new L79–L88 block is flushed left (column 1) while the surrounding class properties are indented 4 spaces. The diff itself broke the indentation when it added the comment header as a single line and the subsequent lines without matching whitespace.

Recommended remediation path (not applied — analysis only):

- Rewrite the new L79–L88 comment in English only, indented 4 spaces to match surrounding class members, keeping the audit trail (URL + Boss date marker + upstream issue ref + decision rationale) intact.
- Rewrite the commit body and subject in English only, keeping the boss-quote citations as `Boss 2026-08-25 said: "..."` style paraphrases (or as `Boss said: <English paraphrase>`), still anchored to the 8/25 OOB date and the `hermes-agent issue #37289` reference.

After F1 + F2 are remediated (likely via a `fix(wenshu): v0.24 boss验收 — re-apply 015.006 contextMax fix with English-only comment + body` follow-up commit per boss 8/22 1-ticket-1-commit-1-file rule), the verdict moves to **accept**.

---

*Report produced by Standards axis subagent of the v0.24 双轴 code-review (per boss 8/22 protocol).*
