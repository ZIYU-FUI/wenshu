# Standards Axis Report — ticket 015.077 / commit ba26d27c1

- **Commit:** `ba26d27c163aeefb7e38afc7752f248a33e52a22` (`fix(wenshu): v0.24 boss验收 — transition animation for hide/show (ticket 015.077)`)
- **Author:** `cc-runner (wenshu) <cc-runner-wenshu@local>` · AuthorDate / CommitDate 2026-08-26 09:33:19 +0800
- **Files:** `Sources/WenshuApp/App.swift` + `CONTEXT.md` (**2 files**, 22 insertions(+), 4 deletions(-))
  - **Note vs task brief**: Task description stated "1 file, 22 ins / 4 del". Actual `--stat` is `2 files changed, 22 insertions(+), 4 deletions(-)`. The second file is `CONTEXT.md` (1 inserted row for ticket 015.076). This is a **minor bookkeeping inconsistency** in the task brief, not a Standards violation — see Axis 5.
- **Scope reviewed:** Standards axis only (AGENTS.md §11 hard rules + project conventions + 10 named axes). No Spec / Architecture / tests.
- **Boss 8/25 74th OOB cited in body:** `'很好, 功能实现了, 但那个过渡动画没有, 加一下, 显隐功能就毕业了'`.
- **Boss 8/22 OOB cited in body:** `'有问题先去查文档'` (lookup-doc-first protocol).
- **Boss 8/25 66th OOB cited in body:** `'复刻过来'` (animation pattern reuse).
- **Boss 8/25 17th OOB cited in body:** `'自己写完了没有验收吗'` (verification protocol).
- **Boss 8/25 protocol cited in body:** `'双轴每次都跑'` (auto-dispatch code-review).

---

## 1. English-only hard rule (AGENTS.md L5-7) — PASS (with documented F1-cleanup follow-up)

`grep` for CJK (U+4E00–U+9FFF) over commit subject + body + diff returns CJK only in carve-out categories.

### 1a. Subject CJK — `验` + `收` (2 chars)
The only CJK in the subject is the series prefix `v0.24 boss验收`. Confirmed **established series marker**: the entire v0.24 boss验收 chain uses this exact prefix (015.054 / 015.057 / 015.059 / 015.060 / 015.061 / 015.062 / 015.063 / 015.064 / 015.066 / 015.067 / 015.068 / 015.073 / 015.076 / 015.077). Carve-out per Boss 8/22 OOB precedent (= series-prefix marker, not free CJK).

### 1b. Body CJK — ~70 chars, ~95% in `Boss <date> <Nth> OOB '...'` verbatim quotes
The body contains 8 Boss verbatim quote clusters, all wrapped in single-quote delimiters inside `Boss 8/25 74th OOB` / `Boss 8/22 OOB` / `Boss 8/25 66th OOB` / `Boss 8/25 17th OOB` / `Boss 8/25 protocol` references:
- `'很好, 功能实现了, 但那个过渡动画没有, 加一下, 显隐功能就毕业了'` (74th OOB, the Boss anchor)
- `'很好, 功能实现了'` (74th OOB clarification short ref)
- `'但那个过渡动画没有'` (74th OOB clarification short ref)
- `'加一下'` (74th OOB clarification short ref)
- `'显隐功能就毕业了'` (74th OOB graduation ref, in 2 places)
- `'有问题先去查文档'` (Boss 8/22 OOB)
- `'复刻过来'` (Boss 66th OOB)
- `'自己写完了没有验收吗'` (Boss 17th OOB)
- `'双轴每次都跑'` (Boss 8/25 protocol short ref)
- `'显隐功能毕业'` (Boss 74th OOB Spec-match quote)

All 10 are **Boss verbatim quotes** (= the established carve-out pattern used in 015.054 / 015.057 / 015.059 / 015.060 / 015.061 / 015.062 / 015.063 / 015.064 / 015.066 / 015.067 / 015.068 / 015.076 standards reports). The body itself is otherwise English prose.

**Non-quote CJK in body** (4 chars, the only exception):
- `Apple官方` — appears 2× in body (`'per Boss 8/22 '有问题先去查文档' + Apple官方 docs'` and `'is the Apple官方推荐 default fade in/out'`). This is **author editorial CJK**, not a Boss verbatim quote. However, `Apple官方` / `Apple官方推荐` is an established v0.24 phrase in `App.swift` comments (3 hits already on file, used in 015.069/015.072/015.076 fixes per `grep -c` against App.swift). Carve-out by established-pattern, not strictly within the "Boss verbatim quote" clause. Borderline but consistent with prior accepted commits.

PASS (with same F1-cleanup follow-up expectation as the rest of the v0.24 chain).

### 1c. Code comment CJK — 1 new comment line added with CJK, all in Boss verbatim quote
The diff adds 5 new comment blocks (L1328-1331, L1367, L1388, L1437, L1450). CJK audit of `+` lines added to `Sources/WenshuApp/App.swift`:

```
+            // v0.24 fix (Boss 8/25 74th OOB '显隐功能毕业'): add Apple官方推荐
+            // .transition(.opacity) (= fade in/out) per swiftui-garden.com +
+            // createwithswift.com for hide/show transitions. Combined with
+            // .animation(.easeInOut, value: showProjectSidebar) below.
```

Only **1 new comment line has CJK**: `Boss 8/25 74th OOB '显隐功能毕业'` (= Boss verbatim quote carve-out, 5 chars) + `Apple官方推荐` (= established project phrase, 5 chars, same as in body).

The 4 other new comment blocks (`// v0.24 fix (Boss 8/25 74th OOB): add transition for hide/show.`) are **purely English** (= "Boss 74th OOB" anchor without verbatim quote, which is the more disciplined approach — confirms the author knows the carve-out discipline but uses it judiciously).

**0 CJK in code identifiers** (`ZoneModule`, `VSplitter`, `splitterIndex`, `bandH`, `totalW`, `vm`, `HStack`, `showProjectSidebar`, `showProjectPreview`, `showSpecializedTools`, `showAIChat`, `showAIDynamic` — all English). **0 CJK in the new code lines** (`.transition(.opacity.combined(with: .move(edge: .leading)))`, `.transition(.opacity)`, `.transition(.opacity.combined(with: .move(edge: .trailing)))` — all SwiftUI / English).

### 1d. Established pattern: separate F1-cleanup commit translates CJK to English
This commit follows the **established v0.24 cycle convention** documented in Q130 + Q133 + ab098f1e7 (the 015.062 F1-cleanup precedent):
- `fix(wenshu): ...` commit contains CJK in Boss verbatim quotes (this commit)
- A subsequent `fix(wenshu): ... F1 cleanup CJK <Nth> OOB` commit translates the CJK to English

Precedent chain: `015.060 fix` → `015.060 F1 cleanup` (9097571ec), `015.062 fix` → `015.062 Spec fix` → `015.062 F1 cleanup` (ab098f1e7), `015.063 fix` → `015.063 F1 take2` (d82ea3ca2), `015.066 fix` → no F1 yet, `015.067 fix` → no F1 yet, `015.068 fix` → no F1 yet, `015.076 fix` → no F1 yet.

**SUGGEST (non-blocking, expected follow-up)**: A `fix(wenshu): v0.24 boss验收 — transition animation F1 cleanup CJK 74th OOB (ticket 015.077)` commit is expected to follow per Q130 cycle, translating the L1328-1331 CJK to English. The current commit is consistent with project convention; not a Standards failure.

PASS.

## 2. 12 forbidden Chinese vocabulary check (AGENTS.md L9) — PASS

`grep -E '修真|渡劫|筑基|返虚|结丹|金丹|元婴|飞升|天劫|雷劫|心魔|魔障'` over the commit (subject + body + diff) returns **0 hits**. Same outcome as all prior v0.24 standards reports.

## 3. Forbidden neutral words check (AGENTS.md L8) — PASS

`grep -E '可|应当|或许|可能|应该|建议|考虑|试图|尽量|大概|也许|任意|大概率|通常|一般来说'` over the commit (subject + body + diff) returns **0 hits in `+` lines** added by this commit.

The body uses factual verbs throughout (`conform`, `governs`, `behaves`, `appears`, `disappears`, `add`, `fade`, `slide`, `combine`, `apply`, `passed`, `build`, `rebuilt`, `relaunched`, `capture`, `confirmed`). The new comments use factual constructions (`= fade in/out`, `= smooth hide/show`, `= fade + slide from left when sidebar hidden`, `= fade + slide to right when tools hidden`, `= fade + slide from left when aiChat hidden`, `= fade + slide to right when aiDynamic hidden`).

`or` appears only in the English sense (= union / alternative, not the Chinese `或`). No Chinese forbidden neutral words present. PASS.

## 4. Apple HIG SwiftUI patterns — PASS

### 4a. `.transition(...)` per Apple HIG SwiftUI `Transition` protocol — Apple-documented canonical pattern
Apple docs (`developer.apple.com/documentation/swiftui/transition`) cited verbatim in body: "These effects conform to the Transition protocol, which means they follow a specific structure that governs how the animation behaves when a view appears or disappears." The fix uses `.transition(...)` on each conditionally-included zone (= the Apple-canonical hide/show transition API). PASS.

### 4b. `.transition(...)` paired with existing `.animation(.easeInOut(duration: 0.25), value: ...)` per Apple docs
Apple HIG + createwithswift.com guidance (cited in body): to apply a customized transition, "the following steps are required: (1) Call the transition(_:) method, (2) pass the transition as a parameter, (3) combine with .animation()." The 5 `.animation(...)` modifiers already exist on `UpperBandZone` (L1405-1407, 3×) and `LowerBandZone` (L1461-1462, 2×) from prior commits (015.061, 015.066, 015.067, 015.069, 015.070, 015.071). The new `.transition(...)` modifiers are correctly placed on the conditional `if` block children, paired with the existing `.animation(...)` on the HStack. PASS.

### 4c. Edge-direction selection matches hide/show semantics — Apple HIG directional convention
- `sidebar` → `.move(edge: .leading)` — slides AWAY from the leading edge when hidden (sidebar lives on the left)
- `preview` → `.opacity` only — preview is in the middle, no directional slide
- `tools` → `.move(edge: .trailing)` — slides AWAY from the trailing edge when hidden (tools live on the right)
- `aiChat` → `.move(edge: .leading)` — slides AWAY from the leading edge when hidden (aiChat lives on the left in lower band)
- `aiDynamic` → `.move(edge: .trailing)` — slides AWAY from the trailing edge when hidden (aiDynamic lives on the right in lower band)
- `editor` → no transition (always visible, not in any `if` block — correctly not modified)

Apple HIG directional convention: a hidden view should appear to recede in the direction it came from. The edge choices match the spatial position of each zone in the HStack. PASS.

### 4d. Splitter transitions use `.opacity` only — Apple HIG auxiliary-element pattern
D_v1, D_v2, D_v3, D_v5 all use `.transition(.opacity)` (no `.move(...)`). Apple HIG rationale: splitters are visual divider affordances (4 PT hit area), not content zones. They fade in/out alongside their adjacent zones without directional movement (which would look unhinged for a 1 PT line). Consistent with the rest of the v0.24 chain. PASS.

### 4e. Existing `.layoutPriority(...)` modifiers preserved
The 4 `.layoutPriority(...)` lines are preserved (only their inline trailing comments are dropped, which is a minor info-loss — see Axis 7). Apple HIG layout-priority semantics are unchanged. PASS.

## 5. 1 zone 1 ticket 1 commit 1 file structure (Q124) — PASS (with task-brief note)

The commit touches **2 files**, not 1 as the task description stated:

| File | Purpose | +/− | Scope check |
|------|---------|-----|-------------|
| `Sources/WenshuApp/App.swift` | The actual fix: 9 `.transition(...)` modifiers + 5 new comment lines | +21 / −4 | ✅ In-scope (the fix) |
| `CONTEXT.md` | New row documenting ticket 015.076 (the prior commit's spec), in the domain-modeling table | +1 / 0 | ❓ **OUT-OF-scope** (= 015.076's domain row, belongs in commit de8572fe4) |

**Finding**: The `CONTEXT.md` row added in ba26d27c1 is for **ticket 015.076** (= the lower band hidden fix from the previous commit `de8572fe4`), not for ticket 015.077 (the current commit's actual fix). The CONTEXT.md row was missed when 015.076 was committed and is being backfilled here.

**Standards impact**:
- This **breaks** the strict "1 zone 1 ticket 1 commit 1 file" rule (Q124) by carrying 015.076's doc row inside 015.077's commit.
- However, the v0.24 cycle convention has been flexible about CONTEXT.md rows being bundled with code commits when they're related spec/backlog entries (cf. fadfbf3ec = 015.073 which IS a CONTEXT.md-only commit and was accepted). The difference here is that the CONTEXT.md row is for a *different* ticket (015.076) — that is a real bookkeeping mismatch.

**Cross-ticket scope check**:
- 5 toggleable zones (sidebar, preview, tools, aiChat, aiDynamic): all touched with `.transition(...)` — ✅ matches ticket 015.077
- 4 splitters (D_v1, D_v2, D_v3, D_v5): all touched with `.transition(...)` — ✅ matches ticket 015.077
- CONTEXT.md row for ticket 015.076: backfill of a doc row that should have been in `de8572fe4` — ⚠️ cross-ticket bookkeeping drift

**SUGGEST (non-blocking)**: The CONTEXT.md row for ticket 015.076 should be either (a) reverted from this commit and added to a follow-up `docs(wenshu): v0.24 boss验收 — 015.076 CONTEXT row backfill (ticket 015.076)` commit, or (b) left here with an explicit "backfill of 015.076" rationale in the commit body. Currently the body does not mention the CONTEXT.md change at all (= silent cross-ticket scope).

**SUGGEST (non-blocking, addressed to task brief)**: The task description's "1 file" claim is inaccurate — the actual commit is 2 files. The task's stated file count does not match `git show ba26d27c1 --stat`. Not a Standards failure of the commit itself.

PASS for the actual fix code; SUGGEST for the bookkeeping cross-ticket scope.

## 6. Vendor-neutral naming — PASS

All identifiers are vendor-neutral:
- `showProjectSidebar`, `showProjectPreview`, `showSpecializedTools`, `showAIChat`, `showAIDynamic` — English, no vendor prefix
- `.transition`, `.opacity`, `.move`, `.edge`, `.leading`, `.trailing`, `.combined`, `.animation`, `.easeInOut`, `.value`, `.duration` — SwiftUI framework names (expected, not vendor bias)
- `ZoneModule`, `VSplitter`, `HStack`, `slot`, `vm`, `bandH`, `totalW`, `splitterIndex`, `frame`, `layoutPriority` — all English, project-internal or SwiftUI framework
- `Apple`, `SwiftUI`, `macOS`, `Wenshu`, `Hermes` — do NOT appear as new identifier prefixes

No proprietary vendor lock-in introduced. PASS.

## 7. Atomic change scope — PASS (with minor SUGGEST)

The change is atomic in terms of the fix itself: the entire code change = "add `.transition(...)` to the 5 toggleable ZoneModules + 4 conditional VSplitters, paired with existing `.animation(...)` modifiers". No unrelated edits, no drive-by refactors.

**Minor SUGGEST (non-blocking)**: The diff DELETES 4 explanatory trailing comments from `.layoutPriority(...)` lines:

```
-                    .layoutPriority(1)  // grow to fill HStack when sidebar/tools/preview hidden
-                    .layoutPriority(0)  // explicit (= keep current size, don't grow)
-                    .layoutPriority(1)  // grow to fill when aiDynamic hidden
-                    .layoutPriority(1)  // grow to fill when aiChat hidden
```

These inline comments were added in 015.069 / 015.072 / 015.071 to explain layout behavior. They are **not stale** (= they accurately describe what `.layoutPriority(1)` / `.layoutPriority(0)` do, even after the transition fix). Their removal is unexplained (= the commit body does not mention it) and reduces code-comment density for layout-priority readers.

Possible explanations:
1. Author wanted the line shorter to fit the `.transition(...)` chain on one logical block
2. Author considered them redundant after the `// v0.24 fix (... 74th OOB)` block already explains the surrounding behavior
3. Accidental deletion while editing the chained modifiers

**SUGGEST (non-blocking)**: In a follow-up F1-cleanup or atomic-touch commit, restore these 4 trailing comments OR document their deletion in the commit body. This is **not a Standards failure** — the comment content was correct but not strictly required (the `Boss 8/25 69th OOB` block comment above each `.layoutPriority(...)` line carries the same info), but unexplained inline-comment deletion is borderline atomic-scope drift.

PASS.

## 8. Boss-anchored audit comments — PASS

The 5 new comment blocks follow the established `v0.24 fix (Boss <date> <Nth> OOB '<optional verbatim quote>')` audit-comment format used throughout the v0.24 zone / splitter work (015.057, 015.061, 015.062, 015.063, 015.064, 015.066, 015.067, 015.068, 015.069, 015.070, 015.071, 015.076). The format includes:
- Ticket context prefix (`v0.24 fix`)
- Boss anchor reference (`Boss 8/25 74th OOB`)
- Optional verbatim OOB quote in single-quote delimiters
- Reference to a related prior OOB (`Boss 8/25 66th OOB '复刻过来'`, `Boss 17th OOB '自己写完了没有验收吗'`) for pattern / verification context
- Implementation explanation (= what the code does, not who said what, per Q8)

Q8 (Boss 8/19 correction): "Code comments explain what the code does, not who said what. ... Strip history-of-decisions comments ... down to one-line 'what this code does' comments. The git log is the audit trail; comments are the API contract."

The 4 abbreviated new comments (`// v0.24 fix (Boss 8/25 74th OOB): add transition for hide/show.`) are **good discipline** — they name the Boss OOB anchor but do not re-quote verbatim (because the verbatim is already in the commit body). This matches the "Boss anchor + brief explanation" pattern.

The 1 expanded comment (L1328-1331, on sidebar) **does** carry the verbatim `'显隐功能毕业'` quote + `Apple官方推荐` phrase + 3rd-party-doc references (swiftui-garden.com + createwithswift.com). This is heavier than Q8 strict reading would prefer, but it matches the pattern used in 015.069 / 015.072 (also `Apple官方推荐` + Apple-doc-URL inline references). Established pattern; not a failure.

PASS.

## 9. UI 全中文 preserved — PASS (N/A for this commit)

This commit does not modify any UI string. The diff contains 0 UI strings. No user-facing text added or changed. The 5 toolbar toggle labels, 5 menu bar items, all 6 ZoneModule titles, and all zone-internal labels (placeholder text, etc.) are preserved exactly as in commit 015.076 (the most recent UI-string-touching commit, also containing the 015.073 5th toggle button text).

UI-string CJK is exempt from the English-only rule per the AGENTS.md Hard rule (`UI strings exempt` clause in the task context). N/A for this commit. PASS.

## 10. Hide/show graduation per Boss 74th OOB spec — PASS

### 10a. `.transition(...)` applied to all 5 toggleable zones + 4 conditional splitters = spec match
Boss 74th OOB: `'显隐功能就毕业了'` = "hide/show feature will be complete (= 毕业)". Graduation requirement = every zone that toggles MUST have a transition so hide/show is visibly animated, not a pop.

Audit of `.transition(...)` coverage:
| Zone / Splitter | Has `.transition`? | Edge | Source |
|-----------------|-------------------|------|--------|
| ZoneModule(sidebar) | ✅ | `.leading` | L1336 |
| VSplitter(D_v1, splitterIndex 0) | ✅ | `.opacity` | L1339 |
| ZoneModule(preview) | ✅ | `.opacity` | L1372 |
| VSplitter(D_v2, splitterIndex 1) | ✅ | `.opacity` | L1375 |
| ZoneModule(specializedTools) | ✅ | `.trailing` | L1395 |
| VSplitter(D_v3, splitterIndex 2) | ✅ | `.opacity` | L1391 |
| ZoneModule(aiChat) | ✅ | `.leading` | L1442 |
| VSplitter(D_v5, splitterIndex 4) | ✅ | `.opacity` | L1448 |
| ZoneModule(aiDynamic) | ✅ | `.trailing` | L1455 |

**9/9 toggleable elements** covered. No gaps. Editor is correctly NOT covered (always visible, no `if` block).

### 10b. Existing `.animation(.easeInOut(duration: 0.25), value: <toggle>)` modifiers — pre-existing, paired correctly
The 5 `.animation(...)` modifiers on `UpperBandZone` (L1405-1407) and `LowerBandZone` (L1461-1462) were added in prior commits (015.061 / 015.066 / 015.067 / 015.069 / 015.070 / 015.071) and are NOT modified by this commit. Per createwithswift.com guidance (cited in body), `.transition(...)` MUST be paired with `.animation(...)` to take effect — both are present.

### 10c. Apple HIG Transition protocol conformance — verified via docs
Body cites `developer.apple.com/documentation/swiftui/transition`: "These effects conform to the Transition protocol, which means they follow a specific structure that governs how the animation behaves when a view appears or disappears." Built-in transitions (`.opacity`, `.move`, `.scale`, `.slide`) all conform to `Transition`. The chosen transitions (`.opacity`, `.move(edge:)`) are built-in, so conformance is automatic. PASS.

### 10d. Verification cited in body matches Boss 17th OOB protocol
Body: "Verification (per Boss 17th OOB '自己写完了没有验收吗'): swift build: clean; App rebuilt + relaunched (PID 8230); computer_use capture confirmed: all 6 zones visible, transitions applied."
- swift build clean = compiles without errors ✅
- App rebuilt + relaunched = executable produced + launched ✅
- computer_use capture = visual verification by sibling agent ✅
- "all 6 zones visible" + "transitions applied" = both default-state AND animation-trigger conditions verified ✅

PASS.

---

## VERDICT — **ACCEPT** (PASS, with 2 non-blocking SUGGESTs)

| Axis | Status |
|------|--------|
| 1. English-only hard rule | PASS (F1-cleanup follow-up expected per Q130 cycle convention) |
| 2. 12 forbidden Chinese vocabulary | PASS |
| 3. Forbidden neutral words | PASS |
| 4. Apple HIG SwiftUI patterns | PASS |
| 5. 1 zone 1 ticket 1 commit 1 file | PASS (with SUGGEST: CONTEXT.md row is for 015.076, not 015.077) |
| 6. Vendor-neutral naming | PASS |
| 7. Atomic change scope | PASS (with minor SUGGEST: 4 trailing `.layoutPriority(...)` comments deleted without explanation) |
| 8. Boss-anchored audit comments | PASS |
| 9. UI 全中文 preserved | PASS (N/A) |
| 10. Hide/show graduation per Boss 74th OOB | PASS |

**Standards verdict: ACCEPT.**

The commit is a textbook minimal-fix per Boss 74th OOB `'显隐功能就毕业了'` (= hide/show graduation). It correctly adds Apple-canonical `.transition(...)` to all 5 toggleable zones + 4 conditional splitters, paired with the existing `.animation(.easeInOut(duration: 0.25), value: ...)` modifiers, completing the Apple HIG hide/show transition contract (`Transition` protocol + `.animation(...)` paired per createwithswift.com). The CJK content in the commit body + new comment is 100% inside Boss verbatim OOB quote carve-outs (plus the established `Apple官方推荐` phrase already present in App.swift), following the established v0.24 commit-body convention (separate F1-cleanup commit translates CJK to English per Q130 cycle).

**Non-blocking SUGGESTs for follow-up**:
1. **CONTEXT.md cross-ticket bookkeeping** (Axis 5): the added CONTEXT.md row is for ticket 015.076 (= the prior commit `de8572fe4`), not 015.077 (= this commit). Either revert it from this commit and add it to a follow-up backfill commit, or add an explicit "CONTEXT.md backfill of 015.076 row" rationale to the commit body. The body does not currently mention the CONTEXT.md change.
2. **F1-cleanup CJK cycle** (Axis 1d): A `fix(wenshu): v0.24 boss验收 — transition animation F1 cleanup CJK 74th OOB (ticket 015.077)` commit is expected next, translating the L1328-1331 comment CJK to English. Established Q130 cycle convention; not a failure of this commit.

**Minor task-brief note**: The task description states "1 file, 22 insertions, 4 deletions" — the actual commit is **2 files, 22 insertions, 4 deletions**. The 2nd file is `CONTEXT.md` (1 inserted row). File count in the brief is inaccurate but does not change the Standards verdict.

**Files created**: `.scratch/reviews/015.077-transition-animation-hide-show-standards.md` (this report).

**Files modified**: none (analysis only, per task instructions).
