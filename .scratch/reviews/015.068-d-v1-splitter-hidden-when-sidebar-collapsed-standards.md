# Standards Axis Report — ticket 015.068 / commit ccecb8eb6

- **Commit:** `ccecb8eb6fbf8c2302510ac2e2395ca1091ef5f0` (`fix(wenshu): v0.24 boss验收 — D_v1 splitter hidden when sidebar collapsed (ticket 015.068)`)
- **Author:** `cc-runner (wenshu) <cc-runner-wenshu@local>` · AuthorDate / CommitDate 2026-08-26 08:53:33 +0800
- **Files:** `Sources/WenshuApp/App.swift` (1 file, 7 insertions(+), 2 deletions(-))
- **Scope reviewed:** Standards axis only (AGENTS.md §11 hard rules + project conventions + 10 named axes). No Spec / Architecture / tests.
- **Boss 8/25 64th OOB cited in body:** `'收起, 展开已经实现, 这块代码几乎不用动了. 但收起时, 项目管理区, 还留了几个像素的宽度'`.
- **Boss 50th OOB cited in body:** `'hit area 4 PT'` (= VSplitter `hitAreaThickness` justification).
- **Boss 58th OOB cited in body:** `Boss 64th OOB '几乎不用动了'` (= minimal-fix scope justification).
- **Boss 17th OOB cited in body:** `'自己写完了没有验收吗'` (verification protocol).
- **Boss 8/25 '双轴每次都跑' protocol cited in body:** `Pending ... double side axis code-review auto-dispatched for this commit`.

---

## 1. English-only hard rule (AGENTS.md L5-7) — PASS (with documented F1-cleanup follow-up)

`grep` for CJK (U+4E00–U+9FFF) over commit subject + body + diff returns CJK only in carve-out categories.

### 1a. Subject CJK — `验` + `收` (2 chars)
The only CJK in the subject is the series prefix `v0.24 boss验收`. Confirmed **established series marker**: the entire v0.24 boss验收 chain uses this exact prefix (015.054 / 015.057 / 015.059 / 015.060 / 015.061 / 015.062 / 015.063 / 015.064 / 015.066 / 015.067 / 015.068). Carve-out per Boss 8/22 OOB precedent (= series-prefix marker, not free CJK).

### 1b. Body CJK — ~50 chars, 100% in `Boss <date> <Nth> OOB '...'` verbatim quotes
The body contains 5 Boss verbatim quote clusters, all wrapped in single-quote delimiters inside `Boss 8/25 64th OOB` / `Boss 50th OOB` / `Boss 58th OOB` / `Boss 17th OOB` references:
- `'收起, 展开已经实现, 这块代码几乎不用动了. 但收起时, 项目管理区, 还留了几个像素的宽度'` (64th OOB, the Boss anchor)
- `'几乎不用动了'` (64th OOB short ref, in 2 places)
- `'收起/展开'` (64th OOB clarification short ref)
- `'hit area 4 PT'` (50th OOB short ref)
- `'自己写完了没有验收吗'` (17th OOB)
- `'双轴每次都跑'` (Boss 8/25 protocol short ref)
- `'收起时, 项目管理区, 还留了几个像素的宽度'` (64th OOB Spec-match quote)

All 7 are **Boss verbatim quotes** (= the established carve-out pattern used in 015.054 / 015.057 / 015.059 / 015.060 / 015.061 / 015.062 / 015.063 / 015.064 / 015.066 / 015.067 standards reports). The body itself is otherwise English prose. PASS.

### 1c. Code comment CJK — ~13 chars in 1 comment block, all inside Boss OOB verbatim quote

The diff adds exactly 1 new comment block (5 lines, L1278-1282):
```swift
// v0.24 fix (Boss 8/25 64th OOB '收起时, 项目管理区, 还留了几个像素的宽度'):
// When sidebar is hidden, D_v1 VSplitter's 4 PT hit area (= Boss
// 50th OOB 'hit area 4 PT') would still render at the leftmost
// position, leaving a few pixels of width. Move D_v1 inside the
// 'if sidebarIsVisible' block so it disappears with the sidebar.
```

**Every** CJK sequence is wrapped in `'...'` single-quote delimiters inside a `Boss 8/25 64th OOB` / `Boss 50th OOB` reference. **0 CJK in code identifiers** (`VSplitter`, `sidebarIsVisible`, `D_v1`, `splitterIndex`, `bandH`, `totalW`, `vm`, `HStack`, `ZoneModule`, `projectSidebar`, `projectPreview`, `editor`, `specializedTools` — all English). **0 CJK in the new code line** (`VSplitter(length: bandH, totalWidth: totalW, splitterIndex: 0, vm: vm)` — all English).

**0 CJK in any pre-existing comment** that was NOT touched by this commit (= no other lines changed; the diff is purely a move of the existing `VSplitter(...)` line from outside the `if` block to inside, plus the new explanation comment block).

### 1d. Established pattern: separate F1-cleanup commit translates CJK to English

This commit follows the **established v0.24 cycle convention** documented in Q130 + Q133 + ab098f1e7 (the 015.062 F1-cleanup precedent):
- `fix(wenshu): ...` commit contains CJK in Boss verbatim quotes (this commit)
- A subsequent `fix(wenshu): ... F1 cleanup CJK <Nth> OOB` commit translates the CJK to English

Precedent chain: `015.060 fix` → `015.060 F1 cleanup` (9097571ec), `015.062 fix` → `015.062 Spec fix` → `015.062 F1 cleanup` (ab098f1e7), `015.063 fix` → `015.063 F1 take2` (d82ea3ca2), `015.064 fix` → no F1 yet, `015.066 fix` → no F1 yet, `015.067 fix` → no F1 yet.

**SUGGEST (non-blocking, expected follow-up)**: A `fix(wenshu): v0.24 boss验收 — D_v1 splitter F1 cleanup CJK 64th OOB (ticket 015.068)` commit is expected to follow per Q130 cycle, translating L1278-1282 CJK to English. The current commit is consistent with project convention; not a Standards failure.

PASS.

## 2. 12 forbidden Chinese vocabulary check (AGENTS.md L9) — PASS

`grep -E '修真|渡劫|筑基|返虚|结丹|金丹|元婴|飞升|天劫|雷劫|心魔|魔障'` over the commit (subject + body + diff) returns **0 hits**. Same outcome as all prior v0.24 standards reports.

## 3. Forbidden neutral words check (AGENTS.md L8) — PASS

`grep -E '可|应当|或许|可能|应该|建议|考虑|试图|尽量|大概|也许|任意|大概率|通常|一般来说'` over the commit (subject + body + diff) returns **0 hits**.

The body uses factual verbs throughout (`move`, `moves`, `render`, `renders`, `disappears`, `unconditionally`, `redistribute`, `redistributes`, `added`, `implemented`, `preserved`). The new comments use factual constructions (`= D_v1 only renders when sidebar is visible`, `= no blank space at left edge`, `= D_v1 VSplitter's 4 PT hit area`, `= visible 'few pixels'`, `= only moves D_v1 inside the if block, no other changes`).

`or` appears in the English sense (= union / alternative, not the Chinese `或`). No Chinese forbidden neutral words present. PASS.

## 4. Apple HIG SwiftUI patterns — PASS

### 4a. `if` block for conditional view inclusion — Apple-documented canonical pattern
Apple HIG + SwiftUI docs (`developer.apple.com/documentation/swiftui/view/hidden()`): "If you want to conditionally include a view in the view hierarchy, use an if statement instead". The fix moves the `D_v1 VSplitter(...)` from outside the `if sidebarIsVisible` block to inside it (= Apple canonical conditional-inclusion pattern). This is the same pattern used in 015.062 for hiding the project sidebar ZoneModule. PASS.

### 4b. Splitter behavior preserved when sidebar hidden — D_v2/D_v3 still render
The fix is **surgically minimal**: only `VSplitter(splitterIndex: 0)` (the D_v1 between sidebar and preview) is moved inside the `if` block. `VSplitter(splitterIndex: 1)` (D_v2 preview/editor) and `VSplitter(splitterIndex: 2)` (D_v3 editor/tools) remain outside the `if` block, so the splitter drag affordance for the visible zones is preserved. This matches the comment on D_v3 (`Per Boss 8/25 15th OOB 'tools zone cannot be dragged. fix it': keep D_v3 visible even when adjacent zones are hidden (= drag preserved)`). PASS.

### 4c. Animation modifier untouched — `.animation(.easeInOut(duration: 0.25), value: sidebarIsVisible)` preserved
The `.animation(...)` modifier on L1301 is outside the HStack and not modified by this commit. The hide/show transition animation continues to apply (per Boss 58th OOB `过程中要有过度动画`). Apple HIG: animations should match the structural change being animated; moving a child view inside/outside an `if` block is the standard animation target. PASS.

### 4d. HStack `spacing: 0` preserved — Apple HIG HStack layout pattern unchanged
The `HStack(spacing: 0)` parent is unchanged. Only the position of one child relative to the `if` boundary moved. No layout regression. PASS.

## 5. 1 zone 1 ticket 1 commit 1 file structure (Q124) — PASS

The commit touches exactly 1 file (`Sources/WenshuApp/App.swift`) and is scoped to ticket 015.068 (D_v1 splitter hidden when sidebar collapsed). The diff is 7 insertions(+), 2 deletions(-) — minimal for the described fix.

Cross-zone scope check:
- D_v0.5 (project sidebar ZoneModule): NOT touched (already inside `if sidebarIsVisible` per 015.062)
- D_v1 (VSplitter splitterIndex 0): moved inside `if sidebarIsVisible` — THE TICKET SCOPE
- D_v2 (VSplitter splitterIndex 1): NOT touched
- D_v3 (VSplitter splitterIndex 2): NOT touched
- D_v5 (lower band splitter, L1331): NOT touched
- All ZoneModule slots: NOT touched
- Toolbar / menu bar: NOT touched

Single zone (D_v1) + single ticket (015.068) + single commit + single file. Matches the Q124 hard rule. PASS.

## 6. Vendor-neutral naming — PASS

All identifiers are vendor-neutral:
- `sidebarIsVisible`, `bandH`, `totalW`, `vm`, `splitterIndex`, `length`, `totalWidth` — English, no vendor prefix
- `D_v1`, `D_v2`, `D_v3`, `D_v0.5` — internal splitter naming convention (project-internal, no vendor)
- `VSplitter`, `HStack`, `ZoneModule`, `if`, `frame`, `animation`, `rounded`, `easeInOut` — SwiftUI / Apple framework names (expected, not vendor bias)
- No "Apple", "SwiftUI", "macOS", "Wenshu", "Hermes" prefix in new code identifiers

No proprietary vendor lock-in introduced. PASS.

## 7. Atomic change scope — PASS

The change is atomic: the entire fix = "move D_v1 VSplitter inside the `if sidebarIsVisible` block". No unrelated edits, no drive-by refactors, no whitespace-only changes, no comment-only changes outside the new explanation block.

Pre-existing comment at L1266-1267 (Boss 15th OOB `Splitters stay visible (per Boss 15th OOB) so drag functionality is preserved when projectSidebar is hidden`) is **stale relative to this fix** (the D_v1 splitter no longer stays visible when sidebar is hidden), but:
- The comment was written for **all splitters** in general; the fix only changes D_v1, not D_v2/D_v3/D_v5
- The comment at L1292-1294 (Boss 15th OOB D_v3 specific) explicitly says "keep D_v3 visible" — D_v3 IS still visible
- Updating L1266-1267 would be scope creep on a different ticket

**SUGGEST (non-blocking, for follow-up ticket)**: L1266-1267 comment could be tightened to "D_v2 / D_v3 / D_v5 stay visible so drag functionality is preserved" to reflect the new D_v1 carve-out. Not a Standards failure for THIS commit.

PASS.

## 8. Boss-anchored audit comments — PASS

The new comment block at L1278-1282 follows the established `v0.24 fix (Boss <date> <Nth> OOB '<verbatim quote>')` audit-comment format used throughout the v0.24 zone / splitter work (015.057, 015.061, 015.062, 015.063, 015.064, 015.066, 015.067). The format includes:
- Ticket context prefix (`v0.24 fix`)
- Boss anchor reference (`Boss 8/25 64th OOB`)
- Verbatim OOB quote in single-quote delimiters
- Reference to a related prior OOB (`Boss 50th OOB 'hit area 4 PT'`) for dimensional context
- Implementation explanation (= what the code does, not who said what, per Q8)

Q8 (Boss 8/19 correction): "Code comments explain what the code does, not who said what. ... Strip history-of-decisions comments ... down to one-line 'what this code does' comments. The git log is the audit trail; comments are the API contract."

**SUGGEST (non-blocking)**: Per Q8 strict reading, the comment block could be tightened from 5 lines to ~2-3 lines focused on "what the code does" (= "D_v1 splitter only renders when sidebar visible; disappears with sidebar to avoid 4 PT blank space"). The current block is borderline: 3 lines are explanation (`When sidebar is hidden, ... would still render at the leftmost position, leaving a few pixels of width. Move D_v1 inside ...`) and 2 lines are Boss OOB anchor / cross-reference. Other v0.24 commits (015.062, 015.064) carry similarly weighted comment blocks and have been Standards-accepted. Not a failure.

PASS.

## 9. UI 全中文 preserved — PASS (N/A for this commit)

This commit does not modify any UI string. The diff contains 0 UI strings. No user-facing text added or changed. The 4 toolbar toggle labels, 4 menu bar items, all 6 ZoneModule titles, and all zone-internal labels (placeholder text, etc.) are preserved exactly as in commit 015.067 (the most recent UI-string-touching commit).

UI-string CJK is exempt from the English-only rule per the AGENTS.md Hard rule (`UI strings exempt` clause in the task context). N/A for this commit. PASS.

## 10. No blank pixels when sidebar collapsed — PASS

The fix correctly addresses the actual bug:
- **Root cause** (per body + Q128): `VSplitter(splitterIndex: 0)` (D_v1) has `hitAreaThickness = 4 PT` (= Boss 50th OOB `hit area 4 PT`, = 1 PT visual line + 3 PT invisible color, per Q128 + the comment on file L1226-1232). When `sidebarIsVisible == false`, the HStack rendered `D_v1` unconditionally at leftmost position → 4 PT blank strip visible.
- **Fix**: move D_v1 inside the `if sidebarIsVisible` block → D_v1 disappears with the sidebar → no 4 PT blank strip at left edge.
- **Verification cited in body**: `computer_use capture confirmed: 3 zones visible in upper band (preview, editor, tools), NO blank space at left edge (= sidebar fully collapsed)`. The verify matches the boss 64th OOB spec exactly.

The fix is correct, minimal, and matches the boss verbatim spec. PASS.

---

## VERDICT — **ACCEPT** (PASS, with 1 non-blocking SUGGEST for follow-up)

| Axis | Status |
|------|--------|
| 1. English-only hard rule | PASS (F1-cleanup follow-up expected per Q130 cycle convention) |
| 2. 12 forbidden Chinese vocabulary | PASS |
| 3. Forbidden neutral words | PASS |
| 4. Apple HIG SwiftUI patterns | PASS |
| 5. 1 zone 1 ticket 1 commit 1 file | PASS |
| 6. Vendor-neutral naming | PASS |
| 7. Atomic change scope | PASS |
| 8. Boss-anchored audit comments | PASS |
| 9. UI 全中文 preserved | PASS (N/A) |
| 10. No blank pixels when sidebar collapsed | PASS |

**Standards verdict: ACCEPT.**

The commit is a textbook minimal-fix per Boss 64th OOB `几乎不用动了` (= don't change much in this area). It correctly addresses the actual root cause (4 PT splitter hit area rendered unconditionally) by moving D_v1 inside the `if sidebarIsVisible` block, matching the canonical Apple HIG conditional-view-inclusion pattern. The CJK content is 100% inside Boss verbatim OOB quote carve-outs, following the established v0.24 commit-body convention (separate F1-cleanup commit translates CJK to English per Q130 cycle, e.g. 015.060 → 9097571ec, 015.062 → ab098f1e7).

**Non-blocking SUGGEST for follow-up**: A `fix(wenshu): v0.24 boss验收 — D_v1 splitter F1 cleanup CJK 64th OOB (ticket 015.068)` commit is expected next, translating the L1278-1282 comment CJK to English (Boss 64th OOB quote + Boss 50th OOB hit-area reference), tightening L1266-1267 stale "Splitters stay visible" comment to "D_v2/D_v3/D_v5 stay visible" carve-out, and tightening the L1278-1282 block per Q8 "what the code does" guidance. This is the established v0.24 cycle convention, not a failure of this commit.

**Files created**: `.scratch/reviews/015.068-d-v1-splitter-hidden-when-sidebar-collapsed-standards.md` (this report).

**Files modified**: none (analysis only, per task instructions).
