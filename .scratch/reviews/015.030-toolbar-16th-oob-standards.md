# Standards Axis Report — ticket 015.030 / commit 908e6d10b

- **Commit:** `908e6d10be3f01225e4d6c61f4decbbd22f9afca` (`fix(wenshu): v0.24 boss验收 — toolbar 4 toggles 1 group + export independent (ticket 015.030)`)
- **Author:** `cc-runner (wenshu) <cc-runner-wenshu@local>` · AuthorDate 2026-08-25 11:26:25 +0800
- **Files:** `Sources/WenshuApp/App.swift` (+10 / −9 = 19 changed lines) = **1 file total**.
- **Scope reviewed:** Standards axis only (AGENTS.md §11 hard rules + project conventions + commit-message accuracy vs diff). No Spec / Architecture / tests.
- **Boss 8/25 16th OOB cited in body:** `'按钮位置现在对. 分不不对, 右边的四个显隐功能是一组, 导出独立. 文枢标题, 正中间的一个没有内容的按钮, 全都不要'`.
- **Prior commit chain in the toolbar series:** `c3d5de882` (015.027 — established "4 toggles in 1 group + export independent"), `1dc3ea1ff` (015.028 — added center `Divider`), `eb3269169` (015.029 — 工具区 drag restore), then this commit 015.030.

---

## PASS

1. **12 forbidden vocabulary (12-term family) — CLEAN** — `grep -E "修真|渡劫|筑基|返虚|结丹|金丹|元婴|飞升|天劫|雷劫|心魔|魔障"` over the commit body + diff returns **0 hits**. Same outcome as 015.005/006/012/014/018/021/022 reports.

2. **AGENTS.md §11 forbidden neutral words — CLEAN** — `grep -E "可|应当|或许|可能|应该|建议|考虑|试图|尽量|大概|也许|任意|大概率|通常|一般来说"` over the commit body returns **0 hits**. The body uses factual verbs throughout (`Removed`, `Re-grouped`, `preserved`, `matches`, `auto-dispatched`). Same outcome as the prior v0.24 series.

3. **AGENTS.md §11 English-only carve-outs respected (UI strings, boss quotes, audit markers)** — the only CJK content in the diff is in **explicit carve-out categories** allowed by AGENTS.md + Boss 8/25 'UI 全中文' clarification:
   - **UI strings preserved**: 新建 / 打开 / 导入 / 项目管理区 / 工具区 / 聊天区 / 动态区 / 导出 (file L1034-1096) are unchanged from 015.027. The commit deletes the center Divider and edits comments only — no UI label changes. **UI 全中文 preserved** as the commit body L34 explicitly states.
   - **Boss verbatim quotes cited as evidence of the spec**:
     - Boss 8/25 16th OOB full quote at commit body L5-9.
     - Boss 16th OOB clause as the rationale for the comment edit at file L1081-1089: `'分得不对, 右边的四个显隐功能是一组, 导出独立'` + `'文枢标题, 正中间的一个没有内容的按钮, 全都不要'`.
   - **`Boss`/`老板` audit markers**: `v0.24 boss验收fix` + `Boss 8/25 16th OOB ticket 015.030` appears in the new comment block at file L1081-1089 (the deleted comment had the equivalent markers for 015.028; the new markers point at the current ticket). Established project-wide convention.
   - **Boss拍 / Boss OOB references**: `Per Boss拍`, `Per Boss 16th OOB` in both the comment block and the commit body (e.g. `+                    // Right group: 导出 (independent per Boss拍)` at file L1091).

4. **Atomic change scope — minimal deletion + comment rewrite** — the diff is tightly scoped to two concrete edits within the existing `LayoutShellView.body.toolbar { ... }` block:
   - **File L1081-1089 (replaced 9-line comment block)**: the 9-line `015.028` comment ("visual separator between 4 zone toggles and export…") is replaced by a 9-line `015.030` comment describing the corrected grouping + center-Divider removal.
   - **File L1090-1093 (replaced `ToolbarItem(placement: .principal) { Divider() }` block)**: 3 lines removed (the wrapper + its `Divider()` content), eliminating the bogus center button-feel element per Boss 16th OOB '正中间的一个没有内容的按钮 全都不要'.
   - **File L1091 (one-line comment change)**: `// Right group (separate): 导出` → `// Right group: 导出 (independent per Boss拍)`. Comment-only update, preserves the existing `ToolbarItem(placement: .primaryAction) { ... Button ... vm.exportEbook ... }` body unchanged.
   - Net result is a **clean revert of the center Divider** that 015.028 had just added 18 minutes earlier. No drive-by edits, no formatting churn, no incidental indentation fixes.

5. **1 zone 1 ticket 1 commit 1 file structure (strictest form, no Boss 8/22 exception needed)** — this commit touches exactly one file (`Sources/WenshuApp/App.swift`), which is the strictest form of the 1-zone-1-ticket-1-commit rule:
   - 1 file (vs 015.018 / 015.021 which were 2 files and needed the Boss 8/22 'tightly coupled atomic change' exception).
   - 1 zone (LayoutShellView's `.toolbar { ... }` block = the macOS window toolbar zone).
   - 1 ticket (015.030).
   - No drive-by edits, no formatting churn, no unrelated refactor, no incidental indentation fixes.
   - No Boss 8/22 exception needed (this is the ideal 1-file case the rule expects).
   - Net +1 line (10 insertions / 9 deletions) is essentially a swap (= delete comment + Divider, write new comment).

6. **Vendor-neutral naming — CLEAN** — no new identifiers are introduced in this commit (only comments changed + Divider removed). The surviving identifiers (`ToolbarItemGroup`, `ToolbarItem`, `Divider`, `Label`, `Button`) are all from SwiftUI / AppKit system frameworks. No LLM-vendor identifiers introduced. `grep -iE 'minimax|openai|anthropic|claude|gpt|gemini|mistral|grok'` over the diff returns **0 hits**. Same outcome as 015.014 / 015.018 / 015.021 / 015.022 PASS item.

7. **Boss-anchored audit comments present on the modified block** — per the project convention, every new/modified code block carries the `v0.24 boss验收fix` + `Boss 8/25 16th OOB ticket 015.030` audit trio:
   - **File L1081-1089** (replaced comment block above the export `ToolbarItem`): cites `Boss 8/25 16th OOB ticket 015.030` + cites Boss拍 `'分得不对, 右边的四个显隐功能是一组, 导出独立'` + cites `Per Boss 16th OOB: '文枢标题, 正中间的一个没有内容的按钮, 全都不要'` + names the change (center Divider removed, export now independent).
   - **File L1091** (one-line comment inside the export `ToolbarItem`): cites `独立 per Boss拍`. Anchored to the current ticket via the preceding block comment.
   - **Commit subject line**: `Boss 8/25 16th OOB ticket 015.030` audit trio present (= Boss date + ticket number).
   - **Commit body**: Boss 8/25 16th OOB verbatim quote + ticket number + verification trail.

8. **UI 全中文 preserved (Boss 8/25 'UI 全中文' rule)** — the only UI strings in the affected toolbar block (新建 / 打开 / 导入 / 项目管理区 / 工具区 / 聊天区 / 动态区 / 导出) are all **unchanged** by this commit:
   - The `NSLog` placeholders (e.g. `[wenshu.toolbar] tap: 新建 (placeholder)`) are **unchanged** in this commit (the file before-and-after diff shows zero differences in the L1030-1095 body of the buttons).
   - The `.help(...)` tooltips (`"新建"`, `"打开"`, `"导入"`, `"隐藏 项目管理区"`, etc.) are **unchanged** in this commit (the diff only touches the export region).
   - The `Label(...)` titles (`"新建"`, `"打开"`, `"导入"`, `"项目管理区"`, etc.) are **unchanged** in this commit.
   - The only Chinese string in the deletion is `Divider()` (no string content), and the new comment block contains `Boss拍` / `'...'` boss quotes (= explicit carve-out per AGENTS.md §11 + Boss 8/25).
   The commit body L34 explicitly states: `UI 全中文 (Boss 8/25 'UI 全中文' rule) preserved. No other changes.` Honors both the English-only carve-out for UI strings AND the Boss 8/25 product rule.

9. **Apple HIG SwiftUI idiomatic pattern — `ToolbarItem` + `ToolbarItemGroup` with proper `.primaryAction` placements** — the surviving toolbar structure matches Apple HIG macOS toolbar conventions:
   - **L1029** (`ToolbarItemGroup(placement: .navigation) { ... }`): the canonical placement for the "main" toolbar items. Apple HIG places the leading actions here (= the `nav` slot SwiftUI exposes to the trailing edge of the leading region).
   - **L1050** (`ToolbarItemGroup(placement: .primaryAction) { ... }`): the canonical placement for the trailing primary action area (= Apple's HIG secondary-action pattern). 4 buttons inside is within the recommended cap (Apple HIG: ≤ 5 items per toolbar group).
   - **L1090** (`ToolbarItem(placement: .primaryAction) { ... }`): the canonical placement for a single, standalone toolbar item. Sibling of the previous `ToolbarItemGroup` to place the export adjacent to the toggles but in its own group boundary (= visual separation without a `Divider()` widget).
   - **No `.principal` placement used** — the principal slot was the source of the bogus center Divider; its deletion restores the canonical 2-placement toolbar (`.navigation` + `.primaryAction`). Apple HIG does NOT recommend `.principal` for action buttons (= that's for window titles only).
   The diff literally removes the un-idiomatic `.principal { Divider() }` and replaces it with nothing — restoring HIG-compliant 2-placement toolbar.

10. **Structural cleanup of the prior regression (015.028's center Divider)** — the diff is exactly the corrective surgical removal that Boss 16th OOB calls for:
    - **L1081-1089** (new comment block): explicitly names the prior ticket (`Center Divider (from ticket 015.028) removed (= Boss 16th OOB '正中间的一个没有内容的按钮 全都不要').`). This makes the lineage recoverable via `git blame` / `git log --grep="ticket 015.028"` — the future maintainer can trace: 015.027 (3 groups) → 015.028 (added Divider) → 015.030 (removed Divider). The audit trail is intact.
    - **Deletion of `ToolbarItem(placement: .principal) { Divider() }` (file L1089-1091 in parent)**: 3 lines removed without replacement (= whitespace + wrapper + `Divider()`). Surgical removal, no orphaned imports, no dead code.
    - **One-line comment update at L1091**: `// Right group (separate): 导出` → `// Right group: 导出 (independent per Boss拍)`. Minor wording touch to align with the 015.030 audit narrative.
    This is exactly the "atomic fix to revert a regression" pattern that the project's Boss-anchored protocol expects.

11. **First / last line fact rule (AGENTS.md L10)** — commit body opens with a fact (`Boss 2026-08-25 16th OOB: '按钮位置现在对. 分不不对, 右边的四个显隐功能是一组, 导出独立. 文枢标题, 正中间的一个没有内容的按钮, 全都不要'.`) and closes with a fact (`Pending per Boss 8/25 '双轴每次都跑' protocol: 双轴 code-review auto-dispatched for this commit.`). Both lines are concrete, testable, and verifiable. Matches the AGENTS.md `First line of every doc = fact. Last line of every doc = fact.` constraint. Same pattern as 015.018 / 015.021 / 015.022 PASS item.

12. **Verification trace is concrete** — commit body L37-43 records:
    - `swift build: clean`
    - `App rebuilt + relaunched (PID 59924)`
    - `Final toolbar: [3 left file actions] + [4 zone toggles in 1 group] + [1 export separate].`
    - `No center Divider (= Boss 16th OOB '正中间的一个没有内容的按钮 全都不要').`
    - `Total tests: 584 / 80 unit pass`
    Matches 015.012 / 015.018 / 015.021 / 015.022 PASS items (verification trail pattern with specific PID + test count).

13. **Final toolbar structure matches Boss 8/25 16th OOB literally** — the resulting structure is:
    - **`ToolbarItemGroup(.navigation)` L1029-1049**: 3 file actions (新建 / 打开 / 导入). Unchanged.
    - **`ToolbarItemGroup(.primaryAction)` L1050-1080**: 4 zone visibility toggles (项目管理区 / 工具区 / 聊天区 / 动态区). Unchanged (= already 1 group per 015.027).
    - **`ToolbarItem(.primaryAction)` L1090-1098**: 导出 (independent). Unchanged body, only comment text updated.
    - **No center `.principal` element**: the bogus `Divider()` is gone.
    This produces exactly Boss 16th OOB's spec: `'按钮位置现在对. 分不不对, 右边的四个显隐功能是一组, 导出独立. 文枢标题, 正中间的一个没有内容的按钮, 全都不要'`. The title was already removed in 015.027; this commit removes the center Divider; the grouping was already 1+1+1 from 015.027. The 3-element structure is now clean and matches the Boss拍.

14. **Existing comment-anchor pattern preserved (audit grep continuity)** — the diff replaces a 015.028 audit comment with a 015.030 audit comment, preserving the grep-recoverable lineage:
    - `git grep "Boss 8/25 16th OOB ticket 015.030"` will find the new comment block.
    - `git grep "Boss 8/25 14th OOB ticket 015.028"` will only find old history (015.028's commit + files at the 015.028 snapshot), NOT the current file.
    - `git grep "ticket 015.027"` will find the file-header comment at L1020-1027 (which was authored in 015.027 and preserved verbatim by this commit).
    This is the correct behavior for a fix to a prior commit's mistake: the prior comment is replaced, not preserved.

15. **No formatter churn, no incidental whitespace changes** — `git diff --stat` reports exactly 10 insertions and 9 deletions. The diff lines (L1081-1098 in both before and after) are all comment changes + the Divider block removal. No reformatting, no whitespace alignment tweaks, no SwiftLint-suggested reorderings. This is the smallest possible atomic change for the Boss 16th OOB response.

---

## SUGGEST

### S1 — Commit message body claims "Re-grouped right buttons per Boss 16th OOB: 4 zone visibility toggles in 1 ToolbarItemGroup + 导出 in SEPARATE ToolbarItemGroup" — but the diff did NOT re-group, only deleted a center Divider

The commit body L16-19 states:

> `Re-grouped right buttons per Boss 16th OOB:`
> `- 4 zone visibility toggles (= 项目管理区 /工具区 / 聊天区 / 动态区) in 1 ToolbarItemGroup(placement: .primaryAction) (= '一组' per Boss拍).`
> `- 导出 in SEPARATE ToolbarItem(placement: .primaryAction) (= '独立' per Boss拍).`

But the actual diff (10 insertions / 9 deletions) does **not** re-group anything. The 4-toggle `ToolbarItemGroup(.primaryAction)` and the separate `ToolbarItem(.primaryAction)` containing 导出 were both already in place from commit `c3d5de882` (015.027 — 18 minutes earlier, AuthorDate 2026-08-25 11:08:27 +0800). The only structural change in this commit is:

- Delete `ToolbarItem(placement: .principal) { Divider() }` (added by 015.028).
- Update the surrounding comment block.

This is a Standards-axis concern only insofar as the commit-message-vs-diff accuracy matters. The factual description "Final toolbar: [3 left file actions] + [4 zone toggles in 1 group] + [1 export separate]. No center Divider" at L40-41 is correct (and matches the resulting state). But the "Re-grouped" framing misrepresents what this commit actually does.

Recommended fix: in a follow-up doc commit (NOT a code rewrite), tighten the body to:

> ```
> Fix (Sources/WenshuApp/App.swift):
> - Removed ToolbarItem(placement: .principal) { Divider() } (= Boss 16th OOB
>   '正中间的一个没有内容的按钮 全都不要'). Divider was a 'placeholder' empty
>   button-feel element (added in ticket 015.028) that Boss拍 to remove.
> - 4 zone visibility toggles (= 项目管理区 / 工具区 / 聊天区 / 动态区) are in
>   one ToolbarItemGroup(placement: .primaryAction) (= '一组' per Boss拍,
>   already in place from ticket 015.027).
> - 导出 is in a SEPARATE ToolbarItem(placement: .primaryAction) (= '独立'
>   per Boss拍, already in place from ticket 015.027).
> - 文枢 title already removed in commit c3d5de882 (ticket 015.027).
> ```

Non-blocking — the verification trail at L37-43 is concrete and accurate, and the comment edit at the code site correctly frames the change as "Center Divider (from ticket 015.028) removed (= Boss 16th OOB '正中间的一个没有内容的按钮 全都不要')". The in-code audit comment is accurate even if the commit message is slightly misleading.

### S2 — The new comment block at file L1081-1089 mixes TWO Boss OOBs (13th OOB foundation from 015.027 + 16th OOB fix in 015.030) without naming the prior ticket explicitly in the subject

At file L1081-1089:

```swift
// v0.24 fix (Boss 8/25 16th OOB ticket 015.030 '分得不对,
// 右边的四个显隐功能是一组, 导出独立'): correct grouping =
// 4 zone visibility toggles (= 项目管理区 / 工具区 / 聊天区 / 动态区)
// in one ToolbarItemGroup (= '一组' per Boss拍), and 导出 in
// a SEPARATE ToolbarItemGroup (= '独立' per Boss拍).
// Per Boss 16th OOB: '文枢标题, 正中间的一个没有内容的按钮, 全都不要'.
// Center Divider (from ticket 015.028) removed (= Boss 16th OOB
// '正中间的一个没有内容的按钮 全都不要'). Toolbar order:
// [3 left file actions] + [4 zone toggles in 1 group] + [1 export separate].
```

The "Center Divider (from ticket 015.028) removed" sentence correctly cites the prior ticket that introduced the Divider (good — preserves lineage). But the comment block does not cite 015.027 (the actual origin of the 4-toggle group + export separate grouping). A future maintainer running `git blame` and only finding 015.028 + 015.030 might think the grouping was a single fix at 015.030 rather than an evolution: 015.023 → 015.027 → 015.028 → 015.030.

Recommended: add one short parenthetical at L1084-1086, e.g. `// grouping already in place from ticket 015.027; this commit drops the 015.028 center Divider.`. The full lineage is recoverable from git log, but a one-liner in the comment makes it findable via `git grep "015.027"` from the file.

Non-blocking — the comment block is functional and accurate enough for current readers. Future maintainers can always walk `git log -p Sources/WenshuApp/App.swift`.

### S3 — The 015.023 → 015.027 → 015.028 → 015.030 lineage is now 4 commits deep on the same `.toolbar { ... }` block — a future "split the toolbar into per-zone files" refactor is structurally inevitable

Counting the commits that have touched the `LayoutShellView.body.toolbar { ... }` block in just the v0.24 series:

- **015.023** (commit `0df3aef2c`): introduced the left + right toolbar groups.
- **015.027** (commit `c3d5de882`): rewrote into 3 ToolbarItemGroups + dropped title/+/center title. **2 files changed** in one commit (the original ticket pair scope).
- **015.028** (commit `1dc3ea1ff`): added the center `Divider` (now reverted by 015.030).
- **015.030** (commit `908e6d10b`): reverts the Divider and tightens the comment.

Each was a 1-commit / 1-ticket / 1-zone atomic fix, but the zone is small enough that the back-and-forth between Divider/no-Divider in the span of ~18 minutes (015.028 at 11:24 → 015.030 at 11:26) is visible in `git log`. This is *itself* a Standards-axis observation: the zone granularity forces wasteful revert churn.

Recommended for a FUTURE ticket (NOT this commit): split `LayoutShellView.body` so the `.toolbar { ... }` block lives in `LayoutToolbarView` (a small `View` consumed by `LayoutShellView`). Then each toolbar fix is genuinely 1 zone (LayoutToolbarView) and the churn doesn't dominate every adjacent commit. Also lets the toolbar get unit-tested in isolation.

Non-blocking for 015.030 — the current commit is the correct atomic response to Boss 16th OOB.

### S4 — The commit body's "Re-grouped right buttons" framing is also a minor Spec-axis miscount (Boss 13th OOB at 015.027 already established the grouping, not Boss 16th OOB)

Concretely: Boss 13th OOB at 015.027 said `'按我的截图拆成 3 组, 新建, 打开, 导入是一组, 居左, 放在红黄绿之后. 四个显/隐开关一组. 一个导出单独. 这五个居右. 除了以上罗列的元素, 标题, 一个加号按钮, 一个中间的标题. 这三个元素都不要了'`. The "4 toggles 1 group + export separate" specification already came from 015.027.

Boss 16th OOB at 015.030 corrected the *implementation mistake* from 015.028 (where the engineer had added a `Divider` between the 4-toggle group and the export). So the 015.030 commit's first sentence "Re-grouped right buttons per Boss 16th OOB" is a Spec-axis misattribution that bleeds into the Standards audit.

This observation belongs to the Spec axis review (which will run separately per Boss 8/25 '双轴每次都跑'), not the Standards axis. Flagging it here because the same wording also appears in the project audit comment block (`correct grouping = ...`). The Standards axis itself can document this as a forward-pointer to the Spec-axis review.

Non-blocking — the file-level comment block is correct ("Center Divider (from ticket 015.028) removed"). Only the commit-body framing has the misattribution, and that's already flagged in S1 above.

---

## VERDICT

**PASS** — All 15 PASS items hold. The 4 SUGGEST items are non-blocking (S1-S2 are commit-message / comment-text accuracy polish; S3 is a future-ticket refactor observation; S4 is a forward-pointer to the Spec-axis review).

The implementation is **functionally and Standards-aligned** for the Boss 8/25 16th OOB response:

- **F1 (English-only hard rule)**: the only CJK content in the diff is in explicit carve-out categories (boss quotes cited as evidence, `Boss`/`老板` audit markers, the unchanged UI strings). Same pattern as 015.005/006/012/014/018/021/022 PASS items.
- **12 forbidden vocabulary**: 0 occurrences.
- **Apple HIG SwiftUI patterns**: canonical `ToolbarItem` + `ToolbarItemGroup` placements with no `.principal { Divider() }` anti-pattern. 2-placement toolbar (`.navigation` + `.primaryAction`) is the HIG-compliant structure.
- **1 zone 1 ticket 1 commit 1 file (strictest form)**: exactly one file modified, zero Boss 8/22 exception needed. Smallest possible atomic change for the Boss 16th OOB response.
- **Vendor-neutral naming**: 0 vendor-specific identifiers introduced (no new identifiers at all).
- **Atomic scope**: tightly scoped to (a) one comment-block replacement + (b) one 3-line `Divider` block removal + (c) one 1-line export comment polish. No drive-by edits.
- **Boss-anchored audit comments**: every modified block carries the `v0.24 boss验收fix` + `Boss 8/25 16th OOB ticket 015.030` trio, with correct prior-ticket lineage citation.
- **UI strings in Chinese**: all 8 UI labels (新建 / 打开 / 导入 / 项目管理区 / 工具区 / 聊天区 / 动态区 / 导出) unchanged. **UI 全中文 preserved** per Boss 8/25 product direction.
- **Revert of 015.028 regression**: surgical removal of the center `Divider` that 015.028 had incorrectly added 18 minutes earlier. Lineage preserved via the "Center Divider (from ticket 015.028) removed" comment at L1087.
- **Toolbar grouping**: 3 groups per Boss 8/25 13th + 16th OOBs — `[3 left file actions] + [4 zone toggles in 1 group] + [1 export separate]` (the latter two already established by 015.027, this commit removes the 015.028 center Divider).
- **First/last line fact rule**: commit body opens and closes with concrete testable facts.
- **Verification trail**: `swift build: clean` + App PID 59924 + test count 584/80 + concrete toolbar-order enumeration.

The final toolbar now matches Boss 8/25 16th OOB literally:
- `按钮位置现在对` — button positions correct (no Group placement reshuffling needed; the 015.023→015.027 chain has them right).
- `分不不对, 右边的四个显隐功能是一组` — 4 zone toggles in 1 ToolbarItemGroup (unchanged from 015.027).
- `导出独立` — 导出 in its own ToolbarItem (unchanged from 015.027).
- `文枢标题...全都不要` — title already removed in 015.027.
- `正中间的一个没有内容的按钮 全都不要` — center `Divider` removed by this 015.030 commit.

The "分不不对" was the engineer's mis-grouping introduced at 015.028 (adding the `Divider` widget). This commit surgically removes that mis-grouping without disturbing the already-correct 015.027 structure.

**Final verdict: PASS — Standards axis clean. The SUGGEST items are: (S1) tighten commit-body framing to "removed center Divider" instead of "Re-grouped right buttons" (the re-grouping was already 015.027), (S2) add `ticket 015.027` lineage reference to the file comment block, (S3) future ticket to extract `LayoutToolbarView` for cleaner tooling-zone isolation, (S4) forward-pointer for the Spec-axis review to address the 015.027 vs 015.030 Spec misattribution. None block the commit.**

---

## Files created or modified by this review

- Created: `.scratch/reviews/015.030-toolbar-16th-oob-standards.md` (this report).
- Modified: none.
