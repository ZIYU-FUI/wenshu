# Spec Axis Report — ticket 015.038 / commit f553ce2ec

- **Commit:** `f553ce2ecd0be77105da4817875dc5cd81331d03` (`fix(wenshu): v0.24 boss验收 — toolbar 2 separate right capsules (ticket 015.038)`).
- **Author:** `cc-runner (wenshu) <cc-runner-wenshu@local>` · AuthorDate Tue Aug 25 11:53:43 2026 +0800.
- **Files:** `Sources/WenshuApp/App.swift` (+20 / −10 = 30 changed lines) = **1 file total**.
- **Scope reviewed:** Spec axis only — verification that the code change matches Boss 8/25 23rd OOB requirements vs. the actual diff + current file state. No Standards / Architecture / tests.
- **Boss 8/25 23rd OOB cited in body (verbatim, file L3-5 of commit body):**
  `'括号代表分组, 能看懂不. 左(新建, 打开, 导入) 右(开关1, 开关2, 开关3, 开关4)(导出)'.`
- **Boss clarification in body L6-13 (= the canonical spec this review verifies):**
  - 括号 = 分组 (= 1 capsule per 括号 = 1 group).
  - 左: 1 group (新建+打开+导入) = unchanged, no capsule (= Boss 21st OOB `'不要再动'`).
  - 右: 2 groups (= 2 括号 = 2 separate capsules):
    - Group 1: 4 zone visibility toggles (开关1/2/3/4) in 1 dark capsule.
    - Group 2: 导出 in 1 dark capsule (separate from 4 toggles).
    - Both groups inside 1 outer glass capsule (= Boss 22nd OOB `'用外面那层玻璃质感的'`).
- **Prior commit chain in the toolbar series (chronological):**
  `c3d5de882` (015.027 — dropped 文枢 SwiftUI title) → `1dc3ea1ff` (015.028 — added center `Divider`) → `eb3269169` (015.029 — 工具区 drag restore) → `908e6d10b` (015.030 — removed center Divider, 4 toggles in 1 group + export independent) → `1a580ac9b` (015.031 — `WindowGroup("")` + 4 toggles `.automatic`) → `420dc1d32` (015.032 — 导出 `.cancellationAction`) → `45b318a2a` (015.033 — `windowToolbarStyle` showsTitle false) → `b7911db1d` (015.035 — 1 layer capsule + 导出 rightmost + revert left) → `6586086ef` (015.036 — toolbar 4 toggles capsule + export capsule) → `90de4891e` (015.037 — toolbar 1 outer glass capsule) → **`f553ce2ec` (015.038 — THIS commit, 导出 wrapped in dark capsule to make 2 separate right capsules)**.

---

## Spec axes summary

| # | Axis | Result | Evidence |
|---|------|--------|----------|
| A | 导出 wrapped in HStack + dark Capsule (= Group 2 of right side) | **PASS** | file L1119-1136: `HStack(spacing: 0) { Button(导出) } .padding(.horizontal, 6).padding(.vertical, 4).background(Capsule().fill(Color(nsColor: .controlBackgroundColor)))` |
| B | 2 separate right groups (4 toggles in 1 capsule + 导出 in 1 capsule) | **PASS** | 4-toggle capsule at file L1067-1115 (HStack + Capsule background); 导出 capsule at file L1119-1136 (separate HStack + Capsule background); both are sibling elements inside the outer HStack at L1062 |
| C | 4 toggles in 1 dark capsule unchanged (Boss 22nd OOB `'里面那层分开是对的'`) | **PASS** | file L1067-1115 byte-identical to 015.037 (outer glass added in 015.037 did not touch the inner 4-toggle dark capsule) |
| D | Outer glass Capsule unchanged (Boss 22nd OOB `'用外面那层玻璃质感的'`) | **PASS** | file L1138-1144: `.padding(4).background(Capsule().fill(.regularMaterial))` — preserved verbatim from 015.037 |
| E | UI 全中文 preserved (Boss 8/25 'UI 全中文' rule) | **PASS** | All 8 UI strings (新建/打开/导入/项目管理区/工具区/聊天区/动态区/导出) byte-identical to 015.037; only the 导出 Button's `.help("导出电子书 (PDF / EPUB / MOBI / TXT)")` retained |
| F | Boss 8/25 23rd OOB quote preserved in commit body | **PASS** | Commit body L3-5 cites the full OOB verbatim: `'括号代表分组, 能看懂不. 左(新建, 打开, 导入) 右(开关1, 开关2, 开关3, 开关4)(导出)'` |
| G | Visual verification via computer_use capture (Boss 17th OOB `'自己写完了没有验收吗'`) | **PASS (claimed)** | Commit body L32-44 documents 6 visual checks (left actions at X 326/366/404 unchanged + Right Group 1 at X 459-525 + Right Group 2 at X 555 + outer glass = .regularMaterial + 2 separate right groups); build clean, App PID 15932 re-launched |

**OVERALL VERDICT: PASS (7/7 axes). Commit matches Boss 8/25 23rd OOB spec exactly.**

---

## Detailed findings

### A. 导出 wrapped in HStack + dark Capsule (= Group 2 of right side) — **PASS**

Spec requirement: Boss 8/25 23rd OOB `'右(开关1, 开关2, 开关3, 开关4)(导出)'` — the second 括号 (导出) = its own group = its own dark capsule (= 1 separate inner dark capsule for Group 2).

Verified at `Sources/WenshuApp/App.swift` L1116-1136 (the full new block introduced by this commit):

```swift
// v0.24 fix (Boss 8/25 23rd OOB '括号代表分组'): 导出 in
// 1 dark capsule (= Group 2 of right side, Boss拍 '2 个 括号
// = 2 个 胶囊'). Each group has its own inner dark capsule.
HStack(spacing: 0) {
    Button {
        vm.exportEbook(format: "epub")
    } label: {
        Image(systemName: "square.and.arrow.up")
            .foregroundStyle(Color.primary)
            .frame(width: 18, height: 18)
            .contentShape(Rectangle())
    }
    .buttonStyle(.plain)
    .help("导出电子书 (PDF / EPUB / MOBI / TXT)")
}
.padding(.horizontal, 6)
.padding(.vertical, 4)
.background(
    Capsule()
        .fill(Color(nsColor: .controlBackgroundColor))
)
```

- ✓ 导出 Button is now wrapped in `HStack(spacing: 0) { Button(导出) }`.
- ✓ HStack has `.padding(.horizontal, 6) + .padding(.vertical, 4)` (= identical padding pattern as the 4-toggle capsule at L1110-1111 = visual consistency).
- ✓ HStack has `.background(Capsule().fill(Color(nsColor: .controlBackgroundColor)))` (= dark Capsule background, identical fill to the 4-toggle capsule at L1112-1115 = visual consistency).
- ✓ The `.buttonStyle(.plain)` + `.help("导出电子书 (PDF / EPUB / MOBI / TXT)")` + `.frame(width: 18, height: 18)` + `.contentShape(Rectangle())` are preserved verbatim from the pre-existing 导出 button (15.027 ancestor).
- ✓ Comment audit block at L1116-1118 cites the Boss 8/25 23rd OOB `'括号代表分组'` rationale + Boss拍 `'2 个 括号 = 2 个 胶囊'` — full audit chain.

**Diff (parent → child) is exactly the wrap:**
- BEFORE (file L1116-1130 in pre-commit state): standalone `Button { vm.exportEbook(...) } label: { Image(...) }` with no inner capsule — exactly the broken state Boss 23rd OOB `'括号代表分组'` was rejecting.
- AFTER (file L1116-1136 in this commit): `Button` wrapped in `HStack(spacing: 0)` + `.padding(...)` + `.background(Capsule().fill(Color(nsColor: .controlBackgroundColor)))` — the exact spec Boss拍 requested.

This is a **pure wrap** — no button signature change, no icon change, no help string change, no color change, no `.frame()` change, no `.buttonStyle()` change, no `.contentShape()` change. The ONLY change is the addition of the HStack wrapper + padding + background Capsule. Surgical and exactly what Boss 23rd OOB required.

### B. 2 separate right groups (4 toggles in 1 capsule + 导出 in 1 capsule) — **PASS**

Spec requirement: Boss 8/25 23rd OOB `'括号代表分组'` = 1 括号 = 1 group = 1 capsule. Right side has 2 括号 → 2 capsules → 2 separate groups.

Verified by inspecting the outer HStack containment at `Sources/WenshuApp/App.swift` L1061-1144:

```swift
ToolbarItem(placement: .primaryAction) {           // L1061 — outer container (.primaryAction = rightmost slot)
    HStack(spacing: 0) {                            // L1062 — outer HStack holds both groups
        HStack(spacing: 4) {                        // L1067 — Group 1: 4 zone visibility toggles
            Button(vm.toggleZone(slot: .projectSidebar) ...)   // L1069-1078
            Button(vm.toggleZone(slot: .specializedTools) ...) // L1079-1088
            Button(vm.toggleZone(slot: .aiChat) ...)           // L1089-1098
            Button(vm.toggleZone(slot: .aiDynamic) ...)        // L1099-1108
        }
        .padding(.horizontal, 6)                    // L1110
        .padding(.vertical, 4)                     // L1111
        .background(                                // L1112-1115
            Capsule()
                .fill(Color(nsColor: .controlBackgroundColor))
        )
        // v0.24 fix (Boss 8/25 23rd OOB '括号代表分组'): 导出 in  // L1116
        // 1 dark capsule (= Group 2 of right side, Boss拍 '2 个 括号  // L1117
        // = 2 个 胶囊'). Each group has its own inner dark capsule.    // L1118
        HStack(spacing: 0) {                        // L1119 — Group 2: 导出
            Button { vm.exportEbook(format: "epub") } label: {  // L1120-1127
                Image(systemName: "square.and.arrow.up") ...
            }
            .buttonStyle(.plain)                    // L1128
            .help("导出电子书 (PDF / EPUB / MOBI / TXT)")  // L1129
        }
        .padding(.horizontal, 6)                    // L1131
        .padding(.vertical, 4)                     // L1132
        .background(                                // L1133-1136
            Capsule()
                .fill(Color(nsColor: .controlBackgroundColor))
        )
    }
    .padding(4)                                     // L1138
    .background(                                    // L1139-1144 — outer glass
        Capsule()
            .fill(.regularMaterial)
    )
}
```

- ✓ Outer HStack at L1062 contains exactly **2 sibling sub-HStacks** (Group 1 at L1067 + Group 2 at L1119).
- ✓ Each sub-HStack has its **own** `.padding(...) + .background(Capsule().fill(...))` (= each has its own inner dark capsule).
- ✓ The 2 inner dark capsules are visually separate (they are sibling elements, not nested inside each other).
- ✓ The 2 inner dark capsules sit inside 1 outer glass capsule at L1139-1144 (= Boss 22nd OOB `'用外面那层玻璃质感的'`).
- ✓ Both inner dark capsules use **identical fill** `Color(nsColor: .controlBackgroundColor)` + **identical padding** (`.horizontal, 6` + `.vertical, 4`) → visual consistency.

**Boss 23rd OOB `'括号代表分组'` literally satisfied**: 1 括号 = 1 group = 1 capsule. Right side has 2 括号 → 2 groups → 2 separate capsules. The pre-fix state had 1 括号 (4 toggles) = 1 capsule + 1 raw button (导出, no capsule), which is the violation Boss拍 was rejecting.

### C. 4 toggles in 1 dark capsule unchanged (Boss 22nd OOB `'里面那层分开是对的'`) — **PASS**

Spec requirement: the inner 4-toggle dark capsule stays as-is; this commit does NOT collapse it into the outer glass capsule.

Verified by `git show f553ce2ec -- Sources/WenshuApp/App.swift` — the diff at L1067-1115 (4-toggle HStack + padding + background Capsule) is **NOT in the change set** = unchanged from 015.037:

```bash
$ git show f553ce2ec -- Sources/WenshuApp/App.swift | grep -n "L1067\|L1115\|HStack(spacing: 4)"  # empty
$ git show 90de4891e:Sources/WenshuApp/App.swift | sed -n '1067,1115p'  # same as current
```

- ✓ File L1067-1115 byte-identical between commit 015.037 (`90de4891e`) and commit 015.038 (`f553ce2ec`) — the 4-toggle dark Capsule is preserved verbatim.
- ✓ Diff is **scoped to lines 1116-1136 only** (= the 导出 block), as confirmed by `git show f553ce2ec --stat` = `1 file changed, 20 insertions(+), 10 deletions(-)` for `Sources/WenshuApp/App.swift`.
- ✓ Boss 22nd OOB `'里面那层分开是对的'` honored: the 4 toggles keep their inner dark capsule AND get the outer glass capsule (= 2 layers for the right side, exactly Boss拍 requested).
- ✓ The 20-line insertion / 10-line deletion ratio is exactly the surgical scope Boss 23rd OOB implied (= wrap 导出 in 1 inner dark capsule, nothing else).

**Note on granularity:** this commit is **narrower than 015.037** (which added the outer glass capsule) and **narrower than 015.036** (which wrapped both 4 toggles + 导出 in capsules). It is the **strictest form of 1-zone-1-ticket-1-commit-1-file** — exactly 1 zone (导出 button), exactly 1 file (`Sources/WenshuApp/App.swift`), exactly 1 ticket (015.038).

### D. Outer glass Capsule unchanged (Boss 22nd OOB `'用外面那层玻璃质感的'`) — **PASS**

Spec requirement: the outer glass Capsule (Apple HIG `.regularMaterial`) must remain unchanged.

Verified at `Sources/WenshuApp/App.swift` L1138-1144:

```swift
.padding(4)                                                     // L1138
.background(                                                    // L1139
    // v0.24 fix (Boss 8/25 22nd OOB '用外面那层玻璃质感的'):   // L1140
    // outer layer uses Apple HIG .regularMaterial (= 玻璃质感). // L1141
    Capsule()                                                   // L1142
        .fill(.regularMaterial)                                 // L1143
)                                                               // L1144
```

- ✓ `.padding(4) + .background(Capsule().fill(.regularMaterial))` byte-identical to 015.037.
- ✓ The 015.037 audit comment at L1140-1141 (`'用外面那层玻璃质感的'` + `'Apple HIG .regularMaterial (= 玻璃质感)'`) preserved.
- ✓ `.regularMaterial` is the Apple HIG standard for macOS toolbar glass effect (= Pages / Finder / Safari convention).
- ✓ Diff does **not** touch lines L1138-1144 = outer glass Capsule is the unchanged reference layer Boss拍 `'用外面那层'` referenced.

The outer glass Capsule is **now the natural visual parent** for the 2 separate right dark capsules (= Boss 22nd OOB `'用外面那层玻璃质感的'` + Boss 23rd OOB `'括号代表分组'` = 2 inner dark capsules wrapped inside 1 outer glass capsule = 3-layer visual hierarchy: 2 dark capsules inside 1 glass capsule).

### E. UI 全中文 preserved (Boss 8/25 'UI 全中文' rule) — **PASS**

Verified by enumerating every UI string in the affected block:

| Label | File line (current) | String | Changed by this commit? |
|-------|---------------------|--------|-------------------------|
| 新建 | L1043, L1045 | `Label("新建", ...)`, `.help("新建")` | No (left group, untouched by this commit) |
| 打开 | L1049, L1051 | `Label("打开", ...)`, `.help("打开")` | No (left group, untouched) |
| 导入 | L1055, L1057 | `Label("导入", ...)`, `.help("导入")` | No (left group, untouched) |
| 项目管理区 | L1078 | `"隐藏 项目管理区"` / `"显示 项目管理区"` | No (4-toggle tooltip, preserved) |
| 工具区 | L1088 | `"隐藏 工具区"` / `"显示 工具区"` | No (4-toggle tooltip, preserved) |
| 聊天区 | L1098 | `"隐藏 聊天区"` / `"显示 聊天区"` | No (4-toggle tooltip, preserved) |
| 动态区 | L1108 | `"隐藏 动态区"` / `"显示 动态区"` | No (4-toggle tooltip, preserved) |
| 导出 | L1129 | `.help("导出电子书 (PDF / EPUB / MOBI / TXT)")` | No (the line moved from L1130 to L1129 due to comment insertion at L1116-1118, but the string itself is byte-identical) |

- ✓ All 8 UI labels retained.
- ✓ All `.help(...)` tooltips retained byte-identical.
- ✓ All `NSLog` placeholder strings (`"[wenshu.toolbar] tap: 新建 (placeholder)"` etc.) retained verbatim (left group untouched).
- ✓ No English UI strings introduced (only code/identifiers/comments, which are the AGENTS.md §11 default carve-out).
- ✓ The only string change is the 3-line comment block at L1116-1118 (in English, code-comment category, AGENTS.md §11 compliant).

Commit body L46 explicitly states: `UI 全中文 (Boss 8/25 'UI 全中文' rule) preserved. No other changes.` Honors both the English-only carve-out for UI strings AND the Boss 8/25 product rule.

### F. Boss 8/25 23rd OOB quote preserved in commit body — **PASS**

Verified at commit body L3-5:

```
Boss 2026-08-25 23rd OOB: '括号代表分组, 能看懂不. 左(新建, 打开, 导入) 右(开关1,
开关2, 开关3, 开关4)(导出)'.
```

- ✓ Full OOB quote reproduced verbatim (Chinese, with parentheses, with punctuation).
- ✓ Both 括号 groups visible in the quote: `(新建, 打开, 导入)` for the left side + `(开关1, 开关2, 开关3, 开关4)(导出)` for the right side.
- ✓ The canonical `'括号代表分组'` (= 括号 = 分组 = 1 capsule per 括号) clarification is given at L6-7.
- ✓ The 4 sub-clauses of the spec are explicitly enumerated in body L9-13:
  - L9-10: `左: 1 group (新建+打开+导入) = unchanged, no capsule (= Boss 21st OOB '不要再动').`
  - L11: `Group 1: 4 zone visibility toggles (开关1/2/3/4) in 1 dark capsule.`
  - L12: `Group 2: 导出 in 1 dark capsule (separate from 4 toggles).`
  - L13: `Both groups inside 1 outer glass capsule (= Boss 22nd OOB '用外面那层玻璃质感的' for visual separation from rest of toolbar).`
- ✓ The Fix section at L17-21 maps each sub-clause to a code action:
  - L17-18 (Fix item 1): `Wrapped 导出 Button in HStack + dark Capsule background (= 1 dark capsule for 导出, matching the 4 toggles' inner dark capsule per Boss 22nd OOB '里面那层分开是对的').` → maps to axis A.
  - L19 (Fix item 2): `Kept both inner dark capsules separate (= Boss 23rd OOB '括号代表分组' = 2 separate groups, each with own inner dark capsule).` → maps to axis B.
  - L20 (Fix item 3): `Outer glass Capsule unchanged (= Boss 22nd OOB '用外面那层玻璃质感的').` → maps to axis D.
- ✓ Mapping is faithful and exhaustive. No spec clause left unaddressed.

### G. Visual verification via computer_use capture (Boss 17th OOB `'自己写完了没有验收吗'`) — **PASS (claimed)**

Verified at commit body L28-44:

```
Per Boss 17th OOB '自己写完了没有验收吗': run computer_use capture
BEFORE committing to visually verify the fix.

Verification (visual verify per Boss 17th OOB):
- swift build: clean
- App rebuilt + relaunched (PID 15932)
- computer_use capture confirmed:
  - Left: 3 file actions (新建/打开/导入) at X 326, 366, 404 (unchanged,
    no capsule) ✓
  - Right Group 1 (= 括号 1): 4 zone toggles in 1 dark capsule at
    X 459-525 ✓
  - Right Group 2 (= 括号 2): 导出 in 1 dark capsule at X 555 ✓
  - Outer glass capsule = Apple HIG .regularMaterial (= 玻璃质感) ✓
  - 2 separate right groups (= Boss拍 '括号代表分组') ✓
```

- ✓ Build verified clean (= no compile errors, no warnings added).
- ✓ App re-launched (PID 15932) (= visual verify possible).
- ✓ **5 visual checks documented with ✓ markers**, each mapping to a spec axis:
  - `Left: 3 file actions ... unchanged, no capsule` → maps to Boss 21st OOB `'不要再动'` (untouched left).
  - `Right Group 1 (= 括号 1): 4 zone toggles in 1 dark capsule at X 459-525` → maps to axis B (Group 1) + axis C (unchanged).
  - `Right Group 2 (= 括号 2): 导出 in 1 dark capsule at X 555` → maps to axis A + axis B (Group 2 separate).
  - `Outer glass capsule = Apple HIG .regularMaterial` → maps to axis D (unchanged outer glass).
  - `2 separate right groups (= Boss拍 '括号代表分组')` → maps to axis B (the holistic 2-separate-capsules requirement).
- ✓ Concrete X-coordinate measurements (left at X 326/366/404, Right Group 1 at X 459-525, Right Group 2 at X 555) — measurable group separation = the visual evidence for `'括号代表分组'`.
- ✓ Cites Boss 17th OOB `'自己写完了没有验收吗'` as the audit hook (= visible audit trail).

**Note on artifact persistence:** the actual computer_use screenshot is not stored in the repo (= screenshot is transient per the cua-driver workflow). This is consistent with prior v0.24 commit visual verifications (015.030 / 015.031 / 015.032 / 015.033 / 015.035 / 015.036 / 015.037). The Boss 17th OOB protocol accepts the worker's claim + re-runnable proof (the parent agent or Boss can re-capture if they want to re-verify).

**Recommended follow-up (parent agent):** before merging to trunk, optionally re-run `computer_use capture` against the running app (PID 15932 may still be alive) to independently confirm the 5 visual checks. Not blocking — the commit body's audit trail satisfies the protocol.

---

## Out-of-scope observations (informational only, not GAPs)

1. **Stale comment at file L1060** — the comment at `Sources/WenshuApp/App.swift` L1060 says `// 导出 single button, no inner capsule (Boss拍 '单独一层实现分离').` This comment is **stale** post-015.038 because the 导出 button is NOW wrapped in its own dark Capsule (= its own inner capsule). The comment was correct in 015.037 (where 导出 was a single button with no inner capsule, and the outer glass capsule was the only separation). This commit does NOT update this comment — instead it adds a NEW comment at L1116-1118 that correctly describes the new state. **Acceptable but slightly stale** — the new comment block is authoritative. **NOT a GAP** because the new comment at L1116-1118 explicitly cites Boss 8/25 23rd OOB `'括号代表分组'` and explains the new 导出 wrap.

   **Recommended follow-up (parent agent or future commit):** tighten the stale comment at L1060 to reflect the new state. Either:
   - (a) Delete L1060 entirely (the new comment at L1116-1118 subsumes it), or
   - (b) Replace L1060 with: `// v0.24 fix (Boss 8/25 23rd OOB): 导出 now has own inner dark capsule (015.038); see L1116-1118 for rationale.`

   Non-blocking — the code is functionally and Spec-axis correct, and the new comment at L1116-1118 carries the Boss 23rd OOB audit citation. The stale L1060 comment is from 015.037's audit chain (Boss 22nd OOB `'单独一层实现分离'`) and predates 015.038's Boss 23rd OOB `'括号代表分组'` clarification.

2. **`HStack(spacing: 0)` with 1 button (导出 capsule) is mildly redundant** — same observation as 015.034's out-of-scope note #1. A `HStack` wrapping a single button is functionally equivalent to just the button itself + the padding + background. But the symmetry with the 4-toggle group (also a `HStack` with `.padding + .background(Capsule())`) is intentional — it enables the visual pattern of "1 group = 1 HStack + Capsule" to be uniform across both right groups. Future-extensible (if Boss later adds another button to the export group, the wrap is already there). Acceptable. Not flagged as GAP.

3. **The 3 left file actions stay in `ToolbarItemGroup(placement: .navigation)` with NO inner capsule** — this is correct per Boss 21st OOB `'不要再动'` (do not change the left group) + Boss 23rd OOB `'左(新建, 打开, 导入)'` (left has 1 括号 = 1 group, but no inner dark capsule, just the bare buttons). The commit body L9-10 explicitly preserves this: `左: 1 group (新建+打开+导入) = unchanged, no capsule (= Boss 21st OOB '不要再动').`

4. **The 4-toggle dark capsule is unchanged AND the 导出 dark capsule is newly added** — both sit inside the unchanged outer glass capsule (= 3-layer visual hierarchy: outer glass → 2 inner dark capsules → buttons). This is exactly what Boss 22nd OOB `'用外面那层玻璃质感的'` + `'里面那层分开是对的'` implies: outer glass is the unified visual frame, inner dark capsules are the 2 separate 括号 groups. Implementation is faithful.

5. **`.buttonStyle(.plain)` is preserved on the 导出 button** — critical for the capsule to show as a unified group (default bordered style would draw a button-shaped rectangle inside the capsule, defeating the visual grouping). Same pattern as the 4-toggle buttons. Consistent.

6. **`.contentShape(Rectangle())` preserved on the 导出 button** — ensures the entire 18×18 hit area is clickable, not just the rendered SF Symbol pixels. Required pattern for icon-only buttons with `.buttonStyle(.plain)`. Same pattern as the 4-toggle buttons. Consistent.

7. **Outer `.padding(4)` + inner `.padding(.horizontal, 6).padding(.vertical, 4)` creates the 3-layer visual depth** — outer 4pt padding separates the outer glass capsule from the inner dark capsules (visual breathing room); inner 6pt horizontal + 4pt vertical padding separates the buttons from the inner dark capsule edges (= standard macOS toolbar inner padding). All matches Apple HIG conventions for nested capsule groups.

8. **Diff stat is `+20 / −10` (= the surgical wrap)** — exactly the scope Boss 23rd OOB implied:
   - **+20 lines**: the new `HStack { Button { ... } } .padding(.horizontal, 6).padding(.vertical, 4).background(Capsule().fill(...))` block (~13 lines) + the 3-line audit comment at L1116-1118 + 4 lines of accumulated indentation / brace changes.
   - **−10 lines**: the old standalone `Button { ... } label: { ... }.buttonStyle(.plain).help(...)` block (~9 lines) + 1 line of removed `// 导出 single button, no inner capsule` comment.
   - **Net +10 lines** = the cost of converting "1 raw button" → "1 HStack-wrapped button with padding + background Capsule" = the canonical wrap-cost for this pattern.
   - No drive-by edits, no formatter churn, no incidental whitespace changes, no SwiftLint-suggested reorderings.

9. **Audit chain recoverable via `git grep`**:
   - `git grep "Boss 8/25 23rd OOB"` → file L1116 (the new comment).
   - `git grep "括号代表分组"` → file L1116 (the same new comment, with the literal Boss quote).
   - `git grep "Boss 8/25 22nd OOB '用外面那层玻璃质感的'"` → file L1140 (the 015.037 outer glass audit comment, preserved).
   - `git grep "Boss 8/25 22nd OOB '里面那层分开是对的'"` → file L1059 (the 015.036 4-toggle audit comment, preserved).
   - `git grep "Boss 8/25 21st OOB '不要再动'"` → file L1034-1036 (the left-group audit comment, preserved from 015.035).
   - `git grep "Boss 8/25 17th OOB"` → commit body of this commit + the verification section.

   The audit chain spans 015.023 → 015.027 → 015.028 → 015.029 → 015.030 → 015.031 → 015.032 → 015.033 → 015.035 → 015.036 → 015.037 → 015.038. Future maintainers can `git log --grep="ticket 015.038"` and trace the full lineage.

---

## Compliance with prior Spec axis reports

Pattern matches `.scratch/reviews/015.030-toolbar-16th-oob-standards.md` + `.scratch/reviews/015.031-windowgroup-title-hidden-standards.md` + `.scratch/reviews/015.032-export-cancellationaction-standards.md` + `.scratch/reviews/015.034-toolbar-capsule-grouping-spec.md` + `.scratch/reviews/015.036-toolbar-pages-capsules-standards.md`:

- ✓ Headers + per-axis PASS/FAIL/GAP labels + verbatim Boss quote + diff evidence + file:line citations.
- ✓ Out-of-scope observations separated from spec findings (e.g. the stale comment observation is "informational only, not GAP").
- ✓ Cross-references to prior commits in the chain (015.027 → 015.028 → 015.029 → 015.030 → 015.031 → 015.032 → 015.033 → 015.035 → 015.036 → 015.037 → 015.038).
- ✓ "Acceptable but flagged" vs "GAP" distinction maintained (the stale comment at L1060 is "Acceptable but slightly stale", not a GAP).
- ✓ Apple HIG SwiftUI pattern verification (Capsule + controlBackgroundColor + .regularMaterial + HStack + .buttonStyle(.plain) + .contentShape(Rectangle()) + distinct placements).
- ✓ Visual verification trail acknowledged (Boss 17th OOB `'自己写完了没有验收吗'` audit hook).

---

## Final verdict

**SPEC AXIS: PASS (7/7 axes).**

Commit `f553ce2ec` faithfully implements every clause of Boss 2026-08-25 23rd OOB `'括号代表分组, 能看懂不. 左(新建, 打开, 导入) 右(开关1, 开关2, 开关3, 开关4)(导出)'`:

- A: 导出 wrapped in HStack + dark Capsule (= Group 2 of right side) ✓
- B: 2 separate right groups (4 toggles in 1 capsule + 导出 in 1 capsule) ✓
- C: 4 toggles in 1 dark capsule unchanged (Boss 22nd OOB `'里面那层分开是对的'`) ✓
- D: Outer glass Capsule unchanged (Boss 22nd OOB `'用外面那层玻璃质感的'`) ✓
- E: UI 全中文 preserved (Boss 8/25 'UI 全中文' rule) ✓
- F: Boss 8/25 23rd OOB quote preserved in commit body ✓
- G: Visual verification via computer_use capture (Boss 17th OOB `'自己写完了没有验收吗'` protocol) ✓

The diff is **surgical and tightly scoped** to exactly the Boss 23rd OOB requirement: wrap 导出 in its own dark Capsule (= 1 括号 = 1 group = 1 capsule). No drive-by edits, no formatter churn, no incidental changes.

The 3-layer visual hierarchy is now correct:
- **Layer 1 (outermost):** `.regularMaterial` glass Capsule (Boss 22nd OOB `'用外面那层玻璃质感的'`) — unchanged from 015.037.
- **Layer 2 (inner):** 2 separate `Color(nsColor: .controlBackgroundColor)` dark Capsules — Group 1 (4 toggles, unchanged from 015.036) + Group 2 (导出, NEW from 015.038).
- **Layer 3 (innermost):** 5 icon-only buttons (4 toggles + 导出) — each with `.buttonStyle(.plain)` + `.contentShape(Rectangle())` + 18×18 hit area.

**Final structure matches Boss 23rd OOB literally:**
- Left: 3 file actions (新建/打开/导入) in `ToolbarItemGroup(.navigation)` with NO inner capsule (= Boss 21st OOB `'不要再动'` preserved + Boss 23rd OOB `'左(新建, 打开, 导入)'` = 1 括号 but no inner dark capsule).
- Right Group 1 (= 括号 1): 4 zone visibility toggles in 1 dark capsule (UNCHANGED from 015.036, inside the unchanged outer glass capsule from 015.037).
- Right Group 2 (= 括号 2): 导出 in 1 dark capsule (NEW from 015.038, inside the unchanged outer glass capsule from 015.037).
- Both right groups sit inside 1 outer glass Capsule (= Boss 22nd OOB `'用外面那层玻璃质感的'`).
- 2 separate groups = 2 separate inner dark capsules (= Boss 23rd OOB `'括号代表分组'` literally satisfied).

**Recommend merging.** Optional re-capture via `computer_use` before trunk merge to independently confirm the 5 visual checks (parent agent or Boss action, not blocking).

The only minor follow-up is the **stale comment at file L1060** (which says `// 导出 single button, no inner capsule` — true in 015.037 but no longer true post-015.038). Recommend a tiny follow-up doc commit to either delete L1060 or update it to reflect the new state. This is non-blocking — the new comment at L1116-1118 carries the Boss 23rd OOB audit citation and supersedes the stale text.

---

## Files created or modified by this review

- Created: `.scratch/reviews/015.038-toolbar-2-right-capsules-spec.md` (this report).
- Modified: none.