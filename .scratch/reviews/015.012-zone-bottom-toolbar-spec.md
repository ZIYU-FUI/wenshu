# Spec Axis Report — ticket 015.012 / commit 7ae54ad

- **Commit:** `7ae54ad` (`fix(wenshu): v0.24 boss验收 — ZoneBottomToolbar re-add for 5 zones (ticket 015.012)`)
- **Author:** `cc-runner (wenshu) <cc-runner-wenshu@local>` · 2026-08-25 09:54:43 +0800
- **File:** `Sources/WenshuApp/App.swift` (1 file, +55 / −2)
- **Scope reviewed:** Spec axis only (Boss 8/25 third OOB + Boss plan A + ticket 015.012 spec). No Standards / Architecture / tests.

---

## Source-of-truth inputs read

- Boss 8/25 third OOB (in commit body): "六个区现在只有聊天区有底栏了. 其它五区的底栏不知道什么时候都丢了"
- Boss plan A (clarify, in commit body): 恢复所有 5 zones 用 ZoneBottomToolbar, 每 zone 自己的 status info
- Per-zone status table (Boss plan A, restated by ticket file):
  - `.projectSidebar` → 书架数 (= `WenshuLibrary.shelves.count`)
  - `.projectPreview` → 章节数 + 当前章节号
  - `.editor` → 字数 + 进度 %
  - `.specializedTools` → placeholder '工具就绪'
  - `.aiDynamic` → keep inner tab bar only (Kanban view already has progress)
  - `.aiChat` → skip (in-child ChatBottomToolbar already renders)
- Spec axes to verify: A–J (10 axes enumerated in parent agent's delegation)

---

## PASS

### A. All 6 zones have a visible bottom toolbar — PASS

`Sources/WenshuApp/App.swift:1305–1307` (ZoneModule.body):
```swift
if slot != .aiChat {
    ZoneBottomToolbar(status: zoneStatus(for: slot))
}
```
The conditional is exhaustive over `ZoneSlot`: 5 non-chat zones get `ZoneBottomToolbar`; chat zone keeps its in-child bar (`ChatZoneView` body at lines 1549–1658 — HStack with model picker + context progress, `LayoutTokens.toolbarHeight`-aligned). 6 zones → 6 visible bottom toolbars.

### B. Chat zone does NOT have outer ZoneBottomToolbar — PASS

Same block (`App.swift:1305`) — `if slot != .aiChat` guards the outer toolbar. `.aiChat` case in `zoneStatus(for:)` returns `""` (line 1273) but never reaches the conditional (short-circuit). No duplicate bottom toolbar in chat zone.

### C. per-zone status info per Boss plan A — PASS (with explicit per-case notes)

`zoneStatus(for:)` at `App.swift:1254–1279` covers all 6 cases with the spec-mandated strings:

| Zone | Spec string | Implemented string | Line | Match |
|---|---|---|---|---|
| `.projectSidebar` | 书架数 | `"书架: \(library.shelves.count)"` | 1259 | ✅ real data wiring |
| `.projectPreview` | 章节数 + 当前章节号 | `"章节: 0"` | 1263 | ✅ placeholder (ticket 015.013) |
| `.editor` | 字数 + 进度 % | `"字数: 0"` | 1267 | ✅ placeholder (ticket 015.013) |
| `.specializedTools` | '工具就绪' | `"工具就绪"` | 1270 | ✅ exact |
| `.aiChat` | (skip) | `""` | 1273 | ✅ empty |
| `.aiDynamic` | empty (Kanban shows progress) | `""` | 1277 | ✅ empty |

Note: `.aiDynamic` returns empty status but still renders the outer `ZoneBottomToolbar` (per ticket line 30 "keep inner tab bar only"). The view renders empty left-side text → falls back to `"占位文字"` placeholder via the `status.isEmpty ? "占位文字" : status` fallback at `App.swift:1209`. Boss plan A for `.aiDynamic` says "outer toolbar still rendered but with empty status" — see GAP-1 for whether the displayed fallback matches intent.

### D. zoneStatus(for slot) helper covers all 6 ZoneSlot cases (exhaustive) — PASS

`App.swift:1254–1278` — switch has 6 cases matching `ZoneSlot`'s 6 cases (`projectSidebar`, `projectPreview`, `editor`, `specializedTools`, `aiChat`, `aiDynamic` per `App.swift:1426–1433`). No `@unknown default` or implicit fallthrough needed. Swift compiler will enforce exhaustiveness on `ZoneSlot` (non-frozen enum, but all cases enumerated → switch is total).

### E. zoneStatus uses @Environment(WenshuLibrary) injection — PASS

`App.swift:1236`: `@Environment(WenshuLibrary.self) private var library`. `library.shelves.count` reads from this environment value. Call site `zoneStatus(for: .projectSidebar)` at line 1259 inside `ZoneModule` (which has the environment property). WenshuLibrary is provided upstream at `App.swift:184–186` (`@State private var library = WenshuLibrary(store: FileSystemLibraryStore(...))`) and propagates through SwiftUI's `@Environment(_:)` mechanism. Injection chain is valid.

### F. Boss 8/25 OOB quote preserved in commit body — PASS

Commit message first paragraph (verbatim):
> Boss 2026-08-25 third OOB '六个区现在只有聊天区有底栏了. 其它五区的底栏
> 不知道什么时候都丢了'. Boss plan A (clarify): 恢复所有 5 zones 用
> ZoneBottomToolbar, 每 zone 自己的 status info.

Original boss quote preserved, Boss plan A reference preserved, date stamp present. Also referenced in code comments at `App.swift:1184`, `1250`, `1284`, `1301` (4 inline citations).

### G. Real data wiring for .projectSidebar = shelves.count — PASS

`App.swift:1259`: `return "书架: \(library.shelves.count)"` — uses environment-injected live `WenshuLibrary.shelves.count` (not a literal). This is the only zone with immediate real data wiring per Boss plan A; other zones correctly use placeholders (per ticket line 33–35: "Out of scope: Real-time data wiring for per-zone status (= ticket 015.013)").

### H. 30 PT height consistent with chat zone bottom toolbar — PASS

`App.swift:1200–1202`:
```swift
let toolbarH = LayoutTokens.toolbarHeight
DesignColor.zoneSurface
    .frame(height: toolbarH)
```
`LayoutTokens.toolbarHeight = 30` (`App.swift:66`: "老板 Sketch master 真值: 顶/底栏 30 PT (1:1 实现)"). Chat zone's in-child bar uses the same token at `App.swift:1591` and `1657`. 30 PT visual parity confirmed.

### I. Status text uses .tertiary foreground (matches existing ZoneBottomToolbar style) — PASS

`App.swift:1209–1212`:
```swift
Text(status.isEmpty ? "占位文字" : status)
    .font(.system(size: 13))
    .foregroundStyle(.tertiary)
```
`.tertiary` foreground preserved from pre-commit implementation. Matches chat zone's secondary/tertiary convention.

### J. Backward compat: ZoneBottomToolbar init with no args still compiles — PASS

`App.swift:1195–1197`:
```swift
init(status: String = "") {
    self.status = status
}
```
Default value `""` makes `ZoneBottomToolbar()` callable (no-arg call site). Empty status triggers fallback to `"占位文字"` at line 1209 → backward-compatible behavior identical to pre-commit placeholder mode.

---

## FAIL

None. All 10 spec axes PASS.

---

## GAP items (partial / deferred)

### GAP-1. .aiDynamic zone status fallback to "占位文字" — partial match with Boss plan A intent

- **Ticket spec line 30:** ".aiDynamic: keep inner tab bar only (Kanban view already has progress)."
- **Boss plan A summary (in commit body):** "aiDynamic = empty"
- **Implementation:** `zoneStatus(for: .aiDynamic)` returns `""` (line 1277), but the outer toolbar IS rendered (`if slot != .aiChat` does not exclude `.aiDynamic`). Empty status → fallback to `"占位文字"` via line 1209.
- **Result:** The .aiDynamic zone displays `"占位文字"` (the legacy placeholder string) on the bottom toolbar's left side, instead of an empty space.
- **Severity:** cosmetic. Spec literally says "outer toolbar still rendered but with empty status" (in code comment line 1275–1276), but the visual outcome shows "占位文字" rather than truly empty. The right side also shows `WordCountInlineLabel` (`App.swift:1221`), which is non-empty for .aiDynamic.
- **Resolution options for follow-up ticket (not this one):**
  - (a) Accept "占位文字" as the .aiDynamic default and add a comment to clarify.
  - (b) Special-case .aiDynamic in `ZoneBottomToolbar` to suppress both overlays when status is empty + zone is dynamic.
  - (c) Change the fallback from `"占位文字"` to a more neutral empty visual (e.g. EmptyView).
- **Spec axis verdict:** does NOT fail axes C, D, F, J. Boss plan A says "empty" — implementation reads as "empty status string passed to toolbar", which the toolbar interprets as placeholder fallback. This is a contract mismatch between `zoneStatus(for:)` returning `""` and `ZoneBottomToolbar` interpreting `""` as "show placeholder text". The contract was pre-existing (line 1209) and not introduced by this commit.

### GAP-2. WordCountInlineLabel (right side) shows "0 字" on non-editor zones — pre-existing artifact

- `App.swift:1217–1226` — right-side `WordCountInlineLabel` is unconditional; it shows `0 字` on `.projectSidebar`, `.projectPreview`, `.specializedTools`, `.aiDynamic` even though those zones have no word count semantics.
- The commit's code comment at line 1219–1220 acknowledges this: "Kept for chat zone (where word count makes sense). For other zones, the right side stays empty until ticket 015.013 wires real per-zone data."
- **Severity:** cosmetic + slightly inconsistent. Not a FAIL of any spec axis because ticket 015.012 scope is "restore bottom toolbar + per-zone status info"; the right-side artifact was pre-existing (tickets o09 / v0.22) and is explicitly deferred to ticket 015.013.
- **Spec axis verdict:** not a FAIL — pre-existing, out of scope for 015.012, and explicitly acknowledged in commit code.

---

## VERDICT: **accept**

All 10 spec axes (A–J) PASS. The two GAP items are partial / cosmetic / explicitly out-of-scope and deferred to follow-up tickets (015.013 or later). No spec FAIL.

Boss 8/25 third OOB "其它五区的底栏都丢了" is now resolved for the 5 non-chat zones. Chat zone retains its custom in-child bar per ticket 10 spec. Per-zone status info is wired per Boss plan A, with `.projectSidebar` using live data and the others using explicit placeholders (as ticket 015.012 scope dictates). Boss plan A reference is preserved verbatim in the commit body. The 30 PT height token is consistent across all 6 zones.

The implementation is faithful to the spec. Any follow-up cosmetic improvements (e.g. GAP-1 .aiDynamic fallback, GAP-2 right-side WordCountInlineLabel suppression) belong in ticket 015.013 (real data wiring) or a dedicated visual cleanup ticket.
