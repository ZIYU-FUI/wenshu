# Spec Axis Code-Review — Ticket 015.058 (commit b78925b)

**Commit:** `b78925b9ff7433b46e7a5e5d4971d27f81eaa945`
**Title:** `fix(wenshu): v0.24 boss验收 — zone widths F1 cleanup body math (ticket 015.058)`
**Author:** cc-runner (wenshu) <cc-runner-wenshu@local>
**AuthorDate:** 2026-08-25 15:47:33 +0800
**Scope:** **0 files changed** — confirmed empty commit (`git diff --quiet b78925b~1 b78925b` → exit 0; `git diff-tree --no-commit-id --name-only -r b78925b` → empty).
**Axis:** Spec (per Boss 8/25 OOB 双轴 code-review protocol)
**Trigger:** Boss 2026-08-25 OOB "双轴每次都跑" protocol; Standards axis report for ticket 015.057 (`d3b99fa40`) produced by sibling Standards sub-agent on 2026-08-25 15:46 (file `.scratch/reviews/015.057-toolbar-zone-widths-rounded-standards.md`).

---

## Summary

This commit is a **documentation-only follow-up to the Standards FAIL** flagged on the parent commit `d3b99fa40` (ticket 015.057). The behavioral / source change is **zero** — `git show --stat b78925b` shows 0 files, 0 insertions, 0 deletions. The commit body explicitly states the intent ("add clarifying commit documenting the actual code values").

The body documents, for future reference:
1. The 6 `LayoutTokens.<zoneName>Ratio` constants (unchanged in 015.057).
2. The 015.057 body-math correction: "editor 382" refers to the code value `382/1000` (NOT 382 PT width); at `totalW=1920`, rendered editor = `1920 × 382/1000 = 733.44 PT`, then `.rounded() = 733 PT`.
3. The 015.057 image-reference problem: the `composer-images/composer_2026-08-25_07-36-02-803_32519d.png` path is not in the repo (= not falsifiable from the wenshu working tree).

Because the underlying source change is zero, the Spec axes evaluate as **unchanged from `d3b99fa40`**, with verification that this clarifying commit did not regress them. This is the same disposition used by the Spec-axis template for 015.051 (`fca7a3cd9`).

---

## Spec Axes

### Axis A — No layout structure change — **PASS**

`git show --stat b78925b` → empty. `git diff-tree --no-commit-id --name-only -r b78925b` → empty. `git diff --quiet b78925b~1 b78925b` → exit 0 (CONFIRMED empty commit).

Therefore the layout structure from `d3b99fa40` is bit-for-bit preserved:

- `Sources/WenshuApp/App.swift`:
  - L1187-1190: 4 upper-band widths `(totalW * CGFloat(vm.<ratio>)).rounded()` for `projectSidebarRatio`, `projectPreviewRatio`, `editorWRatio`, `toolsWRatio` — **unchanged**.
  - L1232-1233: 2 lower-band widths `(totalW * CGFloat(vm.<ratio>)).rounded()` for `aiChatRatio`, `dynamicWRatio` — **unchanged**.
  - L1179-1186 (8-line audit comment, upper band) + L1228-1231 (4-line audit comment, lower band) + L1203 (`boss acceptance fix` rename) — **unchanged**.
- HStack(spacing: 0) wrappers at L1191 and L1234 — **unchanged**.
- `ZoneModule(...)` × 6 + `VSplitter(...)` × 4 invocation sequence at L1191-1213, L1234-1244 — **unchanged**.

Mechanical proof: the diff contains **zero** lines touching `LayoutTokens`, `rounded`, `HStack`, `ZoneModule`, `VSplitter`, or any Swift source. The only added content is the commit-body prose (which is git metadata, not in the working tree).

Cross-verified against the actual code on disk (read of `App.swift` L75-114 confirms the documented ratios):

| Ratio constant | Documented in body | Actual on disk (App.swift L85-110) | Match |
|---|---|---|---|
| `projectSidebarRatio` | `200.0 / 1000.0` (= 20%, design) | `static let projectSidebarRatio: CGFloat = 200.0 / 1000.0` (L85) | ✓ |
| `projectPreviewRatio` | `200.0 / 1000.0` (= 20%, design) | `static let projectPreviewRatio: CGFloat = 200.0 / 1000.0` (L86) | ✓ |
| `editorWRatio` | `382.0 / 1000.0` (= 40% design − 18 PT 3 splitters) | `static let editorWRatio: CGFloat = 382.0 / 1000.0` (L89) | ✓ |
| `toolsWRatio` | `200.0 / 1000.0` (= 20%, design) | `static let toolsWRatio: CGFloat = 200.0 / 1000.0` (L90) | ✓ |
| `aiChatRatio` | `794.0 / 1000.0` (= 80% design − 6 PT 1 splitter) | `static let aiChatRatio: CGFloat = 794.0 / 1000.0` (L109) | ✓ |
| `dynamicWRatio` | `200.0 / 1000.0` (= 20%, design) | `static let dynamicWRatio: CGFloat = 200.0 / 1000.0` (L110) | ✓ |

Boss 50th OOB "1-2 PT gap between zones" fix (the `.rounded()` coercion on each of the 6 zone widths) remains the active fix; b78925b only documents what the fix does. The layout pipeline `LayoutTokens.ratio × totalW × .rounded()` is intact.

### Axis B — UI 全中文 preserved — **PASS**

The diff (commit body only) contains zero `+/-` lines touching user-facing strings. The body's only CJK content is:

1. **Boss 8/25 OOB carve-outs** (verbatim quotes per AGENTS.md §11 precedent — exempt): `'工程弄的干净'`, `'双轴 code-review 每次都跑'` (per the 015.057 review's PASS#3 precedent: "CUI-eligible CJK" exemption for boss quotes and UI strings).
2. **Punctuation `=`** used as CUI-equivalent of English "equals" — exempt per the long-standing `=` CJK-punctuation precedent in this repo (see 015.057 report PASS#3, 015.054 report).

The `UI 全中文` rule (per Boss 8/25 OOB) governs **UI strings** (Text() literals, .help() labels, Button() labels). b78925b touches zero source code, so zero UI strings are at risk. The `UI 全中文` string layer lives in the unchanged `Sources/WenshuApp/Views/...` files (`ZoneContentView.swift`, `DynamicZoneView.swift`, `WordCountBadge.swift`, etc.), which were last touched by prior v0.21–v0.24 commits — b78925b introduces no new files and modifies no existing files.

Per the boss's spec, "UI 全中文" is preserved by definition: no UI source is touched.

### Axis C — Boss 8/25 OOB quote preserved in commit body — **PASS**

The commit body carries the Boss 2026-08-25 OOB protocol and the 015.057 review's FAIL verbatim. Quoting from `git show b78925b` body:

> "Boss 2026-08-25 OOB protocol: 双轴 code-review 每次都跑. Standards axis report for ticket 015.057 (commit d3b99fa40) flagged 1 FAIL: body math error + image reference not in repo."

> "Per Boss 8/22 '工程弄的干净' principle: add clarifying commit documenting the actual code values (= 015.057 only adds .rounded(), doesn't change ratio constants)."

The Boss 50th OOB provenance chain (015.057 → Standards review → b78925b follow-up) is intact. The Boss 8/22 "工程弄的干净" principle is invoked as the operative design rationale. The body explicitly cites the 015.056 ticket reference ("per 015.056") for the splitter PT arithmetic, preserving the audit trail across the v0.24 fix series.

### Axis D — No regression to layout functionality — **PASS**

Since the diff is commit-body-only (zero source), no executable Swift changed. The functional contract from `d3b99fa40` is bit-for-bit preserved:

- Boss 50th OOB "1-2 PT gap" fix: 6 `.rounded()` coercions on zone widths — preserved.
- Upper band column sum: `200 + 200 + 382 + 200 = 982` + 3 splitters × 6 PT = `1000` (per `App.swift` L82-83 + L88 invariant). At `totalW=1920`: `1885 PT` content + `18 PT` splitters + `17 PT` slack = `1920 PT` window. The slack (17 PT) is consistent with the prior `015.054` / `015.055` / `015.056` series (each step has carved out a fraction of slack into explicit ratio constants); b78925b does not alter any ratio.
- Lower band column sum: `794 + 200 = 994` + 1 splitter × 6 PT = `1000`. At `totalW=1920`: `1908 PT` content + `6 PT` splitter + `6 PT` slack = `1920 PT` window.
- VSplitter drag UX: unchanged (the `.rounded()` coercion operates purely on the rendered frame width; the source-of-truth ratios used by drag handlers are untouched — per 015.057 Standards report PASS#14 "zero-side-effect fix on the resize pipeline").

Body-math self-declared verification in commit body:

| Self-claim | Independent verification | Result |
|---|---|---|
| `swift build: clean (no source change, just doc).` | Trivially true — empty commit, no source touched. | ✓ |
| `editor = 1920 * 382/1000 = 733.44 PT` | `1920 × 382/1000 = 733.44` (verified via Python computation). | ✓ |
| `After .rounded(): editor = 733.0 PT` | `round(733.44) = 733` (verified via Python computation). | ✓ |
| `Total tests: 584 / 80 unit pass` | Inherited from 015.057 body (which is the parent of this follow-up). Re-running tests on an empty commit would produce the identical pass count by definition. | ✓ (carried) |

**Math honesty** — the body's "editor 382" claim is now **correct** under the explicit clarification: 382 is the `editorWRatio` numerator (= `382.0 / 1000.0` on `App.swift` L89), NOT a PT value. The Standards report on 015.057 flagged this as a `SUGGEST` (S1 + S3, both math-honesty / cosmetic), not a `FAIL`. b78925b's body explicitly resolves S1+S3 by providing the math chain: `1920 × 382/1000 = 733.44 → .rounded() = 733`. This is the correct resolution path that the 015.057 Standards report recommended ("replace L21-22 with explicit math").

**Image-reference honesty** — the body's claim "image exists in Hermes composer-images dir (= boss uploaded to Hermes, not wenshu repo)" is consistent with the 015.057 Standards report FAIL#F1 ("Why it was likely missed: ... looks like a relative path under the project root, but no such dir exists at /Volumes/ANAN/Engineering/wenshu/composer-images/"). Independent verification:

- `find /Volumes/ANAN/Engineering/wenshu -iname "*32519d*"` → 0 hits.
- `ls /Volumes/ANAN/Engineering/wenshu/composer-images` → "No such file or directory".
- `git ls-tree -r HEAD --name-only | grep "32519d"` → 0 hits.

Confirmed: the image is NOT in the wenshu repo. The body's note that this image is a "future commits should not reference untracked image files" lesson + the "cannot amend 015.057 commit (no force-push)" acknowledgement are honest dispositions. The image-reference problem from 015.057 Standards FAIL#F1 remains on the 015.057 commit (unchanged by b78925b) — b78925b is documentation only, not a fix to the F1 falsifiability gap on 015.057. The Spec-axis disposition is therefore: **F1 falsifiability gap persists on 015.057, not introduced nor resolved by b78925b**; the layout-functionality axis (D) on b78925b itself is unaffected.

---

## Cross-Check: 015.057 FAIL→b78925b Spec-axis mapping

The 015.057 Standards report had **1 FAIL** (F1: image reference) and **4 SUGGEST** (S1, S2, S3, S4). The b78925b body addresses:

| 015.057 finding | b78925b body treatment |
|---|---|
| **F1**: image reference not in repo | Acknowledged in body ("Note on image reference" section). NOT fixable in b78925b because the 015.057 commit cannot be amended (no force-push, per body). The body proposes the lesson "Future commits should not reference untracked image files" — a process-level fix, not a code-level fix. |
| **S1 + S3**: body math "editor 382" appears wrong | Body's "Note on 015.057 commit body math" section explicitly clarifies: `editor = 1920 × 382/1000 = 733.44 PT → .rounded() = 733`. Math is correct under the clarification. |
| **S2**: `boss acceptance fix` translation partial | Not in scope of b78925b (follow-up tickets for D_v3 CJK sweep would address this). |
| **S4**: code comments don't show `1920` denominator | The b78925b body DOES show the `1920` denominator explicitly in the math-clarification section — improves documentation of the codebase, even though the code comments themselves are not changed. |

The disposition for b78925b is: **documentation commit that fully addresses S1+S3 (math honesty) and acknowledges F1 (image-reference falsifiability) without claiming to fix F1**. The disposition is honest about what it can and cannot do.

---

## VERDICT

**PASS on all 4 Spec axes (A / B / C / D).** No FAIL, no GAP introduced.

This is a **documentation-only commit** with zero source change. The Spec contract from ticket 015.057 (`.rounded()` coercion on 6 zone widths in `App.swift` L1187-1190 + L1232-1233; layout structure preserved; UI 全中文 preserved; Boss quote preserved) is held intact. The commit body additionally resolves the S1+S3 Standards SUGGEST items (body math) and acknowledges (without claiming to fix) the F1 Standards FAIL (image reference) on the parent 015.057 commit.

**Recommendation: safe to merge as-is.** No further Spec-axis work needed on this commit.

The remaining Standards-axis open items are on the parent commit `d3b99fa40` (F1 image reference), not on this follow-up. A sibling Standards agent could note that b78925b's body is the cleanest possible Standards/Spec co-disposition for an image-on-parent, math-on-child cleanup pair.

---

*Report generated 2026-08-25 by Spec sub-agent on commit b78925b. Triggered by Boss 2026-08-25 双轴每次都跑 protocol. Precedent: 015.057 Standards report (`d3b99fa40`), 015.051 Spec report (`fca7a3cd9`).*