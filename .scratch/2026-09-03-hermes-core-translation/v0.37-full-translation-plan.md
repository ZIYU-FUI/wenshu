# v0.37 Full Translation Plan + Frontend Flow + Visual Verification Packet

= Wenshu · 2026-09-03 · pocock single-agent

> Generated 2026-09-03 per 老板 cadence '暂时无法测试,想让 把翻译这个事
> 做完一起验视觉和前端流程'.
>
> Scope: 把 hermes core translation (= Scope B per ADR-0012) 全部完成
> + v0.37 candidates 实施 + 前端流程集成 + ship-ready visual
> verification packet, **让老板一次性 visual + flow verify when
> Mac accessible (= no incremental verification needed)**.

## 1. Definition of "Done"

Per 老板 cadence 2026-09-03:
- 老板 temporarily cannot test visually
- 老板 wants: hermes core translation 100% 完成
- 老板 wants: v0.37 集成到 前端 flow
- 老板 wants: ship-ready for一次性 visual + frontend flow verify

Done means:
1. hermes core translation scope B = 100% (per ADR-0012)
2. test target = 0 compile errors
3. 前端 flow 集成完成 (= wenshu 实际走 ConversationLoop + 7-connector + MemoryManager + SkillRegistry, 不走 legacy WenshuVerifier)
4. ship-ready visual verification packet (= 老板 can run launch + verify 一次性)
5. v0.37 candidates A + B + C 实施 (= per `v0.37_roadmap.md` recommendation)

## 2. Work Breakdown (= 6 batches, ~30 commits)

### Batch 1: Test target cleanup (= 8 commits, ~2 hours)

Per session 25 (= 24 commits fixed 121 → 35 errors). Remaining 35 errors.

| Sub-batch | Files | Errors |
|---|---|---|
| 1.1 My comprehensive tests | ComprehensiveInterfaceTests.swift | 6 (= ProposalKind.allCases, ChatMessage.Role, Provider.openai → minimaxCn, DesignTokens @MainActor, LLMCallOptions arg order) |
| 1.2 ProviderKeychain tests | ProviderKeychainTests.swift + ProviderKeychainMetadataTests.swift | ~8 (= session 25 known + Provider/ProviderKeychainMetadataTests path collision) |
| 1.3 File / Process tools | FileToolsTests + + ProcessToolsTests | ~6 (pre-existing v0.30遗留) |
| 1.4 Store tests | KanbanStoreTests + MemoryStoreTests + SkillRegistryTests | ~10 (pre-existing v0.30遗留) |
| 1.5 Provider tests | ProviderCatalogTests + ProviderFetcherTests | ~5 |

**Exit criteria**: `swift build --target WenshuAppTests` = 0 errors.

### Batch 2: 5 ADR-backed v0.36 deferred items (= 12 commits, ~6 hours)

Per `v0.37_backlog.md` Section 1.

| # | Item | Effort | Scope |
|---|---|---|---|
| 2.1 | Real hermes end-to-end agent dispatch (= ticket 018 sub-step 3) | 4 commits | Real ConversationLoop.run() with mock tools, ReadFile → WriteFile → FinalResponse end-to-end test |
| 2.2 | L30 thinking blocks in AnthropicConnector | 2 commits | .thinking case in LLMBlock, + Anthropic streaming wire thinking blocks |
| 2.3 | Runtime golden file tests for all 11 tickets | 3 commits | generate_golden.py enhancement + Swift parity tests per ticket |
| 2.4 | RuntimeCWD UI integration | 1 commits | Display RuntimeCWD.label in editor zone toolbar |
| 2.5 | Per-ticket X e2e with real API | 2 commits | Mock-server based X e2e tests for all 11 tickets (= behind flag) |

### Batch 3: v0.37 candidates A + B + C (= 8 commits, ~4 hours)

Per `v0.37_roadmap.md` recommendations.

| Candidate | Scope |
|---|---|
| **A** Wire ConversationLoop into wenshu.app main thread | 3 commits |
| 3.1 ChatViewModel creates ConversationLoop at init |
| 3.2 ChatView.send() routes through loop instead of WenshuVerifier |
| 3.3 Manual Compress button delegates to ContextEngine |
| **B** MemoryManager.prefetch integration | 2 commits |
| 3.4 MemoryRetrievalPanel triggers MemoryManager.prefetch on appear |
| 3.5 MemoryAdapter writes through MemoryManager on save |
| **C** Settings → Agent 3-pane wire-up | 3 commits |
| 3.6 LibraryPropertiesView embeds AgentSettingsView |
| 3.7 LLM Connector tab loads ConnectorProfileRow list |
| 3.8 Memory tab loads MemorySettingsView with real MemoryManager |

### Batch 4: Frontend flow integration (= 6 commits, ~3 hours)

The critical 6 user-facing flows per spec §6.4.

| Flow | Scope |
|---|---|
| 4.1 Onboarding → 7-connector config | 1 commits |
| 4.2 Chat send → LLM Connector → Response | 1 commits |
| 4.3 Compression pill → ContextEngine.aggregateContextForTurn | 1 commits |
| 4.4 MemoryRetrievalPanel → MemoryManager.prefetch | 1 commits |
| 4.5 Skills slash-command → SkillRegistry.invoke | 1 commits |
| 4.6 AgentSettingsView → LibraryPropertiesView integration | 1 commits |

### Batch 5: Visual verification packet (= 3 commits, ~2 hours)

**Critical = boss cadence '1 RULE 1 commit' + 您 '1' visual verify packet**.

| Item | Scope |
|---|---|
| 5.1 Update v0_36_visual_verify_test.swift → v0.37_visual_verify_test.swift | 1 commits |
| 5.2 Add 11 smoke tests for v0.37 candidates A + B + C | 1 commits |
| 5.3 Generate "老板 visual verify flow guide" markdown | 1 commits |

**Exit criteria**: v0.37_visual_verify_test.swift contains 19+ smoke tests (= 6 original + 11 new + 2 onboarding).

### Batch 6: Final integration + ship packet (= 4 commits, ~2 hours)

| Item | Scope |
|---|---|
| 6.1 CHANGELOG.md v0.37 (= update from v0.36 → v0.37 with 30 commit summary) | 1 commits |
| 6.2 ADR-0013 v0.37 scope (= record conversation loop integration + 7-connector wire-up decisions) | 1 commits |
| 6.3 Bump AGENTS.md + CLAUDE.md to v0.09 (= reflect new integration decisions) | 1 commits |
| 6.4 Final code-review sweep (= /code-review sub-agents, clean Standards + Spec axes) | 1 commits |

**Exit criteria**: v0.37 = ship-ready, 老板 can verify一次性 when Mac accessible.

## 3. Frontend Flow Verification Checklist

Per 老板 cadence '一起验视觉和前端流程' (=一次性 verify visual + flow).

### 3.1 Onboarding flow

1. Launch wenshu.app fresh (= delete /Users/anbaiqiang/.wenshu/libraryPath)
2. NSOpenPanel for .ws bundle selection
3. Enter Settings → LLM Connector
4. Pick 1 connector (= minimax-cn for first test)
5. Enter API key (= real or test)
6. Click "Test" → see success indicator
7. Save → see Settings pane close

**Pass criteria**: 7 connectors available, test button shows success, settings persist.

### 3.2 Chat end-to-end flow

1. Open chat (= no existing messages)
2. Type message → press Enter
3. See compression pill appear at ~3000 tokens
4. Send → see streaming response
5. See tool_use block (= ReadFile) appear in response
6. See tool_result (= file content) follow
7. See final assistant response
8. Compression pill shows "💡 N tokens used"

**Pass criteria**: chat goes through ConversationLoop (not WenshuVerifier), compression pill accurate, tool use works.

### 3.3 Memory retrieval flow

1. Open DynamicZone (= 看板 or 待办 tab)
2. See MemoryRetrievalPanel appear at right-bottom
3. See entries with relevance score (= 0.0 - 1.0)
4. Click entry → source file shown
5. See entries refresh after chat turn (= MemoryManager.prefetch)

**Pass criteria**: panel visible, entries accurate, refresh on chat.

### 3.4 Skills flow

1. Open Settings → Skills
2. See skill list (= all enabled skills)
3. Type slash-command in chat (= /compress)
4. See skill invoker run
5. See result in chat (= compressed message)

**Pass criteria**: skill list shown, slash-command works.

### 3.5 Settings 3-pane flow

1. Open Settings (= Cmd+,)
2. See 3 tabs: LLM Connector / Memory / Skills
3. Switch tabs → see different content
4. Edit settings → see save indicator
5. Close settings → see persistence

**Pass criteria**: 3 tabs work, settings persist.

### 3.6 Compression manual flow

1. Build chat history to 5000+ tokens
2. See compression pill appear
3. Click "Compress" button
4. See history shrink to <3000 tokens
5. See "💡 Compressed to N tokens" message

**Pass criteria**: manual compress works, history accurate.

## 4. Estimated Total

- 30 commits across 6 batches
- ~17 hours of work (per commit ~30 min)
- 老板 verify time: ~30 min (=一次性 visual + flow)

## 5. Boss Validation Decision

Per 老板 cadence '暂时无法测试,想让 把翻译这个事做完一起验视觉和前端流程',
this plan = full scope of "做完" (= 一次性 verify ready).

老板 validation options:
1. **Approve full plan (= 30 commits)** — I proceed per 6 batches.
2. **Reduce scope (= pick high-impact subset)** — I do selected batches only.
3. **Modify plan (= change priorities)** — 老板 specifies modifications.

## 6. Risks + Mitigations

| Risk | Mitigation |
|---|---|
| 35 test errors deeper than estimated | Each error gets individual fix commit (= traceable) |
| v0.37 candidates A + B + C more complex than expected | Each candidate = 2-3 sub-commits (= isolated failures) |
| Frontend flow integration breaks existing flows | Each flow = 1 commit + integration test |
| 老板 visual verify finds issues | All commits atomic, easy revert |
| Visual packet incomplete | 19+ smoke tests + flow guide = comprehensive coverage |

## 7. Per-Batch Commit Pattern

Per boss cadence '1 RULE 1 commit' + 您 'PO 全链路方法论执行,不要跳步骤':

Each batch = N commits, each commit = 1 RULE 1 commit:
- Build verification (= swift build exit 0)
- Test verification (= relevant tests pass)
- CHANGELOG entry (= if user-visible)

Commit message format:
```
[batch.sub-step] type(scope): [RULE 1 sentence summary]
+ [RULE 1 detail 1]
+ [RULE 1 detail 2]
swift build exit 0.
```

## 8. Auto-Pilot Status

Per 老板 cadence 'auto-pilot 全开' (= session 19+):

- I proceed without per-commit approval
- I do all 30 commits across 6 batches
- I report progress every ~5 commits (= progress updates)
- Final report = ship packet ready for 老板 一次性 verify

## 9. Final Visual Verify Guide (= for 老板 Mac accessible later)

When 老板 can verify visually:

1. **Launch wenshu.app** (= `swift run WenshuApp` or build/Wenshu.app)
2. **Allow Keychain prompt** on first launch
3. **Run through 6 frontend flows** (= Section 3.1 - 3.6 above)
4. **Check 19+ visual verification items** (= per v0.37_visual_verify_test.swift + Section 3.7 in CHANGELOG)

If 老板 finds issues:
- File bug (= which flow + expected vs actual)
- I fix in next session (= hotfix mode)

If 老板 approves:
- Push v0.37 to origin (= 老板 runs per mem0 'push 不归 ANAN 管')
- Done.

---

*Plan v0.37 · 2026-09-03 · pocock single-agent · wenshu full-translation + frontend integration + ship-ready visual packet · English-only + 老板 sole address per AGENTS.md §12*