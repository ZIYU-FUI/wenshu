# v0.37 Backlog Report · Wenshu · 2026-09-03

> Generated 2026-09-03 by wenshu auto-pilot (= pockoc profile + PO 6 步方法论).
> Purpose: consolidate all deferred v0.36 items + new candidates for v0.37
> direction decision. Per boss cadence '不擅自抢跑 + 等我拍', this report
> is READ ONLY = 老板拍 direction before v0.37 work begins.

## v0.36 deferred items (= continue from v0.36 ship)

These are items that v0.36 explicitly deferred (= not blocking v0.36 ship,
but visible in CHANGELOG.md "v0.36 backlogs still deferred" section).

### 1. Real hermes end-to-end agent dispatch (= ticket 018 sub-step 3)

- **Current state**: ticket 018 sub-step 2 ships SIMULATED X e2e harness
  (= same deterministic sequence hard-coded in both hermes and wenshu
  runs). Real end-to-end requires:
  - hermes Python venv setup (= `import agent` fails without
    `PYTHONPATH=/Volumes/ANAN/.hermes/agent` + hermes deps installed)
  - wenshu Swift ConversationLoop actor wired into wenshu.app main thread
    (= currently lives in `Core/Agent/Conversation/ConversationLoop.swift`
    as standalone actor; not yet invoked by ChatViewModel.send())
  - Real Anthropic API key (= BYOK pattern per ADR-0008)
- **Effort estimate**: 1-2 sessions (= hermes venv setup + dispatch
  wiring + X e2e real harness)
- **Scope**: Spec §6.2 X e2e dual-track + ticket 001 L57 manual e2e
  acceptance criterion

### 2. L30 thinking blocks in AnthropicConnector

- **Current state**: AnthropicStreaming.swift ships decoder + tool_use
  collector + thinking block CASE in LLMBlock; AnthropicStreamingWireup
  actor wires EventSource. But the actual `AnthropicConnector.send()`
  method does NOT route thinking blocks through the streaming path.
- **Pre-existing gap from**: ticket 004 sub-step 1 (= spec re-review
  L30 flagged as deferred to AnthropicConnector itself)
- **Effort estimate**: 1 session (= AnthropicConnector refactor)
- **Scope**: ticket 004 L30 + ticket 001 wire-up completion

### 3. Runtime golden file tests for all 11 tickets

- **Current state**: ticket 018 sub-step 1 ships 5 representative golden
  fixtures (= message_sanitization.extract_text x2, context_compressor
  .count_tokens_rough, context_breakdown.analyze, rate_limit_tracker
  .check_budget). Missing golden fixtures for: ticket 001 (= conversation
  loop), ticket 002 (= prompt_caching), ticket 003 (= context
  compressor beyond count_tokens), ticket 004 (= AnthropicConnector
  beyond thinking blocks), ticket 005 (= OpenAIConnector), ticket 006
  (= connector settings UI), ticket 007 (= Gemini), ticket 008 (=
  WenshuModelCatalog), ticket 009 (= MemoryAdapter.prefetch), ticket
  010 (= SkillAdapter.invoke).
- **Effort estimate**: 2-3 sessions (= ~6-10 golden fixtures + tests
  per ticket)
- **Scope**: spec §6.1 Z contract coverage at full depth

### 4. RuntimeCWD UI integration

- **Current state**: ticket 017 ships pure data actor (= CWD tracker
  with override + library path fallback + path resolution + UserDefaults
  persistence). Settings → Library Properties panel UI binding deferred.
- **Effort estimate**: 0.5 session (= add a Picker to existing
  LibraryPropertiesView + wire RuntimeCWD.shared singleton)
- **Scope**: ticket 017 UI activation

### 5. Per-ticket X e2e with real API

- **Current state**: ticket 018 sub-step 2 ships SIMULATED harness. Real
  X e2e = run hermes Python + wenshu Swift side-by-side on 3-turn prompt
  with real API key (= ticket 001 L57 manual e2e acceptance).
- **Effort estimate**: 1 session (= requires user API key + hermes venv)
- **Scope**: ticket 001 L57 final acceptance

## v0.37 candidate tickets (= new work)

These are new tickets that v0.37 could focus on. 老板 must pick 1+ direction.

### A. Wire real ConversationLoop into wenshu.app main thread

- **Why**: Currently ChatViewModel.send() invokes WenshuVerifier
  (= legacy path). ConversationLoop actor exists but is not wired.
- **Effort**: 2-3 sessions (= ChatViewModel refactor + ConversationLoop
  invocation + ToolExecutor integration + new ConversationStore
  actor for SQLite chat history persistence per AGENTS.md §11)
- **Impact**: High (= v0.37 user-visible = chat actually routes through
  LLMConnector + PromptCaching + Tool execution)
- **Risk**: Medium (= touches chat hot path; could break existing
  WenshuVerifier fallback)

### B. Real ConversationEngine + MemoryManager.prefetch integration

- **Why**: ticket 003 shipped ContextEngine facade but it doesn't
  actually invoke MemoryManager.prefetch. Memory subsystem is largely
  stubbed.
- **Effort**: 1-2 sessions
- **Impact**: Medium (= enables memory-aware LLM context)
- **Risk**: Low

### C. Connector settings UI integration (ticket 006 activation = act-2)

- **Why**: ticket 006 shipped LLMConnectorSettingsView + ConnectorAuthField
  + ConnectorTestButton + LLMConnectorSettingsViewModel, but the
  Settings scene doesn't include an "Agent" tab. Per act-2 (= session 8
  AgentSettingsView commit), the 3-pane view exists but is not yet
  wired into Settings.
- **Effort**: 1 session (= add to App.swift Settings scene + wire
  AgentSettingsView as NavigationLink)
- **Impact**: High (= v0.36 promised act-2 activation but it's NOT yet
  reachable from Settings menu)
- **Risk**: Low (= append-only NavigationLink)

### D. Editor zone full Markdown support

- **Why**: ticket 022 (v0.25.1) shipped editor skeleton. Full
  Markdown rendering (= headers, bold, code, wikilinks) per spec
  v0.34 ticket 027-35 deferred.
- **Effort**: 2-3 sessions
- **Impact**: High (= core writing tool feature)
- **Risk**: Medium

### E. EPUB import (= approved third-party lib `witekbobrowski/EPUBKit 0.5.0`)

- **Why**: AGENTS.md §11.1 approved EPUBKit but no ticket scopes it.
  Writing tool = should support EPUB import (= 行业 standard).
- **Effort**: 2 sessions
- **Impact**: High (= feature for novelists who import existing manuscripts)
- **Risk**: Low

### F. Background tasks UI (= wire DisplayStateMachine + BackgroundReview to Settings)

- **Why**: ticket 016 shipped 4 background components but they don't
  have a Settings panel yet (= no way for user to see credits / review
  proposals / view state).
- **Effort**: 1-2 sessions
- **Impact**: Medium
- **Risk**: Low

### G. Search & retrieval across library (v0.34 deferred)

- **Why**: DynamicZoneView had 3rd tab "搜索" (search) but boss 8/24
  OOB removed it. Per spec §11, library-wide search is core.
- **Effort**: 3-4 sessions (= SearchService actor + LLMIndexingService
  + BM25 OR vector search + SwiftUI search results UI)
- **Impact**: High
- **Risk**: High

### H. Multi-window / multi-library support (= v0.34 deferred)

- **Why**: AGENTS.md §11 baseline = single-shelf model (= single .ws
  library). Multi-window + multi-library = future.
- **Effort**: 5+ sessions (= BookStore refactor + Library switcher +
  cross-library references)
- **Impact**: High
- **Risk**: High

## v0.37 direction recommendation

Per boss cadence "1 RULE 1 commit" + PO 6 步方法论 + "等你拍" + auto-pilot
"继续推进目标", my recommendation:

**v0.37 = 3-month scope, focus on shipping ONE complete vertical = chat
that actually works end-to-end (= candidate A + B + C)**.

Rationale:
- v0.36 shipped 60 commits of plumbing (= LLMConnector / PromptCaching /
  ContextCompressor / MemoryAdapter / SkillAdapter) but the chat path
  still routes through legacy WenshuVerifier (= pre-v0.36 code path).
- Without candidate A (= wire ConversationLoop), the v0.36 plumbing is
  unreachable from the user (= user can't actually chat with an LLM
  through the new code path).
- Without candidate C (= Settings → Agent 3-pane), the act-2 activation
  is unreachable from the Settings menu.
- Candidate B (MemoryManager.prefetch) is the "v0.36 22nd backlog item"
  (= ticket 009 adapter ships but no real LLM call uses it).

v0.37 backlog priority (= if 老板 accepts recommendation):
1. Candidate A (= wire ConversationLoop into wenshu.app main thread)
2. Candidate C (= wire Settings → Agent 3-pane)
3. Candidate B (= MemoryManager.prefetch integration)
4. Candidates D-H deferred (= scope creep for v0.37)

## v0.37 alternatives (= if 老板 prefers different direction)

- **Alt 1: v0.37 = visual verify + push + ship** (= minimal new work;
  just make v0.36 shippable and stable)
- **Alt 2: v0.37 = polish v0.36** (= 1 ticket per v0.36 backlog item;
  lower risk, higher polish)
- **Alt 3: v0.37 = new feature focus** (= pick candidate D-H; ship new
  capability without plumbing)
- **Alt 4: v0.37 = 老板 visual verify only** (= no code; 老板 manually
  verifies v0.36; report any fix needs; fix = v0.37)

## 老板 decision required

Per boss cadence "等你拍" + mem0 "push 不归 ANAN 管", 老板 must拍:

1. **v0.36 push decision**: 老板 visual verify then push (= run push
   commands in CHANGELOG.md v0.36 "v0.36 known issues" section) OR
   老板 reject v0.36 (= list specific fix needs)
2. **v0.37 direction**: pick from candidates A-H OR alt 1-4 above
3. **Defer items priority**: if 老板 accepts "polish v0.36" alt 2, pick
   which deferred items (= 1-5) to address first

## v0.37 ticket draft template

For 老板-picked direction, ticket draft will follow this template:

```
# ticket NNN: <title>

**Why**: <problem statement referencing CHANGELOG.md v0.36 backlog or v0.37 candidate>

**What to build**: <concrete deliverable>

**Acceptance criteria**:
- [ ] <observable milestone 1>
- [ ] <observable milestone 2>
- [ ] `swift build` exit 0
- [ ] `swift test` exit 0 (= Z contract tests for new code)

**Iron rules applied** (= check before commit):
- [ ] Rule 1: git grep BEFORE patch (= 0 callers for any symbol to delete)
- [ ] Rule 6: layout/spacing uses DesignTokens (= no magic numbers)
- [ ] Rule 7: Button + system buttonStyle (= no custom-drawn icons)
- [ ] Rule 8: stays inside WindowGroup scene tree (= no new NSWindow)
```

Per boss cadence "1 RULE 1 commit" + PO 6 步方法论.

---

*Generated 2026-09-03 by wenshu auto-pilot (= pockoc profile + PO 6 步方法论).*
*English-only per AGENTS.md hard rule. Sole address = 老板.*
*Per mem0 "push 不归 ANAN 管" + "不擅自抢跑 + 等我拍" = 老板 must拍 before any push or v0.37 work begins.*