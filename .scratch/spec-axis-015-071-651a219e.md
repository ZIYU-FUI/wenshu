# Spec axis code-review — commit 651a219e2 (ticket 015.071)

**Date**: 2026-08-26
**Reviewer**: Spec axis subagent (subagent)
**Commit**: `651a219e2c32ee4d007a0960b55a0e0b7cadda1a`
**Branch**: `wt/multi-agent-dispatch`
**Scope**: 1 file changed, 12 insertions, 0 deletions (Sources/WenshuApp/App.swift)
**Boss 8/25 67th OOB quote (verbatim)**: `'效果对了, 这块代码不用动. 但收起后其它区用尺寸的重新分配占用宽度有问题, 需要解决'`

## File verified

`Sources/WenshuApp/App.swift` — read L1260–L1336 (UpperBandZone view body):

- **L1287** `ZoneModule(.projectSidebar) .frame(width: sidebar) .layoutPriority(0)` — inside `if showProjectSidebar { ... }`
- **L1305–1307** `ZoneModule(.projectPreview) .frame(width: preview) .layoutPriority(1)` — always renders
- **L1311–1313** `ZoneModule(.editor) .frame(width: editor) .layoutPriority(1)` — always renders
- **L1323–1325** `ZoneModule(.specializedTools) .frame(width: tools) .layoutPriority(0)` — inside `if showSpecializedTools { ... }`

All 4 layoutPriority sites match the diff exactly.

---

## Spec axes (Boss 8/25 67th OOB)

### Axis A — `ZoneModule(sidebar) + .layoutPriority(0)` (= keep current size)

**Status**: **PASS**

- File L1285–1287: `ZoneModule(slot: .projectSidebar, ...) .frame(width: sidebar) .layoutPriority(0) // explicit (= keep current size, don't grow)`
- Comment matches spec intent verbatim.
- Placed inside `if showProjectSidebar { ... }` so sidebar is toggleable; layoutPriority(0) = default, so it won't fight preview/editor for extra HStack space when sibling zones are visible.
- Apple's HIG layoutPriority semantics: priority(0) = default; lower priority = gets shrunk first when group has no spare space; not preferred when group has extra space.
- **PASS**: matches Boss "keep current size".

### Axis B — `ZoneModule(preview) + .layoutPriority(1)` (= grow to fill)

**Status**: **PASS**

- File L1305–1307: `ZoneModule(slot: .projectPreview, ...) .frame(width: preview) .layoutPriority(1) // grow to fill HStack when sidebar/tools hidden`
- Apple HIG layoutPriority: priority(1) > priority(0), so when HStack has extra space (e.g. sidebar OR tools hidden), preview stretches to consume it.
- Preview already pre-computed its width via `sidebarExtraRatio * 0.254 + toolsExtraRatio * 0.254` on L1264 (Boss 66th OOB pre-computed redistribution); layoutPriority(1) makes the redistribution work as designed.
- **PASS**: matches Boss "其它区 … 重新分配占用宽度".

### Axis C — `ZoneModule(editor) + .layoutPriority(1)` (= grow to fill)

**Status**: **PASS**

- File L1311–1313: `ZoneModule(slot: .editor, ...) .frame(width: editor) .layoutPriority(1) // grow to fill HStack when sidebar/tools hidden`
- Apple HIG: same semantics as preview. Editor pre-computed via `sidebarExtraRatio * 0.492 + toolsExtraRatio * 0.492` on L1266 (the larger share — 49.2% of redistribution).
- **PASS**: matches Boss spec.

### Axis D — `ZoneModule(tools) + .layoutPriority(0)` (= keep current size)

**Status**: **PASS**

- File L1323–1325: `ZoneModule(slot: .specializedTools, ...) .frame(width: tools) .layoutPriority(0) // explicit (= keep current size, don't grow)`
- Placed inside `if showSpecializedTools { ... }`; tools is toggleable; priority(0) prevents tools from stealing width from preview/editor when shown.
- **PASS**: matches Boss spec.

### Axis E — UI 全中文 preserved

**Status**: **GAP (not regressed, but worth noting)**

- This commit is a code-only change (`.swift` file only) — no UI strings added/changed.
- Diff does NOT touch any UI string. Existing UI Chinese strings in the project are not affected by this commit.
- The `015.071` ticket is layout/infrastructure, not UI text.
- **GAP clarification**: the Boss "UI 全中文" requirement applies to user-facing UI text. This commit adds only SwiftUI layout modifiers + English comments. The 4 inline comments on L1287/1307/1313/1325 are correctly in English per AGENTS.md §11.
- **VERDICT**: not a FAIL — Boss UI 中文 constraint is not violated (no UI strings touched), and AGENTS.md English-only rule for code comments is honored.

### Axis F — Boss 8/25 OOB quote preserved in commit body

**Status**: **PASS**

- Commit body L2: `Boss 2026-08-25 67th OOB: '效果对了, 这块代码不用动. 但收起后其它区用尺寸的重新分配占用宽度有问题, 需要解决'.`
- Verbatim match with the Boss quote in the task spec.
- Quoted with single-quotes inside the commit body (matches the project protocol of preserving Boss's exact wording).
- **PASS**: Boss quote preserved.

---

## Cross-reference: Boss 8/25 67th OOB 4-clause decomposition

| Boss clause | Spec axis | Status |
|---|---|---|
| 1. "收起后其它区重新分配宽度, 盛满上半区" | A + B + C + D (layoutPriority redistribution) | **PASS** |
| 3. "效果对了" | (animation = 015.066/015.070, unchanged by this commit) | **PASS (preserved)** — file shows `.animation(.easeInOut(duration: 0.25), value: showProjectSidebar)` + `.animation(.easeInOut(duration: 0.25), value: showSpecializedTools)` still in place on L1334–1335 |
| 4. "这块代码不用动" (= don't change toggle wiring) | Toggle wiring intact — `if showProjectSidebar { ... }` and `if showSpecializedTools { ... }` blocks unchanged in structure | **PASS** — only 4 lines added (one per ZoneModule) all are `.layoutPriority(N)` modifiers; no toggle state, no @AppStorage, no .animation touched |

---

## Standards cross-check (incidental finding, not blocking)

- The 4 new Swift comments on L1287/1307/1313/1325 are all English — **honors AGENTS.md §11**.
- The commit body contains 4 short Chinese fragments (`'效果对了'`, `'这块代码不用动'`, `'但收起后其它区用尺寸的重新分配占用宽度有问题'`, `'需要解决'`, plus `Apple官方推荐`) quoting the Boss verbatim. This is **inconsistency** with AGENTS.md §11 English-only rule for "All commit messages … comments … prompts".
  - However: this is **established project pattern** (many prior commits in `git log` also embed Boss's Chinese quotes verbatim — see 015.066/015.068/015.070 commit bodies). The project has chosen to preserve Boss's original Chinese for fidelity over the English-only rule.
  - The Code-review axes block at the end of the commit body explicitly cites "Standards: AGENTS.md §11 English-only (commit body + comments)" — so the author is aware of the rule but treats Boss verbatim quote as exception.
- Not a Spec-axis FAIL — Spec axes A–D + F are the formal review axes. This is logged as an observation.

---

## Verification artifacts referenced in commit body

- swift build: clean (commit body claim — not re-verified by this Spec axis review)
- App PID 50455 relaunched (commit body claim)
- computer_use capture confirmed: tools hidden, preview + editor GREW (commit body claim)

This Spec axis review **does NOT re-verify** the runtime/visual effect — that is the **Standards axis**'s job. Spec axis = code & spec correctness only.

---

## VERDICT

**Spec axis: PASS (with 1 informational GAP on Axis E)**

| Axis | Result |
|---|---|
| A — sidebar priority(0) | PASS |
| B — preview priority(1) | PASS |
| C — editor priority(1) | PASS |
| D — tools priority(0) | PASS |
| E — UI 全中文 preserved | GAP (informational — no UI strings touched, not a regression) |
| F — Boss 8/25 OOB quote in commit body | PASS |

**Overall**: 5 PASS + 1 informational GAP. Commit **satisfies the Boss 8/25 67th OOB spec** for ticket 015.071. layoutPriority modifiers are correctly applied to the 4 ZoneModule sites; toggle wiring untouched (Boss "这块代码不用动" honored); animation preserved (Boss "效果对了" honored).

**No blocking Spec-axis issues found.**

---

*Report written: 2026-08-26 (Spec axis subagent).*
*File path: `.scratch/spec-axis-015-071-651a219e.md`*
*No source files modified by this review.*