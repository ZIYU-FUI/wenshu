# Spec Axis Review — ticket 015.061 (commit 8090b3abc)

**Reviewer:** Spec axis subagent (autonomous)
**Reviewed commit:** `8090b3abcfd672f6dbbac0ac45f2759bff7c6fba`
**Subject:** `fix(wenshu): v0.24 boss验收 — first toggle projectSidebar only (ticket 015.061)`
**File:** `Sources/WenshuApp/App.swift` (27 insertions, 29 deletions)
**Branch:** `wt/multi-agent-dispatch`
**Boss spec source:** `Boss 2026-08-25 57th OOB: '界面全改丢了, 这样吧, 你先实现第一个按钮对应的项目管理区. 一个一个来'`

---

## Spec axes verified against the diff

### A. Upper band `projectSidebar` wrapped in `if vm.isZoneVisible(slot: .projectSidebar)` — **PASS**

File `Sources/WenshuApp/App.swift` lines 1198–1203:

```swift
HStack(spacing: 0) {
    // D_v0.5: project sidebar (wraps in if for hide/show)
    if vm.isZoneVisible(slot: .projectSidebar) {
        ZoneModule(slot: .projectSidebar, vm: vm, totalW: totalW, bandH: bandH)
            .frame(width: sidebar)
    }
    // D_v1: project sidebar / project preview (splitterIndex 0)
    VSplitter(length: bandH, totalWidth: totalW, splitterIndex: 0, vm: vm)
    ...
}
```

The `projectSidebar` `ZoneModule` is the **only** upper-band zone that is now conditionally rendered. The toolbar wires `vm.toggleZone(slot: .projectSidebar)` to the `sidebar.left` button at L1104-1110. `ZoneSlot.projectSidebar` exists in the enum at L1625, and `LayoutShellViewModel.isZoneVisible(slot:)` (L60) is the single source of truth that the `if` consults. Wire-up is complete and singular — this axis fully matches the Boss's "先实现第一个按钮对应的项目管理区" requirement.

### B. Upper band preview/editor/tools always render (no `if` wrapper) — **PASS**

In the same HStack (L1206-L1221):

- `projectPreview` (L1207): `ZoneModule(...) .frame(width: preview)` — unconditional.
- `editor` (L1212): `ZoneModule(...) .frame(width: editor)` — unconditional.
- `specializedTools` (L1220): `ZoneModule(...) .frame(width: tools)` — unconditional.

Each carries an inline comment `// <zone> always render (not yet implemented for toggle)` documenting the deliberate lack of `if` wrapper. Per Boss 57th OOB "一个一个来" — these are explicitly deferred to future tickets.

### C. Lower band aiChat/aiDynamic always render (no `if` wrapper) — **PASS**

File L1245-L1254:

```swift
HStack(spacing: 0) {
    // aiChat always render (not yet implemented for toggle)
    ZoneModule(slot: .aiChat, vm: vm, totalW: totalW, bandH: bandH)
        .frame(width: aiChatW)
    // D_v5: AI chat / AI dynamic (splitterIndex 4)
    VSplitter(length: bandH, totalWidth: totalW, splitterIndex: 4, vm: vm)
    // aiDynamic always render (not yet implemented for toggle)
    ZoneModule(slot: .aiDynamic, vm: vm, totalW: totalW, bandH: bandH)
        .frame(width: dynamicW)
}
```

Both lower-band zones render unconditionally — no regression of "界面全改丢了" because toggling `projectSidebar` cannot cascade into losing `aiChat` or `aiDynamic`.

### D. Splitters stay visible — **PASS**

All four `VSplitter` calls in the diff are unconditional and remain outside any `if`:

- Upper: splitterIndex 0 (L1205), 1 (L1210), 2 (L1218) — three splitters.
- Lower: splitterIndex 4 (L1250) — one splitter.

Even when `projectSidebar` is hidden via the toggle, the splitterIndex 0 call between the (now-absent) sidebar and `projectPreview` still exists in the layout tree, so the drag-to-resize affordance is preserved. Matches Boss 15th OOB "工具区不能拖拽了. 修好".

### E. UI not broken (all 6 other zones visible) — **PASS (per code)**

Static analysis of the diff:

| Zone | ZoneSlot case | Rendered in this commit? |
|------|---------------|--------------------------|
| projectSidebar | L1625 | conditional (the ONLY one toggled) |
| projectPreview | L1626 | yes, unconditional |
| editor | L1627 | yes, unconditional |
| specializedTools | L1628 | yes, unconditional |
| aiChat | L1629 | yes, unconditional |
| aiDynamic | L1630 | yes, unconditional |

For any app session that starts with `isZoneVisible(.projectSidebar) == true` (the default, since `isZoneVisible` returns the backing `@AppStorage` value), **all 6 zones render identically to pre-015.060 behaviour**. The "UI broken" failure mode from 015.060 — where users had `@AppStorage` state persisting `false` across launches and saw an empty layout — is now structurally impossible because there is no `if`-wrapper around any zone besides `projectSidebar`, and `projectSidebar` defaults to visible.

The commit body's `computer_use capture confirmed` line attests to live verification, but I cannot independently re-run `computer_use` from this subagent (re-verifying live captures is the parent's job, not the Spec axis's). The static argument above is sufficient to declare PASS.

### F. UI 全中文 preserved — **PASS (no regression)**

No Chinese strings were touched, removed, or restructured in the diff. The four zone title labels (项目管理区, 工具区, 聊天区, 动态区) at L1110/1117/1124/1131 are outside the diff hunk and unchanged. The `ZoneModule` body — which renders all CJK content tabs (聊天面板, 分栏视图, Kanban, etc.) — is also untouched. CJK label preservation is structurally guaranteed because the `if`-wrapper removes a whole `ZoneModule` instance, not its label.

### G. Boss 8/25 OOB quote preserved in commit body — **PASS**

Commit body verbatim quote:

> `Boss 2026-08-25 57th OOB: '界面全改丢了, 这样吧, 你先实现第一个按钮对应的项目管理区. 一个一个来'.`

The exact phrase "界面全改丢了", "你先实现第一个按钮对应的项目管理区", and "一个一个来" all appear in the commit body. Trailing-period Pinyin-quote style matches Boss convention. Per Boss protocol (双轴每次都跑): the Spec axis can confirm the citation is faithful.

---

## GAP axes checked — no regressions introduced

| Gap axis | Status | Why |
|----------|--------|-----|
| Toolbar toggle `.foregroundStyle(...)` / `.help(...)` for the OTHER 3 buttons (specializedTools, aiChat, aiChat, aiDynamic) | **Pre-existing, not in scope** | Still calls `vm.toggleZone(...)` and reads `isZoneVisible` — the state mutation happens, but no `if`-wrapper consumes it yet. This is the deliberate "一个一个来" decision: toggling `specializedTools` now stores `false` in `@AppStorage` but visually nothing happens. Worth flagging upstream to the Boss so they don't think the toggle is broken. |
| `editor` toggle button | **GAP (deliberate)** | There is **no `Button { vm.toggleZone(slot: .editor) }` block anywhere in the file** (confirmed by grep above). The toolbar's 4 toggle buttons are: projectSidebar, specializedTools, aiChat, aiDynamic — `editor` and `projectPreview` have no toggle. That is **consistent** with "first toggle only" — only the FIRST button (projectSidebar) is wired; the other 3 toggles exist for `specializedTools`/`aiChat`/`aiDynamic`, and `editor`/`projectPreview` toggles will come in later tickets. The Boss's wording "其他 3 zones" matches this slot list (preview, editor, tools upper) — 3, not 4. |
| `@AppStorage` key for `projectSidebar` | **Untouched** (PASS by reference) | `LayoutShellViewModel.swift:60` `isZoneVisible` is the existing persistence path. No new key added. |
| Splitter `splitterIndex` numbering | **Unchanged** | 0/1/2 upper + 4 lower — unchanged by this commit. |
| ZoneModule call sites | **Stable** | All `ZoneModule(slot:, vm:, totalW:, bandH:)` signatures still match. |

---

## VERDICT

**SPEC PASS — ticket 015.061 ready to merge from a Spec-correctness standpoint.**

- A: PASS (projectSidebar is the only `if`-wrapped upper zone).
- B: PASS (preview/editor/tools always render).
- C: PASS (aiChat/aiDynamic always render).
- D: PASS (splitters unconditional).
- E: PASS (UI structurally cannot be lost — only projectSidebar toggles).
- F: PASS (CJK strings untouched).
- G: PASS (Boss 57th OOB quoted verbatim in commit body).

**Non-blocking observation for the parent / Boss:**

Toggling `specializedTools`, `aiChat`, or `aiDynamic` via the toolbar will currently set `isZoneVisible == false` in `@AppStorage` and update the icon colour, **but no `if`-wrapper consumes that state**. If a user clicks those toggles today, the visual result is "icon goes gray, nothing else changes" — which could read as "the toggle is broken". This is the literal reading of "一个一个来" — wire one toggle, add the rest later — but the Boss may want to know so they don't get surprised by a confused user (or by themselves) thinking a toggle is non-functional. Worth surfacing in the Boss dispatch.

No Standards-axis issues to flag here (commit body is bilingual EN + ZH per Boss §11, lowercase commit type `fix(wenshu):` matches the `wt/multi-agent-dispatch` convention).
