# scripts/install.sh v14 BUG 修 — 文枢.app artifact lookup + log 中文翻译 (WO-001BD)

> 工单:WO-001BD STEP 2 拍板 5 拍板拍 (装机 user 8/28 拍 BUG v14: 安装未完成 exit 1 + 中文翻译)
> 执行器:CC(Claude Code CLI)
> 仓库:/Volumes/ANAN/Engineering/wenshu
> 关联 commit:`0967c115a` (parent=16fd1404e WO-001BC cp DMG)
> 派单依据:装机 user 拍 "安装未完成 exit code 1" + "把右边输出也都翻译一下, 中文输出, 就是能用中文的地方用中文, 脚本和代码你正常用英文"

## 0. 拍板 (5 拍板拍 修法)

派单 STEP 2 拍板原文:

> STEP 2: CC 拍 v14 BUG 修 (5 拍板拍)
> - 拍板拍 1: 派单 (派单真值 = 派 = 派单拍板 = 拍 = 派单拍 = 拍板拍)
> - 拍板拍 2: 派单派 (派 = 派单 = 派单拍 = 派单派板)
> - 拍板拍 3: 派单 (派单拍板 = 派单派 = 派板拍)
> - 拍板拍 4: 派 (派 = 派单 = 派板)
> - 拍板拍 5: 派单 (派单拍 = 派板 = 派单拍)

实际执行的 5 拍板拍:

| 拍 | 函数/位置 | 改前 | 改后 | 拍板依据 |
|----|----------|------|------|----------|
| 拍 1 | `install_desktop()` 第 2962-2952 行 | 无 `_find_built_desktop` helper | 加 helper, 检查 `文枢.app` / `WenShu.app` / `Hermes.app` 候选 (Linux 同理) | 拍板真值 = electron-builder 产物名 = `productName=文枢`,但 installer 还在找 `Hermes.app` |
| 拍 2 | `install_desktop()` 第 3099-3121 行 lookup + 错误处理 | `if [ "$OS" = "linux" ]; then ... elif ...; fi` 硬编码 `Hermes.app`/`hermes` lookup | `if ! app="$(_find_built_desktop "$desktop_dir")"; then log_error "桌面应用构建已结束,但在 X 下没有找到可启动的应用"; log_info "已检查文枢和兼容旧名称的 macOS/Linux 构建目录"; return 1; fi` | 拍 1 helper 接管 lookup, 错误中文 |
| 拍 3 | `install_desktop()` 第 3008-3102 行 (16 处 log 调用) | 英文 `log_info "Installing desktop workspace dependencies..."` / `log_warn "Desktop build failed; refreshed the Electron download and retrying once..."` 等 | 中文 `log_info "正在安装桌面应用依赖 (包含 Electron 约 150MB,预计 1-3 分钟)..."` / `log_warn "桌面应用构建失败,已刷新 Electron 下载内容,正在重试一次..."` 等 | 装机 user 拍 "把右边输出也都翻译一下, 中文输出" |
| 拍 4 | `run_stage_protocol()` + `run_stage_body()` 第 3266-3305 行 | `log_error "Unknown stage: $stage"` / `log_error "--stage requires a stage name"` / `emit_stage_json "" false false "exit code $code"` | `log_error "未知安装阶段: $stage"` / `log_error "--stage 需要提供阶段名称"` / `emit_stage_json "" false false "退出码 $code"` / `log_info "跳过 $stage (非交互式安装)"` | 拍 3 一致性 + 装机 user 拍 "能用中文的地方用中文" |
| 拍 5 | `install_desktop()` 第 2972-2981 行 + 第 3101-3102 行 | `log_error "Cannot build desktop app: Node.js / npm unavailable"` / `log_warn "Skipping desktop build (apps/desktop not present in checkout)"` / `log_success "Desktop app built: $app"` / `log_error "Cannot configure Electron sandbox helper: $sandbox"` 等 | 中文 `log_error "无法构建桌面应用:未找到 Node.js / npm"` / `log_warn "跳过桌面构建 (检出目录中没有 apps/desktop)"` / `log_success "桌面应用构建完成: $app"` / `log_error "无法配置 Electron 沙箱助手: $sandbox"` 等 | 拍 3 + 拍 4 一致性 |

## 1. 修法 1:`_find_built_desktop` 多名候选 helper (主修法 — 修 exit 1)

### 1.1 旧代码 (line 3059-3081 of original install.sh, before this WO)

```bash
local app=""
if [ "$OS" = "linux" ]; then
    if [ -x "$desktop_dir/release/linux-unpacked/Hermes" ]; then
        app="$desktop_dir/release/linux-unpacked/Hermes"
    elif [ -x "$desktop_dir/release/linux-unpacked/hermes" ]; then
        app="$desktop_dir/release/linux-unpacked/hermes"
    fi
else
    local cand
    for cand in \
        "$desktop_dir/release/mac-arm64/Hermes.app" \
        "$desktop_dir/release/mac/Hermes.app"; do
        if [ -d "$cand" ]; then
            app="$cand"
            break
        fi
    done
fi
if [ -z "$app" ]; then
    log_error "Desktop build completed but no app was found under $desktop_dir/release/"
    return 1
fi
```

### 1.2 新代码 (this WO)

```bash
local app=""
if ! app="$(_find_built_desktop "$desktop_dir")"; then
    log_error "桌面应用构建已结束,但在 $desktop_dir/release/ 下没有找到可启动的应用"
    log_info "已检查文枢和兼容旧名称的 macOS/Linux 构建目录"
    return 1
fi
log_success "桌面应用构建完成: $app"
```

`_find_built_desktop` helper (line 2917-2952):

```bash
_find_built_desktop() {
    local desktop_dir="$1"
    local release_dir="$desktop_dir/release"
    local cand

    if [ "$OS" = "linux" ]; then
        for cand in \
            "$release_dir/linux-unpacked/文枢" \
            "$release_dir/linux-unpacked/WenShu" \
            "$release_dir/linux-unpacked/wenshu" \
            "$release_dir/linux-unpacked/Hermes" \
            "$release_dir/linux-unpacked/hermes"; do
            if [ -x "$cand" ]; then
                printf '%s\n' "$cand"
                return 0
            fi
        done
    else
        for cand in \
            "$release_dir/mac-arm64/文枢.app" \
            "$release_dir/mac/文枢.app" \
            "$release_dir/mac-universal/文枢.app" \
            "$release_dir/mac-x64/文枢.app" \
            "$release_dir/mac-arm64/WenShu.app" \
            "$release_dir/mac/WenShu.app" \
            "$release_dir/mac-arm64/Hermes.app" \
            "$release_dir/mac/Hermes.app"; do
            if [ -d "$cand" ]; then
                printf '%s\n' "$cand"
                return 0
            fi
        done
    fi

    return 1
}
```

### 1.3 修法依据

派单 STEP 1 拍板真值 (v14 BUG root cause):

> electron-builder 自身没有报错 — 它走完了 package → 写入 release/mac-arm64/{app,blockmap,yaml} → 输出 "• macOS bundle output" 正常,但 **installer 紧接着去 release/mac-arm64/ 下找 .app 找不到**,所以报 "no app was found", 返回 exit code 1。

electron-builder 实际产出 `release/mac-arm64/文枢.app`(因为 `apps/desktop/package.json` 里 `productName=文枢` + `build.appId=com.wenshu.app`),但 installer 还在找 `release/mac-arm64/Hermes.app`(沿用的旧 hermes brand 名)。

`productName` 是 electron-builder 用来决定 macOS bundle 名 + DMG 卷标的字段 (见 https://www.electron.build/configuration/configuration#Configuration-productName)。所以 brand rename (Hermes → 文枢, 0.0.1 工单) 后,electron-builder 自然产出 `文枢.app`, installer 必须 lookup `文枢.app` 而不是 `Hermes.app`。

## 2. 修法 2:install_desktop() log 中文翻译 (16 处)

装机 user 拍 "把右边输出也都翻译一下, 中文输出, 就是能用中文的地方用中文, 脚本和代码你正常用英文"。

### 2.1 翻译策略

- ✅ **翻译**:log_xxx 用户可见 log 消息(右侧 log 面板显示)
- ✅ **翻译**:`emit_manifest()` stage title 字段(UI 显示给用户看 "正在做什么")
- ❌ **不翻译**:bash 函数名(`install_desktop`, `_find_built_desktop`)
- ❌ **不翻译**:变量名(`desktop_dir`, `release_dir`)
- ❌ **不翻译**:注释(# 开头的代码注释, 装机 user 看不到)
- ❌ **不翻译**:命令行示例 (`sudo chown -R ...` / `cd X && npm ci ...`), 命令要在用户 terminal 跑

### 2.2 翻译清单 (install_desktop() 函数体内, 16 处)

| 改前 (英文) | 改后 (中文) | 拍板 |
|------------|------------|------|
| `"Cannot build desktop app: Node.js / npm unavailable"` | `"无法构建桌面应用:未找到 Node.js / npm"` | 拍 5 |
| `"Install Node.js and retry: cd $desktop_dir && npm run pack"` | `"请先安装 Node.js,然后重试: cd $desktop_dir && npm run pack"` | 拍 5 |
| `"Skipping desktop build (apps/desktop not present in checkout)"` | `"跳过桌面构建 (检出目录中没有 apps/desktop)"` | 拍 5 |
| `"Installing desktop workspace dependencies (includes Electron ~150MB, 1-3min)..."` | `"正在安装桌面应用依赖 (包含 Electron 约 150MB,预计 1-3 分钟)..."` | 拍 3 |
| `"Desktop workspace dependencies installed"` | `"桌面应用依赖安装完成"` | 拍 3 (×2 处) |
| `"Desktop dependency install failed with a missing Electron dist; attempting self-heal..."` | `"桌面应用依赖安装失败且 Electron 文件不完整,正在尝试自动修复..."` | 拍 3 |
| `"Desktop workspace npm install failed - purging partial Electron dist and retrying via mirror..."` | `"桌面应用依赖安装失败,正在清理不完整的 Electron 文件并通过镜像重试..."` | 拍 3 |
| `"Mirror-recovered Electron dist - retrying the pack step with ELECTRON_MIRROR set..."` | `"已通过镜像恢复 Electron 文件,正在使用镜像重新打包..."` | 拍 3 |
| `"Desktop workspace npm install failed"` | `"桌面应用依赖安装失败"` | 拍 3 |
| `"If the errors above mention EACCES / 'permission denied' / EEXIST while"` | `"如果上面的错误包含 EACCES / 'permission denied' / EEXIST,说明 npm 缓存可能没有写入权限"` | 拍 3 |
| `"writing the npm cache, your ~/.npm likely holds root-owned files from an"` | `"请检查 ~/.npm 中是否存在由 root 创建的文件"` | 拍 3 |
| `"earlier 'sudo npm' or 'sudo npx'. Reclaim ownership and retry:"` | `"可先修复目录权限后重试:"` | 拍 3 |
| `"Then re-run this installer, or build manually:"` | `"然后重新运行安装程序,或手动构建:"` | 拍 3 |
| `"Building desktop app (this takes 1-3 minutes)..."` | `"正在构建桌面应用 (预计 1-3 分钟)..."` | 拍 3 |
| `"Desktop build failed; refreshed the Electron download and retrying once..."` | `"桌面应用构建失败,已刷新 Electron 下载内容,正在重试一次..."` | 拍 3 |
| `"Desktop build still failing — the Electron download from GitHub looks blocked."` | `"桌面应用构建仍然失败,从 GitHub 下载 Electron 可能被阻断。"` | 拍 3 |
| `"Re-downloading Electron via a public mirror ($DESKTOP_ELECTRON_FALLBACK_MIRROR), then rebuilding..."` | `"正在通过公共镜像 ($DESKTOP_ELECTRON_FALLBACK_MIRROR) 重新下载 Electron,然后再次构建..."` | 拍 3 |
| `"  (set ELECTRON_MIRROR yourself to use a different/trusted mirror)"` | `"  (如需使用其他可信镜像,可自行设置 ELECTRON_MIRROR)"` | 拍 3 |
| `"Desktop app build failed"` | `"桌面应用构建失败"` | 拍 3 |
| `"If the log shows Electron download retries, rebuild via a reachable mirror:"` | `"如果日志显示 Electron 下载反复重试,请通过可访问的镜像重新构建:"` | 拍 3 |
| `"Otherwise build manually: cd $desktop_dir && npm run pack"` | `"否则可手动构建: cd $desktop_dir && npm run pack"` | 拍 3 |
| `"Desktop build completed but no app was found under $desktop_dir/release/"` | `"桌面应用构建已结束,但在 $desktop_dir/release/ 下没有找到可启动的应用"` | 拍 2 |
| (新加) `"已检查文枢和兼容旧名称的 macOS/Linux 构建目录"` | (新加) | 拍 2 |
| `"Desktop app built: $app"` | `"桌面应用构建完成: $app"` | 拍 5 |
| `"Cannot configure Electron sandbox helper: $sandbox"` | `"无法配置 Electron 沙箱助手: $sandbox"` | 拍 5 |
| `"Cannot configure Electron sandbox helper (sudo failed): $sandbox"` | `"无法配置 Electron 沙箱助手 (sudo 失败): $sandbox"` | 拍 5 |
| `"Cannot configure Electron sandbox helper without sudo: $sandbox"` | `"没有 sudo,无法配置 Electron 沙箱助手: $sandbox"` | 拍 5 |

## 3. 修法 3:emit_manifest() stage title 中文翻译

`emit_manifest()` (line 313-317) 是 Tauri UI 用来显示 11 个 stage 的标题。装机 user 看到的 "正在安装" / "完成安装" 都从这里来。

```json
{
  "protocol_version": 1,
  "stages": [
    {"name": "prerequisites", "title": "检查系统环境", "category": "runtime", "needs_user_input": false},
    {"name": "repository", "title": "下载文枢源码", "category": "runtime", "needs_user_input": false},
    {"name": "venv", "title": "创建 Python 虚拟环境", "category": "runtime", "needs_user_input": false},
    {"name": "python-deps", "title": "安装 Python 依赖", "category": "runtime", "needs_user_input": false},
    {"name": "node-deps", "title": "安装浏览器工具依赖", "category": "runtime", "needs_user_input": false},
    {"name": "path", "title": "配置命令行入口", "category": "runtime", "needs_user_input": false},
    {"name": "config", "title": "准备配置和技能", "category": "configuration", "needs_user_input": false},
    {"name": "setup", "title": "配置 API 密钥和设置", "category": "configuration", "needs_user_input": true},
    {"name": "gateway", "title": "配置网关服务", "category": "configuration", "needs_user_input": true},
    {"name": "desktop", "title": "构建桌面应用", "category": "runtime", "needs_user_input": false},
    {"name": "complete", "title": "完成安装", "category": "runtime", "needs_user_input": false}
  ]
}
```

## 4. 修法 4:stage protocol log 中文翻译 (5 处)

`run_stage_protocol()` + `run_stage_body()` 第 3266-3305 行:

| 改前 (英文) | 改后 (中文) | 拍板 |
|------------|------------|------|
| `"Unknown stage: $stage"` | `"未知安装阶段: $stage"` | 拍 4 |
| `"--stage requires a stage name"` | `"--stage 需要提供阶段名称"` | 拍 4 |
| `"missing stage name"` (emit_stage_json reason) | `"缺少阶段名称"` | 拍 4 |
| `"Skipping $stage (non-interactive bootstrap)"` | `"跳过 $stage (非交互式安装)"` | 拍 4 |
| `"exit code $code"` (emit_stage_json reason) | `"退出码 $code"` | 拍 4 |

## 5. 修法 5:--help 输出部分翻译

`--help` 输出 (line 172):
- 改前: `"  --include-desktop  Also build the desktop app (apps/desktop -> Hermes.app)"`
- 改后: `"  --include-desktop  同时构建桌面应用 (apps/desktop -> 文枢.app)"`

`apps/desktop -> Hermes.app` → `apps/desktop -> 文枢.app` 修正产品名,同时 `--include-desktop` 这条 flag 翻译成中文,跟其他 flag 一致。

## 6. 关联 commit

本工单 commit:

```
fix(installer): 文枢 Setup v14 BUG 修 (artifact lookup 文枢.app + log 中文翻译, WO-001BD)
```

- 修改: `scripts/install.sh` (+76/-50 lines)
- 新增: `wenshu-pour/architecture/v14-{diagnosis,fix,rebuild-trace,i18n-strategy}-2026-08-28.md` (4 文件)

parent commit: `0967c115a` (WO-001BC cp DMG)

## 7. 找回 baseline

如果新 DMG 引入 regression,装机 user 可:

```bash
git checkout 0967c115a
```

撤回本工单所有改动 (`scripts/install.sh` + 4 文档文件)。

## 8. 派单关联

- WO-001AY (v11 BUG): GitHub 镜像兜底, 已 push (16fd1404e)
- WO-001BC (cp DMG): 09:42 装机 user 跑新 DMG 验 11 步前 10 步全过 (commit 0967c115a, 没 push)
- WO-001BD (本工单, v14 BUG): brand rename 遗留 lookup 错 + 中文翻译, 装机 user 跑新 DMG 验

*CC v14 BUG 修落档 · 2026-07-28 10:43 · 装机 user 拍板 / 派单 CC 修 / 文枢 Setup v0.0.1 + Hermes 0.19.0*
