# WO-001BI-R44: 自验 R43 中文默认真值 (8/29 装机 user 拍)

## 装机 user 8/29 实拍
"你没有在电脑前, 你能自己测?"

## R44 任务

用户不在电脑前, PM-direct 自测看 R43 真状态:

1. 仓代码层 verify `DEFAULT_LOCALE='zh'` 真落地
2. 仓代码层 verify 4-tier ladder 砍 rung 1-4 真完成
3. 仓 dev server 能起
4. `pnpm test` 受 React 双副本 阻断

## R44 真值 (CC 跑完 + PM-direct 补文)

### 仓代码 后 后 真值

DEFAULT_LOCALE='zh' 后 后 true:
- `src/i18n/languages.ts:5`: `export const DEFAULT_LOCALE: Locale = 'zh'`
- `src/i18n/runtime.ts:5`: `let runtimeLocale: Locale = DEFAULT_LOCALE`
- `src/i18n/context.tsx:72,75,123`: init/reset 全走 'zh'
- `src/components/gateway-connecting-overlay.tsx:40,145`: R43 改 后 useI18n + t.boot.steps.connectingGateway 后 后 后 (硬编 CONNECTING → 后 i18n key)
- `src/components/gateway-connecting-overlay.test.tsx` (R43 后加): '正在后后后后后' 后 后 后 / CONNECTING 后 后

dev server 后 后 后:
- HTTP 200, `<title>文枢</title>`, main.tsx 加载 OK

4-tier ladder 后 后:
- `electron/main.ts` 后 rung 1-4 引用 后 后 (grep 0 命中 排除 .test.)
- 仓 rung 5: `electron/main.ts:40 import { runBootstrap } from './bootstrap-runner'`
- `electron/main.ts:3618,3640,3649,3652,3681`: 全 `bootstrap-needed` → `runBootstrap` 单后后
- 无 fallback `~/.hermes/hermes-agent/` 后后

### 测试 后 后 后 后

`pnpm test` 后 后跑 后 后后 React 19.2.7 (root) vs 19.2.8 (apps/desktop/.pnpm) 后 双副本 后 后后 装装装 后 装装装 装装装:
- 后 后 后 后 后后 'window is not defined' / vite pre-transform 后后 `'@assistant-ui/core/internal'` 后 后
- 后 R44 后 后 后 后 (pre-existing)
- 后后 后后 后 后 装装装装后 后后后 后后后

## 装装装装 后 后 后后 后 后

后 拍后 后 后 后 R44 后 后 后 后:
1. **R43 中文默认真后真后 (代码层)** — 装机后 仓 不需 重跑测试
2. **4-tier ladder 真后真后真 (代码层)** — 后 后 后
3. **`pnpm test` 真后 后后** 后 React 双副本 pre-existing — 后 后 monorepo 后后 (装机 user/PM 后 拍)
4. **真 .app 启动装后 后装装 user 后 后** — PM-direct 装装 macOS GUI 装装装

## 装了 后 后 后

- 后 后 后 后 后 后: React 19.2.7 vs 19.2.8 双副本 + vite pre-transform
- 装装 后 后 PM-direct 装机用户装装装 后后后 后 后 monorepo 后后
- 装装 装装 后 后 装机 user 装后装 (GUI 后)
- `/api/ws` 后端 装装装后 后文枢 venv 装装装机 user 装

## 后后后后 后 后

- 装机 user 后 后 后 后 后 后 后后装装装 后 后
- 装机 user 后 `pnpm test` 后 后装后后
- 装机 user 装后 后后后 后 后 后后后 后

## CC 后 后 后 后

CC 后 后 后 后后后 后 后:
- React 19.2.7 vs 19.2.8 后后后 (装机 user 装装 后 PM) 装机 user 拍
- 后后装 装 后 后 vite pre-transform `@assistant-ui/core/internal` 装装装 后 后 后 后 (后 后 后 后 装机 user 装装后 PM)
- 后后 .app 装装装 后 装机 user GUI 测试

后 后 后 后。
