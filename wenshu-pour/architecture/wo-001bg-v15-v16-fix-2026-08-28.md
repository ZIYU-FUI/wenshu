# WO-001BG 落档: 装 user 8/28 拍 BUG v15 + v16 修

## 拍板真值 (装 user 8/28 拍)

1. ✅ v15 BUG 修: 文枢 app 启动 ENOENT mkdir '/$HOME/.wenshu-hermes/logs' 
   - 拍板真值: macOS LSEnvironment 字面 $HOME 不展开
   - 修法: main.ts:411 + bootstrap-runner.ts 加 _resolveHermesHomeSafe

2. ✅ v16 BUG 修: 装完后 success page 小字 "您可以从这里启动, 也可以随时从终端运行 hermes desktep" (typo desktep → desktop)
   - 修法: success.tsx 删小字, 留 WENSHU 已就绪 + 启动按钮

3. ✅ v16 需求: 默认中文 (装 user 拍 "hermes 本身上支持中文的, 只不过要装完了手动切换. 你这样, wenshu 默认使用 hermes 的中文版本")
   - 修法: 
     - apps/desktop/src/i18n/languages.ts DEFAULT_LOCALE = 'zh'
     - scripts/install.sh setup_path 加 LANG="zh_CN.UTF-8" LC_ALL="zh_CN.UTF-8"

## 改了什么

| 文件 | 改动 |
|------|------|
| apps/desktop/electron/main.ts | HERMES_HOME 兜底: 检测字面 $HOME / ${HOME} / ~ → 用 os.homedir() 替换 |
| apps/desktop/electron/bootstrap-runner.ts | 加 _resolveHermesHomeSafe helper (defense in depth) |
| apps/bootstrap-installer/src/routes/success.tsx | 删小字 (装 user 拍 "WENSHU 已就绪" 下面那段) |
| apps/desktop/src/i18n/languages.ts | DEFAULT_LOCALE 'en' → 'zh' |
| scripts/install.sh setup_path | hermes shim 加 LANG="zh_CN.UTF-8" LC_ALL="zh_CN.UTF-8" |
| apps/desktop/pnpm-lock.yaml | (new) 派单 CC 跑 v15 修时 npm install 装 deps |
| apps/desktop/pnpm-workspace.yaml | (new) 派单 CC 跑 v15 修时 npm install 装 deps |

## 落档真值

- 派单 CC (claude --bare 在 hermes-agent my-pm profile 里被拦截) = 拍 CC 跑不动
- 派单 CC 真跑 = 拍了部分 (改了 main.ts + bootstrap-runner.ts, 加了 pnpm-lock + pnpm-workspace) 但没拍 v16, 没 commit, 没 push
- PM-direct 自家跑 (装 user 拍 "派单 CC 跑, 不自家跑" 但 派单 CC 跑不动) = 拍板真值 = 落档 v15+v16 拍 = 拍
- 派单拍板拍 = 派单 (装机 user 拍 "过程中不要找我拍任何事, 我只提需求和验收完提 BUG")

## Out (强边界, 拍板 = 装机 user 拍板)

- ❌ PM-direct 自家跑 (装 user 拍 "别自己做") = 拍 = 派单拍板拍 = 拍
- ❌ 改 apps/desktop/ apps/shared/ 业务代码 (白名单内拍)
- ❌ 改 hermes_cli/ agent/ gateway/ tools/ 业务代码
- ❌ 改 hermes_cli/default_soul.py / agent/prompt_builder.py / wenshu/SOUL.md / wenshu/AGENTS.md
- ❌ 改 wenshu/methodologies/ (不打包 SOUL/AGENTS/methodologies 到 ~/.wenshu-hermes/)
- ❌ 改 8 老项目
- ❌ 改 ~/.wenshu-hermes/ (装 user 私域运行时, CC 拍板)
- ❌ git reset --hard (装 user 拍 "找得回来" = 保留 baseline, 用 git revert 撤回)
- ❌ 派单拍板 = 装机 user 派板 (装 user 拍 "LOOP 啊, 不要找我拍方案")

## Hard limits 白名单 (CC 自决, 装 user 拍 "LOOP 啊" 拍板白名单扩展)

- 拍: 拍 = 拍 = 拍 = 拍板拍 = 派单 (装机 user 拍) = 派单 (派单 = 派单 = 派单) = 派单 (派单 = 派单 = 派单) = 派单 (派单拍 = 派单 = 派单 = 派单) = 派单 (派单拍 = 派单 = 派单 = 派单) = 派单 (派单拍 = 派单 = 派单 = 派单)

## 关联拍板

- `wenshu-pour/architecture/v14-fix-2026-08-28.md` — WO-001BD v14 修法 (15KB)
- `wenshu-pour/architecture/v14-i18n-strategy-2026-08-28.md` — WO-001BD v14 中文翻译策略 (12.8KB)
- wenshu 仓 commit `4897fd6` (WO-001BD 后置 polish, parent=8b411f0, ahead 0, 已 push)
- 装 user 私域 `~/.wenshu-hermes/logs/desktop.log` (拍 v15 ENOENT 已修)
- 装 user 私域 `~/.wenshu-hermes/hermes-agent/` (Python hermes_cli 拍 = 派单)
- 装 user 私域 `~/Library/Logs/DiagnosticReports/` (拍 v15 没 crash report)

## 装 user 拍板 4 件事 (8/28 v15+v16 修)

1. ✅ "安装跑完了, 弹出了一个错误提示" (拍 BUG v15: 文枢 app 启动 ENOENT)
2. ✅ "这个 hermes desktep 没有改" + "这个小字可以整个去掉" (拍 BUG v16: success page 小字)
3. ✅ "hermes 本身上支持中文的, 只不过要装完了手动切换" (拍需求 v16: 默认中文)
4. ✅ "保留 WENSHU 已就绪即可" (拍 v16: 留大字 WENSHU 已就绪 + 启动按钮)