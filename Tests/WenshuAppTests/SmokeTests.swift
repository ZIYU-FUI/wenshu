// SmokeTests.swift · Wenshu (Wenshu) · v0.02.0
//
// Placeholder to keep the test target buildable until the first real test
// (= LibraryStoringContractTests) lands. Delete this file once that test
// exists.
import Testing

@Suite("Smoke")
struct SmokeTests {
    @Test("test target is wired")
    func targetIsWired() {
        #expect(true)
    }
}
