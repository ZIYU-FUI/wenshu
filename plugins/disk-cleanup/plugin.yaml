name: disk-cleanup
version: 2.0.0
description: "自动跟踪并清理 Wenshu 会话期间产生的临时文件 (测试脚本, 临时输出, cron 日志). 通过 plugin hook 自动运行 — agent 不需操作."
author: "@LVT382009 (original), NousResearch (plugin port)"
hooks:
  - post_tool_call
  - on_session_end
