name: byterover
version: 1.0.0
description: "ByteRover — 持久化知识树, 通过 brv CLI 进行分层检索."
external_dependencies:
  - name: brv
    install: "curl -fsSL https://byterover.dev/install.sh | sh"
    check: "brv --version"
hooks:
  - on_pre_compress
